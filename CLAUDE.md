# Divya Yuddha — Battle of the Celestials

Mobile-first strategic card battle game based on Indian mythology (Ramayana, Mahabharata, Puranas).
Gwent-style best-of-3 rounds + Hearthstone-style card abilities. 4 factions, 88 cards at launch.
Solo developer (Sangbaran, Creative Director) with Claude as the engineering team.

## Tech stack (decided July 2026 — Unity was abandoned)
- Pure JavaScript rules engine, **zero framework dependencies**, runs in browser AND Node
- UI: vanilla JS + CSS (may migrate to a bundler/TS later; keep engine UI-agnostic)
- Testing: headless AI-vs-AI simulation via Node (`node src/test.js`)
- Mobile shipping plan: Capacitor wrap → iOS App Store + Google Play
- Multiplayer (later): Node backend, Colyseus or Nakama

## Repo layout
- `src/engine.js` — the rules engine. UI-agnostic. All game logic lives here.
- `src/test.js` — simulation harness: 500 AI-vs-AI matches + rule invariants. RUN THIS AFTER EVERY ENGINE CHANGE.
- `src/test_scenario.js` — `opts.scenario` smoke tests (47 checks): each field, the absent/empty no-op (incl. a byte-identical **g.rng draw-sequence** trace), and event observationality. Run after any setup/`newGame` change.
- `index.html` — playable prototype (engine is inlined by a build step; keep src/engine.js canonical)
- `docs/DESIGN_DECISIONS.md` — QA log, approved art, open rulings. Update when decisions are made.
- `docs/GDD_DELTAS.md` — **authority for card frame text**: every gameplay divergence from GDD v2.0 in printed-card-text form (rulings R1–R9, tuned numbers). Use this, not the GDD PDF, when arting frames.
- `assets/audio/` — `music_menu.mp3` (menu/faction-select), `music_battle.mp3` (match) gapless-looped via Web Audio buffers; **18 file SFX** on the non-limited file bus (fresh `AudioBufferSourceNode` per trigger → overlaps layer): Astra impacts `sfx_astra` / `sfx_brahmastra`; card-play `sfx_unit_clash` (Unit/Hero), `sfx_mantra` (om chant) / `sfx_heal` (Gayatri + Pavamana Deva restoration) / `sfx_artifact`; events `sfx_venom_apply` (Venom Token applied), `sfx_unit_destroy` (any Unit death, overlaps), `sfx_leap`, `sfx_shield_apply` / `sfx_shield_break`, `sfx_chaos_surge`; **power-change blips `sfx_buff` (power up) / `sfx_debuff` (power down, non-Venom) — coalesced, ≤2 overlapping**; **match-start `sfx_conch` (once per match), round-end gong `sfx_round_end` (rounds 1&2 only — suppressed at match end)**; result stingers `sfx_victory` / `sfx_defeat`. Pre-normalized (music −20 / astra −13 / brahmastra −11 / clash −15 / stingers −14 / events −15 / shield ding −16 / buff+debuff −17 / conch+gong −14 LUFS, durations pre-trimmed) — do NOT add per-sound gain compensation. **The synth `cardplay` is fully retired from all card-play paths** (every card type now maps to a file); **round-end is now a file too** (the synth `roundwin`/`roundloss` remain only as the stalemate cue + Test-SFX entries). `playFile` hardened: a decoded-but-absent buffer (missing file) gives up silently instead of retry-spinning.
- `assets/img/menu_bg.jpg` — shared background for the **landing page AND faction-select** (same frameless treatment: `center/cover`, `brightness(.9)`, soft bottom gradient; the `#factionselect, #landing` selectors share it).
- `assets/img/board_bg.jpg` — battle-arena background (896×1344, golden mandala at vertical center → aligns with the divider via `center/cover` on `#field`). **Canonically Mrityulok's board, and the lazy-load/404 fallback** for every realm. Per-realm boards `board_patala.jpg` / `board_swarga.jpg` (pre-cropped, mandala at 50%/50% so the same center-anchored CSS works) are mapped in `REALM_BOARDS` (see the per-realm-boards feature note); add a new realm's board as a one-line map entry.
- `assets/cards/` — final framed card PNGs. **Two filename patterns** by card type:
  - **Powered** (Units/Heroes, `base>0`): `{Faction}_{Type}_{Name}_P{printedPower}_r{RarityName}.png`
  - **Powerless** (Astra/Mantra/Artifact, `base===0`): `{Faction}_{Type}_{Name}_r{RarityName}.png` — **no power segment** (both patterns end in `_r{Rarity}.png`; no `_png` suffix anywhere)
  - Faction is the plural (`Devas`, `Asuras`, `Vanaras`, `Nagas`); Type is Title-cased (`Hero`/`Unit`/`Astra`/`Mantra`/`Artifact`); **Name segment = the card name with ALL non-alphanumeric characters removed** (`c.n.replace(/[^A-Za-z0-9]/g,'')` — strips spaces AND apostrophes etc.: `Rama's Signet`→`RamasSignet`, `Surya Dev`→`SuryaDev`, `Ashwini Kumars`→`AshwiniKumars`); power is the card's **printed/base** power; Rarity is the **full name** (`Legendary`/`Mythic`/`Epic`/`Rare`/`Uncommon`/`Common`).
  - `cardArtSrc` (in index.html UI) branches on `c.base > 0` to pick the pattern.
  - Examples: `Devas_Hero_Indra_P7_rLegendary.png`, `Devas_Unit_SuryaDev_P6_rEpic.png`, `Devas_Astra_Vajra_rLegendary.png`, `Asuras_Hero_Mahabali_P8_rLegendary.png`
- `assets/thumbs/` — **200px-wide JPEG thumbnails** of every card frame (SAME filenames, JPEG bytes in the `.png` name — browsers sniff content), ~20 KB avg / ≤25 KB each, used by the Faction Introduction gallery (the full frames are ~1 MB each — far too heavy to grid). Generated by **`scripts/make_thumbs.sh`** (macOS `sips --resampleWidth 200 -s format jpeg -s formatOptions 60`). The script has a **second step** that also thumbnails the **7 realm board images** (`assets/img/board_*.jpg` → `assets/thumbs/board_*.jpg`, **320px-wide q50, ~25–38 KB** — the Realms tab shows them uncropped at ~220px so 320px keeps them crisp incl. retina) for the Realm Introduction banners. **RE-RUN `bash scripts/make_thumbs.sh` whenever a card frame OR a board image is re-exported/replaced** (e.g. the four pending card re-exports: Chandrahas, Hanuman, Neela, Angad) so the thumbnail stays in sync. The UI derives a thumb URL by swapping `assets/cards/`→`assets/thumbs/` on the `cardArtSrc` output.
- The full GDD v2.0 PDF is the design authority (in the owner's Claude Project / Google Drive)

## Engine architecture rules
1. Engine stays headless — no DOM access in engine.js, ever
2. Every card ability is dispatched by card `id` in `playCard()` / passives computed in `effPower()` and helpers
3. All randomness goes through `g.rng` (injectable for seeded, reproducible tests)
4. Every rule change requires `node src/test.js` to pass all invariants before committing
5. Cross-faction rulings (GDD Section 9) must be implemented as explicit code paths with comments citing the ruling
6. **Counter-effects and protective effects: implement the exact printed text — the weakest defensible reading — and flag any strengthening or simplification to the owner.** Fidelity bugs in counters have caused every major balance distortion in this project (Dharma Shield, Karkotaka R10, Rama's Signet, Pavamana candidate).
7. **Event stream (`g.events`) is OBSERVATIONAL, never causal.** Every damage/buff/destroy/revive/trigger/token/faction-passive calls `emit(g,type,{sourceUid,targetUids,amount,abilityName,text})` alongside its `log()`. `emit()` must never touch game state or `g.rng` — outcomes must stay byte-identical whether or not `g.events` is consumed (verify: harness win-rates unchanged vs the LAUNCH BASELINE). The flat `g.log` stays as the backup channel. The UI (`index.html`) choreographs `g.events` **sequentially** via `runAction()`/`playEvent()` (source pulse + ability callout → staggered per-target floaters/dissolves, faction toasts, input-locked with tap-to-skip); `prefers-reduced-motion` bypasses choreography for instant resolution + log.

## Game rules cheat sheet
- 22-card decks, draw 10 at start, +2 at rounds 2 and 3, no hand limit, **mulligan up to 3 before Round 1 (implemented — GDD §2.2)**
- **Each match is assigned one of the 7 Cosmic Realms (GDD §10) — an engine-level modifier; random by default, `opts.realm` fixes it for tests**
- **`opts.scenario` (Story Mode setup — the first post-freeze engine change, 2026-07):** additive `newGame` option, read ONLY when present (opts.realm precedent) — `{ p0Deck?, p1Deck?, p0Hand?, p1Hand? (ordered decklists/hands by card NAME, minted via `mkCard` for uid safety, deck injection = no shuffle), winTarget? (default 2), handSize? (default 10), draws? (between-round, default 2, realm bonus still added), mulligan? (0..3, 0 disables the phase) }`. Stored as `g.winTarget`/`g.drawCount`/`g.mulliganCount`; every default equals today's hardcoded value. **Absent/empty scenario is byte-identical** (behavior AND `g.rng` draw sequence) — proven by `src/test_scenario.js` (rng-trace) and the unchanged LAUNCH BASELINE. Opponent scripting needs nothing more: the story driver calls `playCard(g,1,…)`/`pass(g,1)` and may hand off to `aiTakeTurn(g,1)` mid-match.
- Play exactly 1 card per turn OR pass; once passed, out for the round; round ends when both pass
- Rounds: higher Yuddha Row total wins; best of 3
- Between rounds: Units and Artifacts CLEAR, Heroes PERSIST, unused hand carries forward, played cards never return
- Max 1 Hero played per round; max 1 Astra per turn; max 1 Artifact active
- Heroes are immune to Astras by default (exceptions: Sudarshana, Takshaka rule)
- Faction passives: Devas=Dharma Shield, Asuras=Chaos Surge, Vanaras=Leap, Nagas=Venom

## v0.1 simplifications (KNOWN, intentional — do not treat as bugs)
- Dharma Shield: the HUMAN Deva player **designates manually** (🛡 SHIELD button → tap a friendly Unit; sticky per EXP-A, Kavacha = 2 designations, **no auto-fallback if unused — unused is unused**). The AI keeps its designate-at-first-opportunity auto-pick (highest-power), so the balance baseline is untouched (`pl.manualShield` gates the two paths; `designateShield()` is the manual call).
- Board positioning: units occupy ordered slots (`pl.units` order = position); adjacency = array neighbours. Units-only (Heroes positionless); Devas/Asuras ignore position, Vanaras use it for Leap. Placement passed as `playCard(...position)`.
- Vayu: original GDD ability restored — displaces the highest enemy Unit out of formation (breaks Vanara Leap adjacency) and −2 power.
- All 4 factions implemented (88 cards): Devas + Asuras + Vanaras + **Nagas (complete — cards, §9, Venom pipeline, teal UI)**. Faction select in the UI; any combo playable. (Balance FROZEN for launch — see BALANCE.)
- `index.html` engine is generated from `src/engine.js` by `node src/build.js` (run after every engine change).

## RESOLVED RULINGS (2026-07, official — implemented in engine.js)
- **R1 — Death at 0:** a Unit whose power reaches 0 or less is DESTROYED (to discard, triggers Yama-style death effects). Enforced generically by `sweepDeaths()` so Venom-token ticks also kill.
- **R2 — Round lead:** Rounds 2/3 the previous round's WINNER plays first; on a drawn round, the same leader as before. (`endRound`: `prev.winner ?? firstThisRound`.)
- **R3 — Deva Soldier (redesigned):** "PASSIVE: While Indra is on the board, gains +1 power at the start of each of your turns." Implemented via `onTurnStart()`.
- **R4 — Amrita Kalasha (official):** "ON PLAY: Your lowest power Unit gains +2. If that unit is destroyed this round, it revives at 1 power (once)." (aegis flag, cleared on round reset.)
- **R5 — Tataka:** "lowest Unit on the board" excludes Tataka herself (a removal Unit shouldn't primarily suicide).
- **R6 — Leap adjacency:** Units only; Heroes on the Yuddha Row are never valid Leap sources. (Positioning is units-only; Heroes stay in their own positionless row.)
- **R7 — Angad:** while Angad is on the board, when the opponent plays an Astra they forfeit their next turn (`skipNext`). Stacks with Varuna's astra limit — both apply if both on board.
- **R8 — Kishkindha Oath:** proactive ward — play it on a friendly Vanara Unit; the next time that unit would be destroyed this round it survives at 1 power and all other friendly Vanara Units gain +1. Ward expires at round end if unused (`ward` flag).
- **R9 — Chandrahas (Asura):** ON PLAY, trigger one Chaos Surge (removes its dead turn). Also the standing PASSIVE.
- **R10 — Karkotaka (Naga, flag A):** while on board, the Venom drain ticks at the start of each OPPONENT turn during the round instead of once at round end (continuous attrition).
- **R11 — Patala Throne (flag B):** while active, Venom drain = −(1 + current round number). R1 −2, R2 −3, R3 −4.
- **R12 — Surasa (flag C):** one-shot trap on the opponent's next card — a Unit enters with −2 power and Surasa gains +2; an Astra is negated (Chaos Surge still triggers per §9). Trap expires after one card.
- **R13 — Venom Tokens (flag D):** drain their bearer at each tick regardless of side; Mrityunjaya's revived Unit pays the corruption cost.
- **R14 — Ananta Coil (flag E):** tokens persist for the rest of the MATCH, draining 1 from a random enemy Unit at each Venom tick.
- **R15 — Shesha (flag F):** at the start of the next round after a lost round, a random friendly Unit returns from discard to the board at full power.
- **R16 — Kaliya / Ashvatara (flag G):** Kaliya enters with +(current round − 1) power; Ashvatara's drain = current round number.
- (Four supplementary Naga engine rulings are logged below as **R17–R20**.)

## ASURA RULINGS (2026-07, official — see docs/ASURA_ROSTER.md)
- **Mahabali** "played for free" = the first Unit played the round after a *voluntary* Pass grants an immediate extra turn (tempo, not cost). Forced Pass (Tamasa) does not arm it.
- **Kumbhakarna** power does NOT count while asleep; wakes at the start of its owner's next turn, sweeps 3 damage to all enemy Units, then counts.
- **Nagastra Venom vs Hiranyakashipu** — immunity holds: venom ticks it but floors at 1; only Brahmastra/a Mantra removes it. Venom ticks at round end BEFORE scoring.
- **R5 — Tataka** (confirmed): "lowest Unit on the board" excludes Tataka herself (a removal Unit shouldn't primarily suicide); she can still hit your other Units.
- Chaos Surge / Chandrahas / §9 cross-faction paths — implemented; see engine `resolveAstra`/`chaosSurge`/`onAstraResolved`. §9 Manasa/Surasa now LIVE (Naga shipped).

## NAGA SUPPLEMENTARY RULINGS (R17–R20, official — docs/GDD_DELTAS.md)
- **R17 — Venom stacking** is ADDITIVE: perUnit drain = 1 + (Patala: +round) + (Vasuki on board & R3: +1) + (Venom Strike: +2, or +3 with Vasuki). Karkotaka is an orthogonal *timing* modifier.
- **R18 — Manasa** = on-board negation: while she is on the board the opponent's Astras are cancelled (effect + Chaos Surge). Surasa (R12) is the one-shot trap — the §9 asymmetry sides.
- **R19 — Astika** skips the next Venom tick and heals friendly Units +1.
- **R20 — Nagapasha** unbind is a turn-costing opponent action; an un-freed bound Unit scores 0 (and `totalPower` skips bound Heroes for the Takshaka §9 case).

## BALANCE — FROZEN FOR LAUNCH (2026-07). LAUNCH BASELINE — all future balance work diffs against this table.
`node src/test.js` — 10 matchups (4 mirrors + 6 crosses) × 500 AI-vs-AI sims. Decided-game win% (draws excluded):

| Matchup | Result | Band |
|---|---|---|
| Deva mirror | 49.8 / 50.2 | ✅ |
| Asura mirror | 53.6 / 46.4 | ✅ |
| Vanara mirror | 52.8 / 47.2 | ✅ |
| Naga mirror | 51.4 / 48.6 | ✅ |
| Devas vs Asuras | 47.7 / 52.3 | ✅ |
| Devas vs Vanaras | 45.1 / 54.9 | ✅ |
| Asuras vs Vanaras | 42.3 / 57.7 | ✅ |
| Devas vs **Nagas** | 40.9 / **59.1** | ◆ accepted counter-matchup |
| Asuras vs **Nagas** | 49.0 / 51.0 | ✅ |
| Vanaras vs **Nagas** | 57.3 / 42.7 | ✅ |

- **Band:** every cross sits in [40.9, 59.1]. **Deva-vs-Naga 59.1 is the one accepted >58 point** — the designated poison-vs-go-wide counter-matchup (design intent), reached with all counters fidelity-correct, so the number has clean provenance (not a bug).
- **Naga integration levers (all applied):** **EXP-L** Karkotaka per-turn tick → flat −1 (escalation only round-end). **EXP-L2** Karkotaka → a single flat −1 early tick on the first pass (killed the ~6×/round volume). **EXP-M** counter-piloting AI (Deva holds Pavamana to 2+ tokens/round-end; Vanara prioritises Signet vs Nagas; all AIs devalue go-wide vs Nagas — measurement of competent play, NOT a detune). Base −1 round-end drain is GDD-verbatim and was never touched.
- **Fidelity fixes during the pass (see rule #6 + GDD_DELTAS):** **Rama's Signet** was blanket-immunity → corrected to floor-at-1 + token-negation (Vanara-vs-Naga 63.8 → 57.3). **Pavamana** was missing clause (b) → restored +1 power per effect removed (Deva-vs-Naga 60.4 → 59.1).
- **Pavamana reads −18pt on played-vs-held in Deva-vs-Naga — EXPECTED, not a defect:** it's reactive damage-control tech; you only hold 2+ Venom Tokens once already losing, so playing it correlates with (not causes) losing.
- Pre-Naga tuning (unchanged, folded in): Chandrahas ON-PLAY Surge · Chaos Surge +3 on Astras+Mantras · +1 floor (EXP-J) · Kumbhakarna counts asleep (EXP-G) · Hanuman entry printed ≥4 (EXP-F) · Kishkindha Crown +1/+1 (EXP-E). All gameplay deltas live in **docs/GDD_DELTAS.md**.
- **Known design hole (owner, Season/errata):** GDD §9 says the Naga answers Rama's Signet by destroying the Artifact, but **no Naga card can destroy Artifacts** (only Deva Vishwakarma). Logged in GDD_DELTAS; not papered over in-engine.
- **⚠ WAVE-1 DESIGN DEBT — realm effect implementations key on hardcoded lists (from `docs/realm_intro_content.md`):** **Patala** keys on the hardcoded `ASTRA_DMG` set (`{'Pashupatastra','Lanka Dahan'}`) and **Yaksha** on a **Vishwakarma-only** guard. Both equal their printed `REALM_INFO.fx` text TODAY (those are the only damage-dealing Astras / the only artifact-destroyer). **Wave 1 MUST revisit both lists** if it adds ANY damage-dealing Astra or any artifact-destruction effect — otherwise realm text and behavior silently diverge (a new damage Astra wouldn't get Patala's +1; a new artifact-destroyer wouldn't be stopped by Yaksha). The Realms tab renders `REALM_INFO.fx` verbatim, so the printed promise ("All Astra damage +1" / "Artifacts cannot be destroyed") is player-facing and must stay honest.

## FEATURE-COMPLETE CHECKLIST (core single-player game loop)
**Core loop is COMPLETE** — mulligan → faction-select → best-of-3 rounds under a Cosmic Realm → match result, with AI opponent and full event choreography.
- ✅ All 4 factions, 88 cards, every §9 cross-faction ruling (R1–R20)
- ✅ Best-of-3 round loop, round-lead (R2), between-round persistence
- ✅ Balance FROZEN for launch (LAUNCH BASELINE table; all counters fidelity-correct)
- ✅ Event stream + sequential UI choreography (rule #7), faction toasts, keyword tooltips, `prefers-reduced-motion` bypass
- ✅ **Mulligan (GDD §2.2)** — up to 3 pre-Round-1, tap-to-mark UI + AI heuristic + choreographed redraw
- ✅ **7 Cosmic Realms (GDD §10)** — `g.realm` engine modifier at chokepoints; match banner + tappable header chip; Mrityulok regression byte-identical to baseline, Swarga/Patala swing measured
- ✅ **Per-realm battle boards — COMPLETE, 7/7 realms mapped (UI only, engine untouched, baseline byte-identical, 38 tests green)** — `REALM_BOARDS` maps all seven `realm id → { img, overlay, ambience? }`: `mrityulok`→`board_bg.jpg`, `patala`→`board_patala.jpg`, `swarga`→`board_swarga.jpg`, `kalki`→`board_kalki.jpg`, `yaksha`→`board_yaksha.jpg`, `rishi`→`board_rishi.jpg`, `gandharva`→`board_gandharva.jpg`; `BOARD_FALLBACK` (board_bg.jpg) covers any unmapped realm; each entry has a commented `ambience` slot RESERVED for a future per-realm audio track (not implemented). **⚠ ASSET PENDING: `board_gandharva.jpg` is not yet in `assets/img/`** — its map entry is wired and ready, but until the file lands a Gandharva match 404s → gracefully falls back to `board_bg` (`#realmboard` opacity 0). The other six board images are present and verified. `applyRealmBoard(G.realm)` runs at match start (`startGame`): sets the per-realm `--board-overlay` strength on `#field::after` and, for a non-fallback realm, **lazy-loads only that realm's image** into `#realmboard` (a z-0 layer above the base `#field::before` board_bg, below the overlay), crossfading opacity 0→1 on load. Until it loads — or if it 404s — the base board_bg shows (no flash). `boardImgLoaded` caches per session (a rematch in the same realm refetches 0). **Per-realm overlay** for readability: base/Mrityulok `.30`, Patala/Kalki/Rishi `.32`, Yaksha `.33` (dark ash/cavern/temple floors), **Gandharva `.40`** (light tan stage) and **Swarga `.44`** (brightest grey-marble board) — the two bright boards deepened so power numbers + Venom badges read over them. Verified at 390px with a full board, chip agreeing, power/Venom badges legible on every present floor: Patala fetch=1 / rematch=0; Swarga (.44), Kalki, Yaksha (.33), Rishi each fetch=1; Gandharva fetch=1 but 404 → fallback to board_bg (opacity 0) since its asset is pending. **NOTE:** `applyRealmBoard` swaps the board only; the header realm-chip label comes from `showRealmIntro` at real match start (they agree in real play — a dev force-swap of `G.realm` alone leaves the chip stale).

- ✅ **Dharma Shield player-designation** — human designates by hand (🛡 SHIELD button, sticky, Kavacha=2, no auto-fallback); AI unchanged, harness byte-identical
- ✅ **Game-feel pass (UI/audio only, engine untouched → harness byte-identical)** — AI think-time 1.2–3.5s scaled by decision weight + "thinking" indicator; opponent-card **showcase** (large center reveal 1.4s) → resolve → **result summary** (plain-language, from event `text`); pacing retune (callout 700ms / stagger 280ms / destroy 600ms dwell; significance & round-end theatre with total count-up); **Settings** (gear): Relaxed/Normal/Fast + music/SFX/volume, localStorage-persisted; tap-to-skip in all modes; reduced-motion keeps audio.
- ✅ **Board VFX** — destroy crack + faction-colored shards, damage impact flash + heavy arc numeral, buff rising ring, venom drip + wet badge, Mythic/Legendary screen-shake + board color-wash + showcase shimmer + low boom, shield gold ring, Leap arc. All transform/opacity, ≤1 shake/sec, through the choreography queue.
- ✅ **Audio: real assets (3rd pass, `assets/audio/`)** — **synthesized music fully retired.** Two-track **gapless file music** (`music_menu` on faction-select, crossfade ~1.5s to `music_battle` on match start, back to menu on return) — decoded via `decodeAudioData` into a looping `AudioBufferSourceNode` (NOT `<audio loop>`, which has an MP3 seam gap); default **ON**, routed through the music-volume/mute. **Astra impacts are files** — every Astra resolution plays `sfx_astra` (Brahmastra → `sfx_brahmastra`) synced with the screen-shake, on a non-limited SFX bus (files are pre-normalized: music −20 LUFS, astra −13, brahmastra −11 — **no per-sound gain compensation**), ducking music −4dB/1.5s. All **other SFX stay synthesized** (through the limiter). **Separate SFX-volume slider (80%)** + **▶ Test all SFX** (previews the two files too). Showcase-clipping fix retained (height-driven width + portrait `750/1050` art).
- **Menu background** — `assets/img/menu_bg.jpg` on the faction-select screen: **frameless** (no modal panel — selection sits directly on the image), image shown at ~90% brightness (no blur/desat), soft bottom gradient; each faction tile + BEGIN get a `rgba(0,0,0,.45)` + backdrop-blur scrim and text-shadows for legibility over the sunburst. Faction grid is **4-col ≥480px, 2×2 below** (no portrait overflow).
- **Pacing/audio device fixes (real-device pass)** — music track bug fixed (faction-select plays `music_menu` 33.0s, match plays `music_battle` 11.4s; the manager is strictly gesture-driven — no pre-gesture AudioContext). Choreography baseline slowed 30% via a single **`CHOREO_SPEED=1.3`** constant applied in `cDelay` (× the user Relaxed/Normal/Fast setting) + the result-summary hold; SFX trigger times shift with the steps but sound playback is untouched.
- **Battle board background** — `assets/img/board_bg.jpg` on `#field` (`::before`, center/cover so the mandala lands on the divider; `::after` ~30% dark overlay). Each card `.row:not(.empty)` gets a barely-there `rgba(4,2,1,.24)` + backdrop-blur scrim so minis/power/Venom badges stay readable over the stone — arena reads through. Verified: teal Naga minis + Venom stacks over the mandala at 390px & desktop.
- **Unit-clash SFX + match-end silence** — Unit/Hero placements play `sfx_unit_clash` (file, SFX bus) on the SAME `play` event as the landing dust/ember VFX (one beat); each call is a fresh buffer source so back-to-back placements OVERLAP, not cut off. At match end (`showGameOver`) `Audio2.stopMusic(2)` fades `music_battle` to SILENCE (no crossfade) so the result screen has room; `music_menu` resumes only on return to faction-select. `stopMusic` never silences the menu track (re-entry-safe).
- **Full file-SFX set (completes card-play retirement + event stingers)** — 11 new pre-normalized files decoded into the file-SFX buffer set on the existing bus (fresh `AudioBufferSourceNode` per trigger → overlaps layer); engine untouched, baseline byte-identical. **Card-play (synth `cardplay` fully retired):** `cardPlaySound(c)` routes Astra→`astraSfx` · Unit/Hero→`clashSfx` · **Mantra→`sfx_mantra` (om chant) EXCEPT Gayatri Mantra + Pavamana → `sfx_heal`** (Deva restoration; the dark revivals Sanjivani Corruption + Mrityunjaya deliberately stay on the om chant) · Artifact→`sfx_artifact`. Used in both the choreographed play branch and the reduced-motion audio path. **Events** (`resolveSound(ev)` → `{file}` or `{synth}`; each file replaces its synth counterpart, all other synth SFX stay): Venom Token APPLIED (`token`)→`sfx_venom_apply` (the per-round drain tick `venom` stays synth & quiet) · any Unit destroyed (`destroy`)→`sfx_unit_destroy` (chained destroys overlap) · Leap toast→`sfx_leap` · Dharma Shield applied (`shield` event)→`sfx_shield_apply` (its accompanying `Dharma Shield` toast is suppressed to avoid a double) · Chaos Surge toast→`sfx_chaos_surge`. **Shield-break:** the engine models Dharma Shield as astra-target-prevention (no dedicated break event), so `sfx_shield_break` is wired to the `block` event — a protective effect absorbing an astra (Hero immunity / floor-at-1 / Rama's Signet), the engine's "protection consumed" signal. **Result stingers:** on the result reveal, over the music fade-out, match win→`sfx_victory`, loss→`sfx_defeat` (stalemate keeps synth). **Test-SFX = 30** (14 synth incl. the now-unused `cardplay` def + 16 files). **Dev "Test VFX" button** next to Test-SFX in Settings — fires `cardLand` → `astraBurst` → full Mythic lightning at board center 1.5s apart (needs an active match for `#field` size) so effects can be reviewed on a real device without engineering a Brahmastra. Added page-weight of the 11 new files ≈ 648 KB; total audio assets ≈ 1.61 MB. (Loudness/mix and loop-seam not verifiable headless — all 16 files fetch 200 + decode; a real device is needed to judge the mix.)
- **Landing page (`#landing`) — the app's first screen, before faction-select** — UI layer only, engine untouched, baseline byte-identical, 38 tests green. Frameless treatment shared with faction-select (`#factionselect, #landing` reuse the `menu_bg.jpg` cover + darken + bottom-gradient), mobile-first 520px column, every panel gets the `rgba` dark + `backdrop-blur` scrim so the art reads through. Content: settings gear top-right (`#landgear` → opens the existing `#settings`, which was bumped to `z-index:90` so it sits above the landing; `.mutetoast` → 95); title block (Devanagari `दिव्य युद्ध` gold / `DIVYA YUDDHA` Cinzel wide-tracked / `BATTLE OF THE CELESTIALS` muted); a **profile card**; and four stacked mode buttons. **Mode buttons:** `Player vs AI` and `Faction Introduction` are ACTIVE (gold border + tint + `›` chevron → `showFactionSelect()` / `showFactionIntro()`); `Story Mode — Divya Yatra` and `Multiplayer` stay **locked** (~55% opacity, inline-SVG padlock, not navigable — a delegated handler shows a `Coming soon` toast). No emoji icons. **Flow:** boot is now `showLanding()` (was `showFactionSelect()`); Landing → PvAI → faction-select (unchanged) → match; the post-match return path (gameover **MAIN MENU** button, formerly "CHANGE FACTIONS") now calls `showLanding()` with fresh stats — faction-select keeps its own BEGIN flow. **Music:** `showLanding()` calls `Audio2.playTrack('menu')`, so menu music covers landing + faction-select (still gesture-driven — first tap starts it); the battle crossfade + match-end `stopMusic` are unchanged. Verified full cycle: landing `menu` → faction-select `menu` → BEGIN `battle` → match-end `stopMusic` (silence) → MAIN MENU `menu` resumes.
- **Player profile + persistent stats (`localStorage` key `dy_profile`, single JSON blob)** — `{name, matches, wins, winsDay, winsToday}`. Name defaults to `Warrior`, tap-to-edit inline (Enter/blur commits, Escape cancels, ≤16 chars) → persists; avatar circle shows the initials. **Counters** increment in `recordMatchResult()`, called from `showGameOver()` and guarded by a one-shot `matchRecorded` flag (re-armed in `startGame()`) because `render()` re-calls `showGameOver()` on every re-render while `G.over` — so each match counts once: `matches` always +1, `wins`/`winsToday` +1 only when the human (p0) wins. `winsToday` is day-keyed (`todayKey()`) and resets on date change. Verified in-browser: name edit persists across reload; a win → `1/1/1`, double `showGameOver` does NOT double-count, a loss → `matches 2, wins 1, today 1`. **⚠ PROVISIONAL:** the level formula `1 + floor(wins/5)` (bar = `(wins%5)/5`) is a PLACEHOLDER to be replaced by the **M1 XP system**; the `matches`/`wins`/`winsToday` counters are the **matches-per-day telemetry feeding the Amsha economy** (keep them even when the XP system lands).
- **Faction Introduction screen (`#factionintro`)** — UI layer only, engine untouched, baseline byte-identical, 38 tests green. Reached from the now-ACTIVE landing button (`showFactionIntro()`); back arrow → `showLanding()`; `music_menu` plays throughout. Frameless `menu_bg.jpg` treatment (added to the `#factionselect, #landing, #factionintro` selectors; their `::before/::after` are now `position:fixed` so the bg holds while the page scrolls). **Six tabs**, `How to win` first/default, then Devas/Asuras/Vanaras/Nagas/**Realms** (horizontally-scrollable tab strip; landing button caption is now "Meet the armies and the realms"). **Faction tab** = identity header (name in faction color, tagline, difficulty + playstyle badges) → mechanic box → strengths/weaknesses as two scrim panels (green `--green-dim` / red `--crimson-dim` borders) → the 22-card gallery (4-col thumbnail grid) → 2 strategy strips → matchup note (Deva & Naga tabs only). **Content authority:** all prose is verbatim from `docs/faction_intro_content.md` (transcribed into `FI_CONTENT`); every card name/power/effect shown is pulled LIVE from the engine (`DECKS`, `RARITY_NAME`) — the inspect view's text panel renders the card's real `txt`. **Gallery order:** Heroes → Units by power desc → Astras → Mantras → Artifacts. **Future-wave teaser:** after the 22 real thumbnails the grid appends **`FI_HIDDEN` (=22) locked slots** (LIVE_GAME_DESIGN seasonal waves) — pure CSS, ZERO network (no `<img>`): dark scrim, faction-color border @30% (`${hex}4d`) + a centered dim `?` glyph @35% (`${hex}59`), all identical and visually quieter than real cards; tapping one toasts `The realms have not revealed this card yet.` (no inspect/nav). The gallery header is data-driven: `CARDS — ${realCount} OF ${realCount+FI_HIDDEN} REVEALED` (real = engine deck length, so wave releases update it automatically → currently 22 OF 44). Lazy-load fetch count is unchanged (still exactly 22 thumbnails per tab). **Performance:** each tab's DOM (and thus its 22 `<img>` thumbnails) is built only on FIRST open and cached in `fiTabNodes` (verified: opening a tab loads only that faction's thumbs, re-opening fetches 0; never preloaded). Tapping a thumbnail opens a full-screen inspect (`#fi-inspect`, z-96) that loads the full `assets/cards/` frame on demand. **Inspect navigation (`fiOpenInspect`/`fiNav`/`fiShowCard`):** once open, the player moves through all 22 revealed cards of the current faction **in gallery order** (hidden slots skipped) via edge **‹ › arrows** (44px, same scrim+gold as back buttons, `position:fixed` vcentered, may overlay the card edge with a scrim behind), horizontal **swipe** (≥50px, vertical ignored — `fiSwiped` flag also swallows the trailing click so a swipe never closes/double-navs), and **ArrowLeft/Right** (Escape closes). **Wrap-around** both ends. A `N / 22` counter sits above the card, updating every move. Entry point sets the start index (gallery thumb → its index; strip thumb → `findIndex` by id). **Loading:** frame + engine-text panel swap together; spinner only when `!fiLoadedFrames.has(src)`; the two neighbors (N±1) are preloaded via off-DOM `fiFrameCache` Images so browsing feels instant — **preloads are lazy (only ±1, never the whole faction)**; verified opening card 5 fetches exactly frames 4/5/6. **Close** only on a direct backdrop tap (`e.target===ov`); the card taps to next, arrows/text/counter are dead-zones so closing isn't accidental mid-browse. **Strategy strips (`FI_STRIPS`):** horizontal step cards with a real card THUMBNAIL (tap→inspect), gold step label + one-line caption, gold `→` arrows, a RESULT panel; universal How-to-win diagrams (round-economy pip bars, when-to-pass icon rows) are pure HTML/CSS. Strips fit the column (no horizontal scroll). Thumbnails come from `assets/thumbs/` (see repo-layout; run `scripts/make_thumbs.sh`). **Content = `faction_intro_content.md` v1.1 (corrected to engine truth).** The two v1.0 PDF-era card references have been fixed against the engine (the engine was right): (1) **"Indra's Net"** never existed as a card — Deva diagram D2 is now **"Two Shields, Not One"** built on **Dharma Kavacha** (`PASSIVE: Dharma Shield protects TWO Units instead of one`), and the Deva strengths bullet is now "Doubled shielding — Dharma Kavacha protects two units at once". (2) **Rama Naam** is **+2** (not +3), and **Hanuman does not amplify Rama Naam** — its passive is `Each Vanara Unit of printed power 4+ you play gains +1 on entry`, so the V1 strip's Step 2 caption now states that, and the Vanara strengths spike bullet reads "(Rama Naam: +2 to your whole Vanara board)". No card fact is hand-typed; the inspect panel still renders live engine `txt`.
- **Realms tab (`fiRenderRealms`, 6th tab) — UI only, engine untouched, baseline byte-identical, 38 tests green.** Prose verbatim from `docs/realm_intro_content.md` (`FI_REALM_CONTENT` + `FI_REALM_INTRO`); **the effect line is `REALM_INFO[id].fx` rendered at RUNTIME, never hand-typed** (programmatically verified equal for all 7). Shows the tab intro line, then all 7 realms in engine `REALMS` order — each: **vertical board banner** (the whole board UNCROPPED at its native portrait aspect, ~60% column width / ≤230px, centered — NO fixed-height crop; `assets/thumbs/board_<id>.jpg` is 320px-wide q50 ~25–38 KB so it stays crisp at that display size) in a **CSS-only double-rule picture frame** driven by an inline `--ra:<accent hex>` (from `REALM_ACCENT`): outer 1px rule `color-mix(--ra 50%)` → 4px near-black mat (the banner's `padding` over `rgba(4,3,1,.6)`) → inner 1px rule `color-mix(--ra 30%)` on a `.fi-realm-inner` wrapper, plus 4 L-shaped corner ticks (`::before`, eight `linear-gradient` segments at `--ra 70%`). `box-sizing:border-box` keeps the padding/borders from changing the banner's outer width, so the tap target is unshifted. Tap → the full board image in the inspect view, name + epithet in the realm's accent color (`REALM_ACCENT`, from the pack header), a REALM EFFECT box (runtime `fx`), Thrives-here/Suffers-here twin panels (reusing `.fi-strength`/`.fi-weak`), and a TIP callout. Lazy like the faction tabs: DOM + the **7 board thumbs load only on first open, 0 on re-open** (`fiTabNodes` cache). **Board inspect:** the card-inspect component was generalized with a `mode` (`'card'`|`'board'`) — `fiOpenBoardInspect(idx)` reuses the same overlay/arrows/swipe/keyboard/close and **navigates between the 7 realm boards** (`N / 7` counter, wrap-around); the full board image (`assets/img/board_<id>.jpg`) is lazy-loaded on demand with the same ±1 neighbor preload. Board thumbnails come from the 2nd step of `scripts/make_thumbs.sh`.
- **Match-start conch + round-end gong (`sfx_conch` / `sfx_round_end`, −14 LUFS)** — audio layer only, engine untouched, baseline byte-identical, 38 tests green. **`sfx_conch`** is a match-start announcement timed to **round 1, not the mulligan**: `startGame()` arms a once-per-match guard (`conchPending=true`) alongside the battle-music crossfade (music plays at mulligan), and `fireConch()` at the top of `pump()` consumes the guard the first time `pump()` runs for the match — which is the round-1 start, right after the mulligan resolves (both the instant KEEP-HAND / reduced-motion path and the draw-choreography path reach `pump()`). `pump()` is never called during the mulligan phase, so the conch can't fire early; the guard makes it silent on rounds 2/3; `startGame` re-arms it each match. **`sfx_round_end`** replaces the synth round-end cue in `showRoundBanner()`, gated `if(!G.over)` — so it sounds for rounds 1 & 2 but is **suppressed at match end** (round 3, or a 2–0 clinch), where the `sfx_victory`/`sfx_defeat` stinger in `showGameOver()` owns the moment (no gong under a stinger). The `!G.over` guard is local to the gong line, so suppression holds regardless of caller. **Test-SFX = 32** (14 synth + 18 files). Both `assets/audio/sfx_conch.mp3` and `sfx_round_end.mp3` are present. Verified in-browser: conch=0 during mulligan, =1 at round-1 start, =0 on later pumps (rounds 2/3), exactly once per match across two consecutive matches (guard re-arms); gong=1 on a non-over round, gong=0 at match end, `showGameOver`→stinger=1/gong=0.
- **Power-change blips + coalescing (`sfx_buff` / `sfx_debuff`, −17 LUFS)** — 2 new files; audio layer only, engine untouched, baseline byte-identical, 38 tests green. `playEventSound` routes the `buff` event → `sfx_buff` (power INCREASE: unit buffs, Rama Naam, Indra's Net, Amrita Kalasha +2, Kishkindha Crown leap bonus, Pavamana cleanse gains, Chaos Surge, Surya Dev, Neela, Kalki Kshetra, …) and the `damage` event → `sfx_debuff` (power DECREASE from non-Venom effects: Ashvatara, Sarpa Satra, debuff/damage Astras). **The Venom drain keeps its own identity** — it emits its own `venom` event (never `damage`), so the quiet synth Venom tick is untouched. **Coalescing (`powerChangeSound`):** the batching signal is the **runAction event beat** — `resetPowerBeat()` clears a `(sound|abilityName)` key-set each beat, and because a multi-unit effect shares one `abilityName` across its per-unit events, a whole-board buff or drain collapses to **one** blip (never one-per-unit). A separate real-time overlap guard (`powerRecent`, 350 ms window) caps each sound at **≤2 concurrent**, dropping extras. Applied in both the choreographed path and the reduced-motion audio path. `ward`/`revive` stay on the synth `buff` cue (survival/revival, not a power increase). **Test-SFX = 30** at this task's landing (14 synth + 16 files); the conch/gong task has since landed and raised it to **32** (that task's `sfx_conch` + `sfx_round_end` asset files are still pending in the repo). Coalescing verified in-browser: one effect × many units → 1 blip; 3 distinct effects in a beat → capped at 2; separate beats → blip each; buff+debuff same beat → 1 each; a Venom tick → 0 debuff blips.
- **Impact VFX (`VFX` module) — quality overhaul (sprite-based, reads as real phenomena)** — one board-sized `#vfxcanvas` (pointer-events none, z-5) driven by the event stream, rAF + delta timing, **DPR-aware (cap ×2)**, skipped under `prefers-reduced-motion`, no external libs. **Sprites pre-rendered ONCE at init** (5 radial-gradient fire glows white→deep-red, one ember dot, 3 warm-dust + 3 dark-smoke irregular multi-blob sprites) → all particles are `drawImage`, so per-frame cost is trivial. **Hard cap 120 particles** (up from 40). Two draw passes: smoke in `source-over`, fire/embers/bolts in `globalCompositeOperation='lighter'` (additive). (a) **card-lands** = warm brown-grey dust (rotate/spin, scale 1×→2.5×, fade-in 10% + long tail, sine curl) + 5–8 gold embers. (b) **Astra fire burst** = 3 populations (6 large soft glows, 12 hot cores, 8 embers) with upward buoyancy + inward pull + per-particle ~10–15 Hz flicker, lifetime color ramp via sprite index, then 4 grey smoke puffs after the flame peak — on the sfx_astra/shake beat. (c) **Mythic/Brahmastra** = real **midpoint-displacement lightning** (fractal polyline, 6 levels, 2–3 forked branches; 3 additive strokes: wide blue-white glow / medium / 1px white core) in a strike sequence (dim tint → bolt 1 → dark gap → bolt 2 with a NEW shape → `#vfxflash` full-screen flash + afterglow → heavy smoke roll + embers at impact), over the existing shake + `sfx_brahmastra`. All durations ×`CHOREO_SPEED`. **Frame-timing verified** at the 120-particle + 2-bolt worst case (DPR-2 canvas 746×875): render **avg 0.10–0.12 ms / worst 0.6 ms** — ~55× under the 16.7 ms budget, far under the 33 ms target; **cap 120 unchanged**. All three effects confirmed firing on their correct `play`-event beats (unit→cardLand, astra→astraBurst, Mythic astra→astraBurst+lightning). Build ships **console-clean**.
- **NOTE: audio LOUDNESS / loop-seam clicks are not verifiable headless (no audio output). Verified: all 4 files fetch+decode, crossfade cycles menu⇄battle, both loop via buffer (gapless by construction). Total added page weight ≈ 1.27 MB (audio 960 KB + menu_bg 336 KB). A real device is needed to judge mix balance.**

**ENGINEERING BACKLOG: EMPTY.** The core single-player game loop is engineering-complete. The only remaining items are art production and a designed deferral (below) — no code work outstanding.

**Deck building — DECIDED: deferred to Season 2 (designed deferral, not open scope).** 22-card decks drawn from 22-card factions have **zero degrees of freedom** — there is nothing to build. Card-level deckbuilding becomes a real feature only when the pool exceeds the deck size (e.g. Rishis expanding it to **44-into-22**). Until then, faction-select is the correct and complete shipping form.

**Art production (content, NOT engineering; `cardArtSrc` naming already handles all of it):**
- ✅ Devas 22/22 · ✅ **Asuras 22/22** · ✅ Vanaras 22/22 (art present)
- ⏳ **Nagas 0/22** pending
- ⏳ **Four blocking re-exports:** Chandrahas, Hanuman, Neela, Angad

**Post-launch (explicitly out of the core loop):** Capacitor mobile wrap · multiplayer (Node + Colyseus/Nakama) · tutorial/onboarding.
