# Divya Yuddha — Battle of the Celestials

Mobile-first strategic card battle game based on Indian mythology (Ramayana, Mahabharata, Puranas).
Gwent-style best-of-3 rounds + Hearthstone-style card abilities. 4 factions, 88 cards at launch.
Solo developer (Sangbaran, Creative Director) with Claude as the engineering team.

## Tech stack (decided July 2026 — Unity was abandoned)
- Pure JavaScript rules engine, **zero framework dependencies**, runs in browser AND Node
- UI: vanilla JS + CSS (may migrate to a bundler/TS later; keep engine UI-agnostic)
- Testing: headless AI-vs-AI simulation via Node (`node src/test.js`)
- Mobile shipping plan: Capacitor wrap → iOS App Store + Google Play
- Multiplayer (later): Node backend, Colyseus or Nakama

## Repo layout
- `src/engine.js` — the rules engine. UI-agnostic. All game logic lives here.
- `src/test.js` — simulation harness: 500 AI-vs-AI matches + rule invariants. RUN THIS AFTER EVERY ENGINE CHANGE.
- `src/test_scenario.js` — `opts.scenario` smoke tests (50 checks): each field, the absent/empty no-op (incl. a byte-identical **g.rng draw-sequence** trace), event observationality, and unknown-card names throwing. Run after any setup/`newGame` change.
- `src/chapters.js` — Story Mode data (Book of Order, schema per `docs/BOOK1_DESIGN.md` §4): chapters b1c1/b1c2 authored (deck lists role-resolved against engine `DECKS`), locked stubs for ch3–7, and `STORY_PREDICATES` (win/bonus). UMD (browser `window.CHAPTERS` / Node `require`).
- `src/test_story.js` — Story predicate unit tests (synthetic streams) + **headless chapter drivers** for ch1/ch2 that mirror the UI driver (a headless win ⇒ a browser win). 15 checks.
- `index.html` — playable prototype (engine is inlined by a build step; keep src/engine.js canonical)
- `docs/DESIGN_DECISIONS.md` — QA log, approved art, open rulings. Update when decisions are made.
- `docs/GDD_DELTAS.md` — **authority for card frame text**: every gameplay divergence from GDD v2.0 in printed-card-text form (rulings R1–R9, tuned numbers). Use this, not the GDD PDF, when arting frames.
- `assets/audio/` — `music_menu.mp3` (menu/faction-select), `music_battle.mp3` (match) gapless-looped via Web Audio buffers; **18 file SFX** on the non-limited file bus (fresh `AudioBufferSourceNode` per trigger → overlaps layer): Astra impacts `sfx_astra` / `sfx_brahmastra`; card-play `sfx_unit_clash` (Unit/Hero), `sfx_mantra` (om chant) / `sfx_heal` (Gayatri + Pavamana Deva restoration) / `sfx_artifact`; events `sfx_venom_apply` (Venom Token applied), `sfx_unit_destroy` (any Unit death, overlaps), `sfx_leap`, `sfx_shield_apply` / `sfx_shield_break`, `sfx_chaos_surge`; **power-change blips `sfx_buff` (power up) / `sfx_debuff` (power down, non-Venom) — coalesced, ≤2 overlapping**; **match-start `sfx_conch` (once per match), round-end gong `sfx_round_end` (rounds 1&2 only — suppressed at match end)**; result stingers `sfx_victory` / `sfx_defeat`. Pre-normalized (music −20 / astra −13 / brahmastra −11 / clash −15 / stingers −14 / events −15 / shield ding −16 / buff+debuff −17 / conch+gong −14 LUFS, durations pre-trimmed) — do NOT add per-sound gain compensation. **The synth `cardplay` is fully retired from all card-play paths** (every card type now maps to a file); **round-end is now a file too** (the synth `roundwin`/`roundloss` remain only as the stalemate cue + Test-SFX entries). `playFile` hardened: a decoded-but-absent buffer (missing file) gives up silently instead of retry-spinning.
- `assets/img/menu_bg.jpg` — shared background for the **landing page AND faction-select** (same frameless treatment: `center/cover`, `brightness(.9)`, soft bottom gradient; the `#factionselect, #landing` selectors share it).
- `assets/img/board_bg.jpg` — battle-arena background (896×1344, golden mandala at vertical center → aligns with the divider via `center/cover` on `#field`). **Canonically Mrityulok's board, and the lazy-load/404 fallback** for every realm. Per-realm boards `board_patala.jpg` / `board_swarga.jpg` (pre-cropped, mandala at 50%/50% so the same center-anchored CSS works) are mapped in `REALM_BOARDS` (see the per-realm-boards feature note); add a new realm's board as a one-line map entry.
- `assets/cards/` — final framed card PNGs. **Two filename patterns** by card type:
  - **Powered** (Units/Heroes, `base>0`): `{Faction}_{Type}_{Name}_P{printedPower}_r{RarityName}.png`
  - **Powerless** (Astra/Mantra/Artifact, `base===0`): `{Faction}_{Type}_{Name}_r{RarityName}.png` — **no power segment** (both patterns end in `_r{Rarity}.png`; no `_png` suffix anywhere)
  - Faction is the plural (`Devas`, `Asuras`, `Vanaras`, `Nagas`); Type is Title-cased (`Hero`/`Unit`/`Astra`/`Mantra`/`Artifact`); **Name segment = the card name with ALL non-alphanumeric characters removed** (`c.n.replace(/[^A-Za-z0-9]/g,'')` — strips spaces AND apostrophes etc.: `Rama's Signet`→`RamasSignet`, `Surya Dev`→`SuryaDev`, `Ashwini Kumars`→`AshwiniKumars`); power is the card's **printed/base** power; Rarity is the **full name** (`Legendary`/`Mythic`/`Epic`/`Rare`/`Uncommon`/`Common`).
  - `cardArtSrc` (in index.html UI) branches on `c.base > 0` to pick the pattern.
  - Examples: `Devas_Hero_Indra_P7_rLegendary.png`, `Devas_Unit_SuryaDev_P6_rEpic.png`, `Devas_Astra_Vajra_rLegendary.png`, `Asuras_Hero_Mahabali_P8_rLegendary.png`
- `assets/thumbs/` — **200px-wide JPEG thumbnails** of every card frame (SAME filenames, JPEG bytes in the `.png` name — browsers sniff content), ~20 KB avg / ≤25 KB each, used by the Faction Introduction gallery (the full frames are ~1 MB each — far too heavy to grid). Generated by **`scripts/make_thumbs.sh`** (macOS `sips --resampleWidth 200 -s format jpeg -s formatOptions 60`). The script has a **second step** that also thumbnails the **7 realm board images** (`assets/img/board_*.jpg` → `assets/thumbs/board_*.jpg`, **320px-wide q50, ~25–38 KB** — the Realms tab shows them uncropped at ~220px so 320px keeps them crisp incl. retina) for the Realm Introduction banners. **RE-RUN `bash scripts/make_thumbs.sh` whenever a card frame OR a board image is re-exported/replaced** so the thumbnail stays in sync (or run the single-file sips command on just the changed files to keep the git diff minimal — that's what the Chandrahas/Hanuman/Neela/Angad re-export used). The UI derives a thumb URL by swapping `assets/cards/`→`assets/thumbs/` on the `cardArtSrc` output.
- The full GDD v2.0 PDF is the design authority (in the owner's Claude Project / Google Drive)

## Engine architecture rules
1. Engine stays headless — no DOM access in engine.js, ever
2. Every card ability is dispatched by card `id` in `playCard()` / passives computed in `effPower()` and helpers
3. All randomness goes through `g.rng` (injectable for seeded, reproducible tests)
4. Every rule change requires `node src/test.js` to pass all invariants before committing
5. Cross-faction rulings (GDD Section 9) must be implemented as explicit code paths with comments citing the ruling
6. **Counter-effects and protective effects: implement the exact printed text — the weakest defensible reading — and flag any strengthening or simplification to the owner.** Fidelity bugs in counters have caused every major balance distortion in this project (Dharma Shield, Karkotaka R10, Rama's Signet, Pavamana candidate).
7. **Event stream (`g.events`) is OBSERVATIONAL, never causal.** Every damage/buff/destroy/revive/trigger/token/faction-passive calls `emit(g,type,{sourceUid,targetUids,amount,abilityName,text})` alongside its `log()`. `emit()` must never touch game state or `g.rng` — outcomes must stay byte-identical whether or not `g.events` is consumed (verify: harness win-rates unchanged vs the LAUNCH BASELINE). The flat `g.log` stays as the backup channel. The UI (`index.html`) choreographs `g.events` **sequentially** via `runAction()`/`playEvent()` (source pulse + ability callout → staggered per-target floaters/dissolves, faction toasts, input-locked with tap-to-skip); `prefers-reduced-motion` bypasses choreography for instant resolution + log.

## Game rules cheat sheet
- 22-card decks, draw 10 at start, +2 at rounds 2 and 3, no hand limit, **mulligan up to 3 before Round 1 (implemented — GDD §2.2)**
- **Each match is assigned one of the 7 Cosmic Realms (GDD §10) — an engine-level modifier; random by default, `opts.realm` fixes it for tests**
- **`opts.scenario` (Story Mode setup — the first post-freeze engine change, 2026-07):** additive `newGame` option, read ONLY when present (opts.realm precedent) — `{ p0Deck?, p1Deck?, p0Hand?, p1Hand? (ordered decklists/hands by card NAME, minted via `mkCard` for uid safety, deck injection = no shuffle; **an unknown card name — or a hand card not in the deck — THROWS**, authoring safety per design ruling), winTarget? (default 2), handSize? (default 10), draws? (between-round, default 2, realm bonus still added), mulligan? (0..3, 0 disables the phase) }`. Stored as `g.winTarget`/`g.drawCount`/`g.mulliganCount`; every default equals today's hardcoded value. **Absent/empty scenario is byte-identical** (behavior AND `g.rng` draw sequence) — proven by `src/test_scenario.js` (rng-trace) and the unchanged LAUNCH BASELINE. Opponent scripting needs nothing more: the story driver calls `playCard(g,1,…)`/`pass(g,1)` and may hand off to `aiTakeTurn(g,1)` mid-match.
- Play exactly 1 card per turn OR pass; once passed, out for the round; round ends when both pass
- Rounds: higher Yuddha Row total wins; best of 3
- Between rounds: Units and Artifacts CLEAR, Heroes PERSIST, unused hand carries forward, played cards never return
- Max 1 Hero played per round; max 1 Astra per turn; max 1 Artifact active
- Heroes are immune to Astras by default (exceptions: Sudarshana, Takshaka rule)
- Faction passives: Devas=Dharma Shield, Asuras=Chaos Surge, Vanaras=Leap, Nagas=Venom

## v0.1 simplifications (KNOWN, intentional — do not treat as bugs)
- Dharma Shield: the HUMAN Deva player **designates manually** (🛡 SHIELD button → tap a friendly Unit; sticky per EXP-A, Kavacha = 2 designations, **no auto-fallback if unused — unused is unused**). The AI keeps its designate-at-first-opportunity auto-pick (highest-power), so the balance baseline is untouched (`pl.manualShield` gates the two paths; `designateShield()` is the manual call).
- Board positioning: units occupy ordered slots (`pl.units` order = position); adjacency = array neighbours. Units-only (Heroes positionless); Devas/Asuras ignore position, Vanaras use it for Leap. Placement passed as `playCard(...position)`.
- Vayu: original GDD ability restored — displaces the highest enemy Unit out of formation (breaks Vanara Leap adjacency) and −2 power.
- All 4 factions implemented (88 cards): Devas + Asuras + Vanaras + **Nagas (complete — cards, §9, Venom pipeline, teal UI)**. Faction select in the UI; any combo playable. (Balance FROZEN for launch — see BALANCE.)
- `index.html` engine is generated from `src/engine.js` by `node src/build.js` (run after every engine change).

## RESOLVED RULINGS (2026-07, official — implemented in engine.js)
- **R1 — Death at 0:** a Unit whose power reaches 0 or less is DESTROYED (to discard, triggers Yama-style death effects). Enforced generically by `sweepDeaths()` so Venom-token ticks also kill.
- **R2 — Round lead:** Rounds 2/3 the previous round's WINNER plays first; on a drawn round, the same leader as before. (`endRound`: `prev.winner ?? firstThisRound`.)
- **R3 — Deva Soldier (redesigned):** "PASSIVE: While Indra is on the board, gains +1 power at the start of each of your turns." Implemented via `onTurnStart()`.
- **R4 — Amrita Kalasha (official):** "ON PLAY: Your lowest power Unit gains +2. If that unit is destroyed this round, it revives at 1 power (once)." (aegis flag, cleared on round reset.)
- **R5 — Tataka:** "lowest Unit on the board" excludes Tataka herself (a removal Unit shouldn't primarily suicide).
- **R6 — Leap adjacency:** Units only; Heroes on the Yuddha Row are never valid Leap sources. (Positioning is units-only; Heroes stay in their own positionless row.)
- **R7 — Angad:** while Angad is on the board, when the opponent plays an Astra they forfeit their next turn (`skipNext`). Stacks with Varuna's astra limit — both apply if both on board.
- **R8 — Kishkindha Oath:** proactive ward — play it on a friendly Vanara Unit; the next time that unit would be destroyed this round it survives at 1 power and all other friendly Vanara Units gain +1. Ward expires at round end if unused (`ward` flag).
- **R9 — Chandrahas (Asura):** ON PLAY, trigger one Chaos Surge (removes its dead turn). Also the standing PASSIVE.
- **R10 — Karkotaka (Naga, flag A):** while on board, the Venom drain ticks at the start of each OPPONENT turn during the round instead of once at round end (continuous attrition).
- **R11 — Patala Throne (flag B):** while active, Venom drain = −(1 + current round number). R1 −2, R2 −3, R3 −4.
- **R12 — Surasa (flag C):** one-shot trap on the opponent's next card — a Unit enters with −2 power and Surasa gains +2; an Astra is negated (Chaos Surge still triggers per §9). Trap expires after one card.
- **R13 — Venom Tokens (flag D):** drain their bearer at each tick regardless of side; Mrityunjaya's revived Unit pays the corruption cost.
- **R14 — Ananta Coil (flag E):** tokens persist for the rest of the MATCH, draining 1 from a random enemy Unit at each Venom tick.
- **R15 — Shesha (flag F):** at the start of the next round after a lost round, a random friendly Unit returns from discard to the board at full power.
- **R16 — Kaliya / Ashvatara (flag G):** Kaliya enters with +(current round − 1) power; Ashvatara's drain = current round number.
- (Four supplementary Naga engine rulings are logged below as **R17–R20**.)

## ASURA RULINGS (2026-07, official — see docs/ASURA_ROSTER.md)
- **Mahabali** "played for free" = the first Unit played the round after a *voluntary* Pass grants an immediate extra turn (tempo, not cost). Forced Pass (Tamasa) does not arm it.
- **Kumbhakarna** power does NOT count while asleep; wakes at the start of its owner's next turn, sweeps 3 damage to all enemy Units, then counts.
- **Nagastra Venom vs Hiranyakashipu** — immunity holds: venom ticks it but floors at 1; only Brahmastra/a Mantra removes it. Venom ticks at round end BEFORE scoring.
- **R5 — Tataka** (confirmed): "lowest Unit on the board" excludes Tataka herself (a removal Unit shouldn't primarily suicide); she can still hit your other Units.
- Chaos Surge / Chandrahas / §9 cross-faction paths — implemented; see engine `resolveAstra`/`chaosSurge`/`onAstraResolved`. §9 Manasa/Surasa now LIVE (Naga shipped).

## NAGA SUPPLEMENTARY RULINGS (R17–R20, official — docs/GDD_DELTAS.md)
- **R17 — Venom stacking** is ADDITIVE: perUnit drain = 1 + (Patala: +round) + (Vasuki on board & R3: +1) + (Venom Strike: +2, or +3 with Vasuki). Karkotaka is an orthogonal *timing* modifier.
- **R18 — Manasa** = on-board negation: while she is on the board the opponent's Astras are cancelled (effect + Chaos Surge). Surasa (R12) is the one-shot trap — the §9 asymmetry sides.
- **R19 — Astika** skips the next Venom tick and heals friendly Units +1.
- **R20 — Nagapasha** unbind is a turn-costing opponent action; an un-freed bound Unit scores 0 (and `totalPower` skips bound Heroes for the Takshaka §9 case).

## BALANCE — FROZEN FOR LAUNCH (2026-07). LAUNCH BASELINE — all future balance work diffs against this table.
`node src/test.js` — 10 matchups (4 mirrors + 6 crosses) × 500 AI-vs-AI sims. Decided-game win% (draws excluded):

| Matchup | Result | Band |
|---|---|---|
| Deva mirror | 49.8 / 50.2 | ✅ |
| Asura mirror | 53.6 / 46.4 | ✅ |
| Vanara mirror | 52.8 / 47.2 | ✅ |
| Naga mirror | 51.4 / 48.6 | ✅ |
| Devas vs Asuras | 47.7 / 52.3 | ✅ |
| Devas vs Vanaras | 45.1 / 54.9 | ✅ |
| Asuras vs Vanaras | 42.3 / 57.7 | ✅ |
| Devas vs **Nagas** | 40.9 / **59.1** | ◆ accepted counter-matchup |
| Asuras vs **Nagas** | 49.0 / 51.0 | ✅ |
| Vanaras vs **Nagas** | 57.3 / 42.7 | ✅ |

- **Band:** every cross sits in [40.9, 59.1]. **Deva-vs-Naga 59.1 is the one accepted >58 point** — the designated poison-vs-go-wide counter-matchup (design intent), reached with all counters fidelity-correct, so the number has clean provenance (not a bug).
- **Naga integration levers (all applied):** **EXP-L** Karkotaka per-turn tick → flat −1 (escalation only round-end). **EXP-L2** Karkotaka → a single flat −1 early tick on the first pass (killed the ~6×/round volume). **EXP-M** counter-piloting AI (Deva holds Pavamana to 2+ tokens/round-end; Vanara prioritises Signet vs Nagas; all AIs devalue go-wide vs Nagas — measurement of competent play, NOT a detune). Base −1 round-end drain is GDD-verbatim and was never touched.
- **Fidelity fixes during the pass (see rule #6 + GDD_DELTAS):** **Rama's Signet** was blanket-immunity → corrected to floor-at-1 + token-negation (Vanara-vs-Naga 63.8 → 57.3). **Pavamana** was missing clause (b) → restored +1 power per effect removed (Deva-vs-Naga 60.4 → 59.1).
- **Pavamana reads −18pt on played-vs-held in Deva-vs-Naga — EXPECTED, not a defect:** it's reactive damage-control tech; you only hold 2+ Venom Tokens once already losing, so playing it correlates with (not causes) losing.
- Pre-Naga tuning (unchanged, folded in): Chandrahas ON-PLAY Surge · Chaos Surge +3 on Astras+Mantras · +1 floor (EXP-J) · Kumbhakarna counts asleep (EXP-G) · Hanuman entry printed ≥4 (EXP-F) · Kishkindha Crown +1/+1 (EXP-E). All gameplay deltas live in **docs/GDD_DELTAS.md**.
- **Known design hole (owner, Season/errata):** GDD §9 says the Naga answers Rama's Signet by destroying the Artifact, but **no Naga card can destroy Artifacts** (only Deva Vishwakarma). Logged in GDD_DELTAS; not papered over in-engine.
- **⚠ WAVE-1 DESIGN DEBT — realm effect implementations key on hardcoded lists (from `docs/realm_intro_content.md`):** **Patala** keys on the `ASTRA_DMG` set and **Yaksha** on a **Vishwakarma-only** guard. Both equal their printed `REALM_INFO.fx` text TODAY (those are the only damage-dealing Astras / the only artifact-destroyer). The Realms tab renders `REALM_INFO.fx` verbatim, so the printed promise ("All Astra damage +1" / "Artifacts cannot be destroyed") is player-facing and must stay honest.
  - **✅ ENGINE TASK 1 (dmgAstra tag) — DONE:** `ASTRA_DMG` is **no longer hardcoded** — it's DERIVED at load from a `dmgAstra:true` card-def tag: `new Set(Object.values(CARD_BY_NAME).filter(c=>c.dmgAstra).map(c=>c.n))` (keyed by name = the `cause` string). Exactly 2 launch defs tagged: **Pashupatastra, Lanka Dahan** (both deal-N; no policy violations). A **permanent test.js invariant** pins the derived set to a hardcoded expected list `['Lanka Dahan','Pashupatastra']` and fails loudly on a typo/missing/extra tag (verified both negative cases). So a wave-1 damage Astra just adds `dmgAstra:true` to its def **and** updates the test's expected list (a deliberate, loud change). PROVEN PARITY: derived set == old hardcoded set; the Patala +1 both-ways is byte-identical to the pre-change engine (`git show HEAD` — Pashupatastra survive→kill only in Patala; destroy-class Vajra/Brahmastra/Gandiva get no bonus, membership `false`); LAUNCH BASELINE unchanged. **Yaksha's Vishwakarma-only artifact-destroy guard is still hardcoded** — its analogous `artifactDestroyer` tag is a future task if wave 1 adds an artifact-destroyer.
  - **✅ ENGINE TASK 2 (wave1 flag + batch-1 six cards) — DONE.** Authority: `docs/WAVE1_ROSTER_v0.2.md`. **THE FLAG:** wave-1 card defs carry `wave:1` and live appended to the existing `DECKS` faction arrays; `mkPlayer(…, wave1)` builds the random pool from `rawSrc.filter(c=>!c.wave)` unless `opts.wave1` is set — **filtered BEFORE `.map(mkCard)`** so with the flag absent the launch pool, its UID assignment, and the `g.rng` shuffle sequence are byte-identical (proven: scenario rng-trace 50 green + LAUNCH BASELINE unchanged, Deva-vs-Naga **40.9/59.1**). `opts.wave1` is additive, read only when present (opts.realm/scenario precedent). Scenario explicit-deck injection (`spec.deck`) is unaffected — it names cards directly. **THE ASTRA_DMG SUBTLETY (proven, not asserted):** the `dmgAstra` derivation runs over ALL defs at load, so tagging Agneyastra grows `ASTRA_DMG` to `{Agneyastra, Lanka Dahan, Pashupatastra}` **even flag-off** — harmless (a card that can't enter play changes no behavior; baseline byte-identical). The permanent invariant's expected list was updated to include Agneyastra — it **FAILED loudly first** (`✖ 1 INVARIANT FAILURES`, the designed loud change), passes after the update. **THE SIX CARDS (verbatim effects):** Deva Sainika (C P3 vanilla), Aruna Charioteer (U P4, On Play +2 if Round 1), Agneyastra (R astra, deal 3 to an enemy Unit, `dmgAstra:true`) · Ash Legionnaire (Asura C P3 vanilla) · Kishkindha Runner (Vanara C P3 vanilla) · Coil Sentry (Naga C P3 vanilla). **⚠ DOC-vs-PROMPT discrepancy (doc won):** the task prompt listed the three extra vanillas without power; the roster doc specifies **P3** for all three — used P3. **Aruna weakest reading (ruled — GDD_DELTAS R29):** "if Round 1" = the match's FIRST round (`g.round===1`) at play time; played R2+, no bonus (verified both ways: R1→6, R2→4). **Agneyastra** resolves via `damageUnit(…, 'Agneyastra')` — the **cause-string** must match the card name for the Patala +1 to route (the tag alone doesn't); verified Patala deals 4, non-Patala 3, and targeting respects shield/immunity (`astraProtected`, like Gandiva → 0 targets when shielded/immune). Vanillas have NO on-play case (Ash Legionnaire's +1 in a play test is the **Asura Chaos Surge faction passive**, not a card effect). **Smoke sim:** 10 matchups × 50 = 500 games with `wave1:true` — 500/500 terminated, 444 exercised a wave-1 card, 0 crashes/hangs (NOT a balance measurement). Scope: exactly these six; no new mechanics/positional/mill/steal.
  - **✅ ENGINE TASK 3 (wave-1 batch 3 — the Round End tier, 5 cards) — DONE.** Authority: `docs/WAVE1_ROSTER_v0.2.md`. **STEP 0 — ROUND-TRANSITION SEMANTICS:** `endRound` (engine.js ~1140) order = **Kalki +2 → `venomRoundEnd` (venom ticks + `sweepDeaths`, deaths counted) → `roundEndCardEffects` (NEW) → scoring (`totalPower`) → match-over check (returns BEFORE reset on the deciding round) → board reset**. **Units CLEAR to discard at reset** (`pl.units=[]`, power/base NOT reset on the way to discard); **Heroes persist**. The new hook slots **after venom, before scoring** (the Kalki precedent — power changes count toward the round). **THE FIVE CARDS (verbatim):** Dawn Sentinel (Deva C P2, ROUND END if survived +1 **permanent**), Kamadhenu (Deva R P4, ROUND END lowest friendly +1), Savitur Verse (Deva U mantra — **the new match-long enchant surface**), Pisacha Skirmisher (Asura C P4, ROUND END −1 **permanent**), Mahishasura (Asura E P7, ROUND END −2 unless an enemy Unit died this round). **WEAKEST READINGS (ruled — GDD_DELTAS R21/R30/R31):** *"permanently"* (Dawn Sentinel/Pisacha) = increment **base AND power**, so it survives the clear-to-discard and a later revival (Kamadhenu/Savitur/Mahishasura touch power only). *Savitur* tracks the enchanted unit by **uid** (`pl.saviturUids[]`, match-long, never reset); if that unit is gone at a round end, nothing + **no retarget** (verified: +1 twice across two round ends, then target removed → enchant persists, no buff, no crash). *Mahishasura "died"* = ANY enemy-Unit destruction this round, ANY cause — via a NEW per-side `deathsThisRound` counter (incremented in `destroyUnit` after the splice; **`lastKillThisRound` was insufficient** — it only holds the LAST kill, for Sanjivani); snapshotted BEFORE the hook's own decay-kills so it's order-independent. *Kamadhenu tie* = **first-found** (`reduce` keeps the first; deterministic, no rng). **DEATH-AT-0:** the hook's power drops (Pisacha 1→0) die via a trailing `sweepDeaths` before scoring (verified: Pisacha p1 → DESTROYED). **PARITY (flag-off):** the hook has an explicit **fast-out** — returns immediately unless a batch-3 card is on a board or a Savitur enchant is active → no state/rng/`sweepDeaths`, **structurally unreachable**; `deathsThisRound`/`saviturUids` are new fields nothing reads flag-off. Proven: full suite green, all 10 matchups byte-identical (Deva-vs-Naga **40.9/59.1**), scenario rng-trace 50. **ASTRA_DMG invariant UNCHANGED** (no astras in this batch — still `{Agneyastra, Lanka Dahan, Pashupatastra}`, deliberate no-change check). **ORDER interaction (PROOF 4):** a venom kill at round end **IS counted** by Mahishasura (venom runs before the hook) → no decay (verified). **Smoke sim:** 500 games (10×50) `wave1:true` — 500/500 terminated, 317 exercised a batch-3 card, 0 crashes/hangs (not balance). `endRound`/`roundEndCardEffects`/`castMantra` exported for tests (benign). Scope: exactly these five; no heroes/artifacts/mill/positional/steal.
  - **✅ ENGINE TASK 4 (wave-1 batch 4 — the passive-aura tier, 5 cards) — DONE.** Authority: `docs/WAVE1_ROSTER_v0.2.md` (R22 governs Holika — now **ruled and written into `docs/GDD_DELTAS.md` R22**). **STEP 0 — PASSIVE ARCHITECTURE (the dominant pattern):** board-state-conditional passives are **computed READ-TIME in `effPower(g,pi,c)`** (engine.js ~284), **card-id-gated** — the launch precedents are the Indra aura (`indraOnBoard`), Vanara Warrior (`c.id==='warrior'`, +1 per other Vanara max +4), Naga Warrior (`c.id==='nagawarrior'`, +1 per Venom Token); Sharabha's targeting protection (`sharabhaProtected`) and the shield check (`isShielded`→`shieldedSet`) are likewise read-time. **The one exception is Jambavan/Hanuman** — an on-ENTRY power mutation via `hanumanEntryBonus(pl)`, not a read-time aura. Batch 4 follows the **read-time effPower** pattern for the four aura cards; Holika is a **damage/drain-path** rule (immunity + sharpening on incoming reductions, not a power read), so it hooks `damageUnit`/`venomLoss`, not `effPower`. **THE FIVE CARDS (verbatim):** Ushas, Dawn Herald (Deva U P3, PASSIVE: your **other** Units at ≤2 power get +1), Vigil Rakshak (Deva R P5, +2 while shielded), Shumbha (Asura R P4, +1 while Nishumbha on your board), Nishumbha (Asura R P4, +1 while Shumbha on your board), Holika (Asura R P5, immune to Astra damage; +1 from every other power loss). **effPower branches** (`c.id`-gated → unreachable without a wave-1 card on a board): Ushas checks OTHER friendly units `c.power<=2` and a friendly `id==='ushas'` present; Vigil `id==='vigilrakshak' && isShielded`; Shumbha/Nishumbha are mutual **presence** checks (no recursion, no double-count). **Holika** in `damageUnit`: an `id==='holika'` guard at the top — `ASTRA_DMG.has(cause)` → log/emit `block` + **return (0 damage)**, ELSE `amt+=1`; this precedes the Patala `+1`, so **immunity beats the realm sharpening** (PROOF 4). In `venomLoss`: `if(u.id==='holika') amt+=1` at the top → every venom drain costs her +1. Destroy-class (Vajra/Brahmastra/Pashupatastra-KILL) routes through `destroyUnit`, not `damageUnit`, and `isAstraImmune` is hiranya-only → **Vajra destroys her normally**. **WEAKEST READINGS (ruled — GDD_DELTAS R22; Ushas boundary is a NEW reading pending a ruling number):** *Ushas "≤2 power"* = **CURRENT stored power** (`c.power`) at eval, chosen non-recursive (NOT `effPower(c)`, to avoid effPower→Ushas→effPower) — a **mutation-buff to 3 exits** the aura, a **drain to ≤2 enters** it; an *aura* buff on the target (e.g. Indra +1) does NOT change `c.power` so it would **stack** and keep the target in-aura. Ushas is p3 → never self-buffs (and `c.id!=='ushas'` excludes the source). *Holika "+1 from all else"* is implemented at the two reduction choke points (`damageUnit` + `venomLoss`) which cover every named case — venom, deal-N astras that aren't dmgAstra, unit deal-N, −N debuffs; **Dhumraksha's deal-1 and Mohanastra's −2 are FUTURE cards** (not in the engine) that route through the same `damageUnit` path, so they'll sharpen identically when they ship (proven via in-engine analogues: **Naga Archer deal-1 → costs 2**, **Vayu −2 → costs 3**). **PARITY (flag-off):** all effPower branches are `c.id`-gated on ids no launch card has; Holika's `damageUnit`/`venomLoss` guards are `unit.id==='holika'` — **structurally unreachable without a wave-1 card in play**. Proven: full suite green (invariants · scenario 50 · venom 38 · story 48 · **0 console.log**), all 10 matchups byte-identical (Deva-vs-Naga **40.9/59.1**). **ASTRA_DMG invariant UNCHANGED** (no astras in this batch — still `{Agneyastra, Lanka Dahan, Pashupatastra}`, deliberate no-change check). **PER-CARD BOTH-WAYS (24 checks, wave1 on):** Ushas aura on/off + the current-vs-printed boundary both directions + no-self-buff + aura vanishes when Ushas leaves; Vigil +2 through the **real** `designateShield` lifecycle then reverts when the shield lifts; Shumbha/Nishumbha alone=printed, together=both 5 (no double-count), one-dies→survivor reverts, both directions; Holika Agneyastra/Pashupatastra/Lanka Dahan **deal 0**, venom-1 **costs 2**, Naga Archer deal-1 **costs 2**, Vayu −2 **costs 3**, **Vajra destroys her**; PROOF 4 Patala+Agneyastra & Patala+Pashupatastra **still 0**. **Smoke sim:** 500 games (10×50) `wave1:true` — 500/500 terminated, **292 exercised a batch-4 card**, 0 crashes/hangs (not balance). No new exports. Scope: exactly these five; no heroes/artifacts/positional.
  - **✅ ENGINE TASK 5 (wave-1 batch 5 — the draw/discard tier, 3 cards) — DONE.** Authority: `docs/WAVE1_ROSTER_v0.2.md`; rulings ruled in `docs/GDD_DELTAS.md` R21–R31. **THE THREE CARDS (verbatim):** Blood Oath (Asura U mantra, "Destroy your lowest-power Unit: draw 2"), Dawn's Rebirth (Deva **E mantra — the wave's FIRST Ratna implemented**, "Return your highest-power Unit from the discard at its printed power; it cannot be shielded for the rest of the match"), Swayamprabha (Vanara R P3 unit, "ON PLAY: Look at the top 3 cards of your deck; take one to your hand, return the rest"). **IMPLEMENTATION (all reuse launch precedents):** *Blood Oath* is a `castMantra` branch — `destroyUnit(g,pi,sac,'Blood Oath')` on the **lowest-effPower** unit (`reduce` `<=` → tie **first-found**, R31 symmetry) exactly like Sarpa Satra, then `pl.deck.splice(0,2)` (draw **up to 2**, deck-permitting — 1-card→1, empty→0, no crash, Sugriva/line-959 precedent); **gated in `playableIndices`** (illegal with no friendly non-ghost Unit). *Dawn's Rebirth* is a `castMantra` branch — highest-**base** discard unit (`>=` → tie first-found; **R21 grown-is-grown** — base already carries permanent growth), returned at `power=base` with all transient flags cleared, then `best.noShield=true`; **NOT gated** — logs a no-op on an empty discard (Gayatri precedent). *Swayamprabha* is the `case 'swayamprabha':` on-play twin of Tara — takes the highest-printed of the top 3, returns the rest to the **top** of the deck. **THE noShield FLAG (three guards, all `noShield`-keyed):** `designateShields` (AI auto) excludes it from candidates · `designateShield` (manual) refuses it · `shieldedSet` **deletes** its uid last (beats a Gayatri `revivedShield` sneaking it in). The flag is on the **card object**, so it survives round-clear-to-discard and a later revival — **match-long, never cleared**. **RATNA is a meta-layer (M1 pools/ownership) — the engine treats the Dawn's Rebirth def identically to any Gupta mantra def; no Ratna gate in engine.** **DEATHS-ARE-PER-SIDE (Mahishasura interaction, demonstrated):** `destroyUnit(g,pi,…)` increments **the OWNER (caster) side's** `deathsThisRound`. So a self-Blood-Oath death does **NOT** feed the **caster's own** Mahishasura (its `enemyDied[pi]` reads the *opponent's* counter, still 0 → it decays), but it **DOES** feed the **enemy's** Mahishasura (from their view your Unit is an enemy that died → they're spared). **⚠ prompt-phrasing nuance:** the task said "does not feed enemy Mahishasura" — the precise truth is it does not feed the *caster's own* Mahishasura; the enemy's is fed. No special-casing — pure `destroyUnit`+`roundEndCardEffects` composition. **NO SPECIAL-CASING on destroy:** a warded lowest Unit consumes its ward and **survives at 1** (destroyUnit ward path), and the draw still happens (the oath was sworn); Dharma Shield is astra-target-prevention and never blocks a destroy. **PARITY (flag-off):** all three ids are absent from the launch pool (`wave:1`, gated by `opts.wave1`); the `noShield` guards test a field no launch card sets; the `castMantra`/on-play branches are id-gated. Proven: full suite green (invariants · scenario 50 · venom 38 · story 48 · **0 console.log**), all 10 matchups byte-identical (Deva-vs-Naga **40.9/59.1**). **ASTRA_DMG invariant UNCHANGED** (no astras — still `{Agneyastra, Lanka Dahan, Pashupatastra}`). **PER-CARD BOTH-WAYS (29 checks, wave1 on):** Blood Oath legality gate both ways · lowest-effPower destroy + tie first-found · draw 2/1/0 · caster-side death counter + both Mahishasura outcomes · warded-lowest survives-at-1 + draw still fires; Dawn's Rebirth highest-base + tie first-found · returns at printed base (R21) · noShield set · astra-in-discard never returned · empty-discard no-op · **all three shield paths** (manual refuse / real `designateShields` auto skip via `playCard` unit entry / Gayatri-revive still unshielded) · normal-Unit shield control · flag survives round-clear; Swayamprabha takes-highest + returns-rest-to-top · tie first-found · 1-card/empty no-crash. **PROOF 4 (cross-batch):** Blood Oath destroys a Dawn Sentinel (the lowest) → its round-end +1 never fires. **Smoke sim:** 500 games (10×50) `wave1:true` — 500/500 terminated, **288 exercised a batch-5 card**, 0 crashes/hangs (not balance). No new exports. Scope: exactly these three; no mill (Drowned Altar waits), no bounce, no heroes.
  - **✅ ENGINE TASK 6 (wave-1 batch 6 — the debuff/price tier, 4 cards, all Asura) — DONE.** Authority: `docs/WAVE1_ROSTER_v0.2.md`; rulings `docs/GDD_DELTAS.md` R21–R31 (R21 governs Surpanakha, R22 every Holika interaction). **THE FOUR CARDS (verbatim):** Maya Shade (C P2 unit, "ON PLAY: copy your lowest other Unit"), Dhumraksha (U P4 unit, "ON PLAY: deal 1 to one of **your** Units"), Mohanastra (U astra, "enemy Unit −2 this round", **`dmgAstra:false` EXPLICIT in the def**), Surpanakha (R P4 unit **RATNA**, "ON PLAY: enemy Unit −1 permanently"). **IMPLEMENTATION:** *Dhumraksha* = on-play `damageUnit(g,pi,t,1,'Dhumraksha')` on a friendly (`targetSpec` friendlyUnit; doc says "your Units" **not "other"** → self-targetable, so **there is always a target, no fizzle case**; AI picks lowest-effPower OTHER than itself, else itself). The non-astra cause is the whole point — it routes through the `damageUnit` choke point so **Holika sharpens it (+1): deal-1 costs her 2** (Task-4 loop-closure, proven). *Mohanastra* = `resolveAstra` `case 'mohanastra'` → `damageUnit(g,1-pi,t,2,'Mohanastra')` (`targetSpec` enemyUnit respecting `astraProtected` like Agneyastra; gated in `playableIndices`). *Surpanakha* = on-play direct `t.base-=1; t.power-=1` (R21 permanent = base AND power) + `sweepDeaths` (R1 death-at-0); `targetSpec` enemyUnit **without** the shield filter (a unit on-play debuff is NOT an astra → does not respect Dharma Shield, like Ashvatara). *Maya Shade* = `case 'mayashade'` reusing **Maricha's full-overwrite** (`c.id=t.id; c.n; c.sub; c.r; c.txt; c.base; c.power`) of the lowest-effPower OTHER friendly (tie first-found); no other Unit → stays a vanilla P2 (logged no-op). **MOHANASTRA DURATION READING (STEP-0):** "this round" is implemented as a **current-power −2, base untouched** — NOT a scheduled restore. It is round-scoped **emergently**: units clear to discard at round end (Task-3 finding, power not reset), and every revival path (Gayatri/Dawn's Rebirth/Mrityunjaya) sets `power=base`, so a revived copy is un-dented. So no separate restore mechanism was invented — this is exactly how every launch debuff (Vayu/Ashvatara/Meghnad) already behaves. **Logged for the rulings queue.** **R22 INTERACTIONS (proven):** Holika + Mohanastra = **−2 costs her 3** (dmgAstra:false → NOT immune, sharpened +1); **Patala does NOT amplify Mohanastra** (−2 stays −2; Patala only lifts `ASTRA_DMG` causes); **Chaos Surge fires on Mohanastra's cast** (Asura astra path); and the **★ Chandrahas-doubled Mohanastra = −4** (the doubling calls `resolveAstra` twice → two −2s — a SIM FLAG). **SURPANAKHA/HOLIKA boundary (logged):** Surpanakha's permanent cut is a **direct base+power mutation**, so it bypasses the `damageUnit`/`venomLoss` choke points → Holika's +1 sharpening does **not** apply to a base reduction (a base cut is not a "power loss" in the damage sense). **PARITY (flag-off):** all four ids `wave:1`-gated; every branch is `c.id`/`unit.id`-gated on ids no launch card has. Proven: full suite green (invariants · scenario 50 · venom 38 · story 48 · **0 console.log**), all 10 matchups byte-identical (Deva-vs-Naga **40.9/59.1**). **ASTRA_DMG invariant UNCHANGED — the no-change check EARNED ITS KEEP: an astra (Mohanastra) IS added this batch but stays untagged (`dmgAstra:false`), so the derived set is still exactly `{Agneyastra, Lanka Dahan, Pashupatastra}` and the invariant passed with Mohanastra present.** **PER-CARD BOTH-WAYS (21 checks, wave1 on; Asura Chaos-Surge floor suppressed via `chaosThisRound=true` to isolate card effect):** Dhumraksha AI-lowest-other / explicit-target / alone-self-targets · Holika+Dhumraksha=2 · Holika+Mohanastra=3 · Mohanastra base −2 + Chaos-fired + Patala-not-amplified + **Chandrahas −4** + not-in-ASTRA_DMG · duration dent-then-revival-wipes · Surpanakha base+power −1 + restore-to-reduced-printed + P1→0 death + no-enemy no-op · Maya Shade full-overwrite-copy + tie first-found + alone-vanilla. **PROOF 4 (cross-batch):** Surpanakha −1 on an enemy Dawn Sentinel (2/2→1/1, survives) then its round-end +1 (1/1→2/2) — R21-down then R21-up compose, no ratchet. **Smoke sim:** 500 games (10×50) `wave1:true` — 500/500 terminated, **184 exercised a batch-6 card** (all four are Asura → only the 4 Asura-involving matchups can surface them), 0 crashes/hangs (not balance). No new exports. Scope: exactly these four; no Simhika (revive-trigger tier waits), no Atikaya (pass-tracking tier), no heroes.
  - **✅ ENGINE TASK 7 (wave-1 batch 7 — the protection tier, 5 cards) — DONE.** Authority: `docs/WAVE1_ROSTER_v0.2.md`; rulings `docs/GDD_DELTAS.md` R21–R31 (R25 governs Vedi Keeper, R22 every damage boundary). **THE FIVE CARDS (verbatim):** Vedi Keeper (Deva C P3, "next Dharma Shield this round free/instant"), Ribhu Craftsman (Deva U P3, "your Artifact untargetable this round"), Airavata's Calf (Deva R P4 **RATNA**, "enters shielded"), Ratri Hymn (Deva R mantra, "prevent all Astra DAMAGE to your Units this round"), Maya Veil (Asura E mantra **RATNA**, "your Units untargetable by Astras this round; AoE pierces").
    - **STEP 0 — THE SHIELD ECONOMY (R25's deferred verification):** shields are granted two ways — AI **auto** (`designateShields`, latches the highest-power unshielded non-`noShield` Unit up to `shieldCap`, runs at the end of every Deva Unit play, line ~1146) and **manual** (`designateShield(g,pi,uid)`, the human SHIELD button, one specific uid). `shieldCap` = 1 (or 2 with Kavacha). **CRITICAL: manual `designateShield` NEVER touches turn state — it just pushes to `shieldUids` and returns; it is a UI designation OUTSIDE the `playCard`/turn flow. So shields are ALREADY ACTION-FREE in the engine.** `astraProtected` reads shields via `isShielded`→`shieldedSet` (sticky `shieldUids` + Gayatri `revivedShield`, minus `noShield`). A shield is "consumed" only as astra-target-prevention (a shielded Unit drops out of `targetSpec`); it is never spent/removed, and `shieldUids` resets at round end. **→ R25 FALLBACK BRANCH FIRED** (shields are action-free, not turn-costing). **Vedi Keeper is therefore implemented as the fallback:** on-play `pl.vediShieldGrants++` → `shieldCap` gains +1 this round; the extra shield is "applied instantly" for the AI by the existing `designateShields` call at the end of Vedi Keeper's own play (proven: with the +1 cap the AI instantly shields 2 Units, not 1), and available immediately for the human via the raised cap. One per Vedi Keeper; `vediShieldGrants` resets at round end so **unconsumed grants expire** (proven: cap 2 → endRound → cap 1). Logged as the R25 ruling outcome.
    - **RIBHU — NOT a no-op (the "if one exists" branch):** the artifact-targeting search found **Vishwakarma** (launch Deva unit, "ON PLAY: Destroy the opponent's Artifact") — the ONLY effect that targets/destroys an enemy Artifact. (Tripura self-shatters on an astra = a self-effect, not targeting; self-replace = not targeting; Yaksha realm already blanket-blocks destroys.) So Ribhu is **wired to Vishwakarma**: on-play sets `pl.artifactShieldRound=g.round`; Vishwakarma's destroy path gains an `opp.artifactShieldRound===g.round` guard that whiffs (mirroring the Yaksha guard, which is left untouched). Proven both ways: no-Ribhu → Vishwakarma destroys; Ribhu → whiffs, Artifact survives; **self-replace still works** under Ribhu (playing a new Artifact over your own is not targeting). Only relevant in a Deva mirror (both cards Deva), but it's a real, non-forward-compat wire.
    - **AIRAVATA'S CALF:** on-play `designateShield(g, pi, c.uid)` — the **real** shield-grant mechanism (no parallel flavor). **Respects the cap** (cap full → Calf enters unshielded, proven) and the **`noShield` flag** — a Calf returned by Dawn's Rebirth carries `noShield`, and the Task-5 three guards refuse it (proven: `designateShield` returns false, `isShielded` false). Enters shielded when the cap has room (proven).
    - **RATRI HYMN (R22):** a round stamp `pl.ratriRound=g.round`, checked **FIRST in `damageUnit`** (before Holika/Patala): `ASTRA_DMG.has(cause) && owner.ratriRound===g.round` → log/emit `block`, **return 0**. Only `dmgAstra` causes route here, so it prevents Agneyastra/Pashupatastra/Lanka Dahan damage but is structurally blind to destroys (Vajra → `destroyUnit`), debuffs (Mohanastra −2, not in `ASTRA_DMG`), venom (`venomLoss`), binds. Proven: Ratri+Agneyastra **0**, Ratri+Mohanastra **−2 lands**, Ratri+Vajra **destroy lands**, Ratri+Holika **0** (Ratri returns first — she's just 0, no double-count), Ratri+**Patala**+Agneyastra **0 not 1** (beats the sharpening), Ratri+**Chandrahas-doubled** Agneyastra **both halves prevented**.
    - **MAYA VEIL (targeting layer):** a round stamp `pl.mayaVeilRound=g.round`, added to **`astraProtected`** → the owner's Units drop out of every targeted astra's `targetSpec` this round. **AoE structurally pierces** because Pashupatastra/Lanka Dahan/Nagastra iterate `opp.units` directly and never consult `targetSpec`/`astraProtected` (verified). Proven: Maya Veil + Agneyastra → **no marks** (targetSpec empty, astra illegal); Maya Veil + Pashupatastra → **pierces**. **ORTHOGONALITY MATRIX (the two flags answer different questions):** Maya Veil **blanks** a targeted Mohanastra (targeting, damage-type irrelevant); Ratri does **not** stop Mohanastra (debuff ≠ damage) — both cells demonstrated.
    - **PARITY (flag-off):** all five ids `wave:1`-gated; the new `damageUnit`/`astraProtected`/`shieldCap`/Vishwakarma guards read player flags that are **0 by default and only stamped by these cards' on-play/cast**, so flag-off they never fire. Proven: full suite green (invariants · scenario 50 · venom 38 · story 48 · **0 console.log**), all 10 matchups byte-identical (Deva-vs-Naga **40.9/59.1**). **ASTRA_DMG invariant UNCHANGED** — no new damage astras; Ratri only READS the set (still `{Agneyastra, Lanka Dahan, Pashupatastra}`). **PER-CARD BOTH-WAYS (25 checks, wave1 on).** **PROOF 4 (cross-tier):** Vigil Rakshak (batch 4) `+2` is self-only (`c.id==='vigilrakshak'`), so a shielded Airavata's Calf gets **no** Vigil bonus (stays 4) while a shielded Vigil is 7 — no accidental cross-trigger. **Smoke sim:** 500 games (10×50) `wave1:true` — 500/500 terminated, **238 exercised a batch-7 card**, 0 crashes/hangs (not balance). No new exports. Scope: exactly these five.
  - **✅ ENGINE TASK 8 (wave-1 batch 8 — the turn-economy tier, 3 cards) — DONE. HIGHEST-CAUTION tier (touches the turn flow).** Authority: `docs/WAVE1_ROSTER_v0.2.md`; `docs/GDD_DELTAS.md` (R26 governs Blueprint). **THE THREE CARDS (verbatim):** Mayasura's Blueprint (Asura E artifact, "Once/round, an Astra doesn't consume the turn"), Atikaya (Asura E P6 **RATNA**, "enters −2 if you haven't passed this match, +2 if you have"), The Long Patience (Naga E mantra **RATNA**, "Apply 1 Venom to EVERY enemy Unit").
    - **BRIHASPATI PREAMBLE (read-only findings):** (a) no Mantra yet → `g.lastMantra` null → logs "no Mantra has been spoken yet", Brihaspati enters as a vanilla P5 (no crash, no effect). (b) `g.lastMantra` is set in `castMantra` and reset ONLY in `newGame` → it **PERSISTS across rounds**, resets only per match. (c) the EFFECT matches the frame text (`castMantra(g,pi,g.lastMantra)` copies + re-triggers the actual last mantra by either player), **but the LOG mislabels it** — `g.lastMantra==='gayatri'?'Gayatri Mantra':'Pavamana'` calls ANY non-gayatri mantra "Pavamana" (e.g. copying Rama Naam/Blood Oath logs "repeats Pavamana"). **⚠ RE-EXPORT/LOG WATCHLIST: Brihaspati's repeat-log names only gayatri/pavamana; every other mantra is mislabeled "Pavamana" (cosmetic — effect is correct).**
    - **STEP 0 — THE TURN ECONOMY (findings):** `afterAction(g,pi)` is the single turn-switch chokepoint (called at the end of `playCard`, `pass`, and the AI unbind). Its order: `sweepDeaths` → both-passed?`endRound` → **`if (g.grantExtraTurn===pi && !pl.passed){ grantExtraTurn=null; g.turn=pi; return; }` (the EXISTING "act again" path, used by Mahabali)** → else `next = opp.passed?pi:1-pi` → consume `skipNext` (Tamasa/Angad) in a `guard<4` while-loop → `g.turn=next`. **A round PASS (`pass()` sets `pl.passed=true`, out for the round) is fundamentally different from a `skipNext` turn-skip (skip ONE turn, stay in the round).** `pass()` is the ONLY place voluntary-pass state is set (`pl.passed`, `mahabaliArm`, now `hasPassedThisMatch`); Tamasa (`opp.skipNext`) and Angad (`caster.skipNext`) never call `pass()` → **the engine already distinguishes voluntary pass from forced skip** (the Mahabali/Tamasa precedent Atikaya follows). Varuna's cap = a `playableIndices` gate (`opp has varuna && pl.astrasThisRound>=1` → astra illegal). The AI loop is external: `while(!g.over) aiTakeTurn(g,g.turn)` — one action per call, so a kept turn (grantExtraTurn) just re-dispatches to the same player, **bounded**.
    - **BLUEPRINT — the safest possible implementation: `afterAction` is UNCHANGED.** The astra branch, after Angad, sets `g.grantExtraTurn=pi; pl.blueprintUsed=true` when `pl.artifact.id==='blueprint' && !pl.blueprintUsed` — **reusing the Mahabali `grantExtraTurn` mechanism that is already in the LAUNCH BASELINE (hence AI-proven safe)**. R26 "everything else stands", each demonstrated: **Angad's forfeit still fires** (both compose — extra action NOW + `caster.skipNext` forfeits the next natural turn); **Varuna's per-round cap still binds** (a 2nd astra stays illegal in `playableIndices`); **Chaos Surge fires normally**; **once-per-round** (`blueprintUsed`, reset at round end — a 2nd astra grants no 2nd extra turn); a **Manasa-negated astra STILL grants the extra turn** — the hook is placed AFTER the negation branch, ungated ("the cast happened", R24 logic — **LOGGED for the rulings queue alongside the pending Brahmadanda interaction, not decided silently**). **CRITICAL SAFETY PROVEN:** the doubled 1000-game smoke (below) shows the **max guard reached in ANY game = 43** (vs the 800 hang cap) with 0 crashes — the AI-holds-Blueprint "act again" never loops or double-acts. Turn-integrity sequence dumped (PROOF 6): `p0 astra → p0 extra → p1 → p0` — alternation correct everywhere except the one sanctioned extra action.
    - **ATIKAYA:** on-play `c.power += hasPassedThisMatch ? 2 : -2` (power-only entry modifier, Aruna/Kaliya precedent — a revival restores base 6). `hasPassedThisMatch` is a **match-long** flag set ONLY in `pass()` (voluntary), never reset, never set by Tamasa/Angad. Proven: not-passed → 4, passed → 8, a forced `skipNext` leaves it false → still 4.
    - **THE LONG PATIENCE:** ("skip your turn" simplified to normal mantra economy per v0.2) a `castMantra` branch reusing the **Nagastra token-application** (`u.venom++` for every enemy Unit; gated in `playableIndices` like Nagastra). **NOT Astra damage** (no `damageUnit`): Patala does not amplify the APPLICATION (+1 token, not +2), Ratri Hymn does not stop it, and **Holika's `venomLoss` +1 sharpens the eventual DRAIN, not the application** — proven: applying gives Holika a token + no immediate loss, then the drain tick costs her 2.
    - **PARITY (flag-off):** all three ids `wave:1`-gated; the Blueprint hook is `pl.artifact.id==='blueprint'`-gated (never in the launch pool → never sets `grantExtraTurn`, so `afterAction` runs its baseline path); `hasPassedThisMatch`/`blueprintUsed` are new fields nothing reads flag-off except the id-gated Atikaya/Blueprint. The `pass()` edit only sets a new field. Proven: full suite green (invariants · scenario 50 · venom 38 · story 48 · **0 console.log**), all 10 matchups byte-identical (Deva-vs-Naga **40.9/59.1**) — **the turn-flow edit did NOT disturb the baseline. ASTRA_DMG invariant UNCHANGED** (venom application is not damage). **PER-CARD BOTH-WAYS (28 checks).** **Smoke sim (DOUBLED for the turn-flow risk):** 1000 games (10×100) `wave1:true` — **1000/1000 terminated, 475 exercised a batch-8 card, 0 crashes, 0 hang-cap hits, max guard 43** (not balance). No new exports. Scope: exactly these three; no Simhika/Kartikeya's-Vanguard (the event-trigger tier is batch 9).
  - **✅ ENGINE TASK 9 (wave-1 batch 9 — the event-trigger tier, 4 cards) — DONE. These cards LISTEN; the risk was trigger placement.** Authority: `docs/WAVE1_ROSTER_v0.2.md`; `docs/GDD_DELTAS.md` R21 (Vishalakshi permanence). **THE FOUR CARDS (verbatim):** Simhika (Asura U P4, "enemy Unit revived → +2"), Kartikeya's Vanguard (Deva E P5, "friendly Unit destroyed → +2, once/round"), **Vishalakshi the Pale** (Naga R P4, "enemy dies with Venom on it → +2 permanent" — **the doc RENAMED "Padma the Pale" → "Vishalakshi the Pale"**, id `vishalakshi`), Raktabija's Curse (Asura E mantra, "next friendly Unit destroyed this round → spawn two 2-power Rakta tokens").
    - **STEP 0 — EVENT CHOKE-POINT MAP:** **(a) DEATH — `destroyUnit` is the SINGLE choke point.** Order: `isAstraImmune` return (no death) → `ward` return (survives at 1, no death) → `pl.units.splice; deathsThisRound++; emit('destroy')` **← THE DEATH POINT** → aegis revive → discard-push + Yama-ghost. ALL real deaths route here: direct destroys (Vajra/Tataka/Blood Oath/Sarpa Satra), `damageUnit` deaths (Agneyastra/Pashupatastra/Kumbhakarna/etc.), venom (`venomLoss`→`sweepDeaths`→`destroyUnit`, cause `'reduced to 0'`). **⚠ FINDING: Ahamkara's "doomed to shatter at round end" is NOT a `destroyUnit` death** — `doomed` is only read at the round-end board-clear (bulk discard-push AFTER scoring), so the doubled power counts that round then the unit clears like every unit; it does NOT feed the death-listeners (a round-end clear ≠ a destruction — defensible). **(b) REVIVAL — SCATTERED across 8 live sites, only 1 emits `revive`.** Enumerated (line refs approx): Amrita aegis (`destroyUnit` ~535, the sole `emit('revive')`), Gayatri (~665), Mrityunjaya (~688), Sanjivani **Corruption** (Asura steal, ~708), Sanjeevani **Call** (Deva **ASTRA**, in `resolveAstra` ~951), Dawn's Rebirth (~766), Ulupi (~1165), Shesha (round-start ~1426). **⚠ FINDING: Shukracharya's roster "once-per-game revive" is UNIMPLEMENTED** (the hero on-play only dispatches sugriva/vasuki) → not a live path, so not wired here (**→ REPAIRED in Task 9.5 as revival site #9, wave-1-gated**). Since revivals are scattered, Simhika is wired via an `onUnitRevive(g,toPi,unit)` helper CALLED AT ALL 8 SITES. **(c) YAMA GHOST:** `destroyUnit` mints `{id:'ghost',ghost:true,power:1,base:1}` on a friendly death under Yama; ghosts are filtered by `!u.ghost` everywhere (effPower/totalPower/targeting) → contribute nothing → Rakta tokens must be REAL (`ghost:false`).
    - **IMPLEMENTATION — two pure LISTENER helpers** (`onUnitDeath`, `onUnitRevive`), each id-gated on the new card ids / flag-gated on new fields → **no-ops flag-off**. `onUnitDeath(g, ownerPi, unit, cause)` is called ONCE in `destroyUnit` right after `emit('destroy')` (before the aegis revive → venom still intact for Vishalakshi; consistent with `deathsThisRound` which counts an aegis death too); it guards `if(unit.ghost)return` (Yama ghosts don't feed listeners; a Rakta token does). It fires: **Vanguard** (side `ownerPi`, first friendly death/round via `vanguardTriggered`; its own death can't trigger it — already spliced off, so `find(id==='vanguard' && u!==unit)` misses it → "must survive to witness"), **Vishalakshi** (side `1-ownerPi`, if `unit.venom>0` → +2 **base AND power**, R21 permanent, stacks per death), **Raktabija's Curse** (side `ownerPi`, if `raktabijaCurse` armed → spawn 2 tokens + disarm). `onUnitRevive(g, toPi, unit)` fires **Simhika** (side `1-toPi` = the OPPOSITE side from where the unit revived → +2, stacks; a revival to Simhika's own side does NOT trigger).
    - **RAKTA-TOKEN READING (logged):** **REAL minted units** — `mkCard({id:'rakta',n:'Rakta',t:'unit',p:2,r:'C',token:true})` → `ghost:false`, uid-safe (mkCard's `UID++`), contribute board power (effPower 2), targetable/poisonable/killable. Ghosts were rejected (they contribute nothing). The tokens are **direct-pushed to `pl.units`, bypassing `playCard`**, so on-ENTRY auras (Hanuman's `hanumanEntryBonus`) do NOT apply (proven: spawned token effPower 2), while read-time `effPower` auras would — "whatever the entry path does," no special-casing.
    - **PARITY (flag-off):** the two helpers read board state for wave-1 ids and only mutate when those cards are present; flag-off none are in the pool and no Curse is armed → both are no-ops, so `destroyUnit` and every revival site behave byte-identically (the 8 inserted calls do nothing). `vanguardTriggered`/`raktabijaCurse` are new fields only the helpers read. Proven: full suite green (invariants · **scenario 50 — the rng-trace byte-identity that guards the 8 insertions** · venom 38 · story 48 · **0 console.log**), all 10 matchups byte-identical (Deva-vs-Naga **40.9/59.1**). **ASTRA_DMG invariant UNCHANGED.** **PER-CARD BOTH-WAYS (33 checks) — death/revive-path matrix:** Simhika vs 3 revival paths (Gayatri/Amrita-aegis/Sanjeevani-Call) + stacks + own-side-negative + in-discard-negative; Vanguard vs 3 death paths (sweep/Blood-Oath/enemy-Astra) + once/round + own-death-negative + round-reset + enemy-death-negative; Vishalakshi venom-drain + Vajra (cause-agnostic) + no-venom-negative + stacks; Raktabija arms/spawns-2-real-tokens/one-shot/round-expiry/entry-path. **PROOF 4 (cross-batch):** a Rakta token dying triggers Vanguard (it IS a friendly Unit); a poisoned Rakta dying feeds enemy Vishalakshi; Blood Oath sacrifice IS the "next friendly destruction" → tokens spawn + draw fires + `deathsThisRound` +1 once. **PROOF 6 (trigger-integrity):** Pashupatastra kills 3 poisoned friendlies → Vanguard fires EXACTLY once (+2), Vishalakshi once per venom-death (+6), no double-counts. **Smoke sim:** 500 games (10×50) `wave1:true` — **500/500 terminated, 312 exercised, 24 games spawned Rakta tokens, 0 crashes, 0 duplicate-uid games** (token spawns kept uids unique). No new exports. Scope: exactly these four; no Mahishi/Kulika, no heroes.
  - **✅ ENGINE TASK 9.5 (launch-repair — Shukracharya's printed revive, wave-1-gated) — DONE. PARITY RULE INVERTED: DEAD flag-off, ALIVE flag-on.** The batch-9 STEP-0 finding surfaced that **Shukracharya's launch frame text** ("TRIGGERED — once per game, revive one destroyed friendly Unit from discard at half its original power") **was never implemented** (the hero on-play only dispatched sugriva/vasuki → he was a vanilla P5). **ASSET CHECK:** `assets/cards/Asuras_Hero_Shukracharya_P5_rEpic.png` present; the `P5_rEpic` filename + printed text match the def (p:5, r:'E') → **no re-export pending on him**. **THE INVERSION:** unlike a wave-1 CARD (filtered out of the pool flag-off), Shukracharya is a LAUNCH card always in the pool — so the *ability branch* is gated, not the card. Added `g.wave1 = !!opts.wave1` in `newGame` (additive; nothing else reads it, no rng consumed) and gated the new hero-dispatch branch `if (c.id==='shukra' && g.wave1)`. **STRUCTURAL PARITY (flag-off DEAD):** with `g.wave1` false the branch never runs → Shukracharya does only `pl.heroes.push` (the vanilla-5 hero testers currently play); proven — all 10 matchups byte-identical (Deva-vs-Naga **40.9/59.1**) with the AI still drafting/playing him, and a direct test (play him flag-off with a stocked discard → **nothing revives**). **READINGS (logged for the rulings queue):** *Timing* ON PLAY; *"once per game"* is **structural** (a Hero enters once per match, persists, is never re-drawn — no counter flag). *Amount* = `Math.max(1, Math.ceil(base/2))` — half the **printed/base** power, ceil (Nala precedent); an R21-grown base revives at half the grown value. *Base UNTOUCHED* — a revival is not a permanence effect, so R21 does not apply to the revive amount (power set to the halved value, base kept). *Target* — **hero plays are UNTARGETED in the engine** (no Hero has a `targetSpec` case), so the dispatch auto-picks the **highest-base** fallen friendly and honors `targetUid` defensively for future UI wiring; the **player-choice reading is DEFERRED** (logged). *Empty discard* → logged no-op (Sanjeevani precedent). **REVIVAL SITE #9:** the revive uses the standard push + `onUnitRevive(g,pi,t)` so **Simhika sees it** (proven +2); the returning Unit is cleansed (venom/flags stripped, consistent with the other 8 sites). **BOTH-WAYS (13 checks, flag-on):** revive at ceil(7/2)=4 + base still 7 + removed from discard · ceil(5/2)=3 · auto highest-base · `targetUid` honored · empty no-op · Simhika +2 on his revive · **Vishalakshi does NOT fire (a revival is not a death)**. **Smoke sim:** 500 games (10×50) `wave1:true` — 500/500 terminated, **74 games exercised his revive** (Mritasanjivani log), 0 crashes. ASTRA_DMG unchanged; scenario 50 / venom 38 / story 48 / 0 console.log green. No new exports. Scope: exactly this one launch-repair.
  - **✅ ENGINE TASK 10 (wave-1 batch 10 — the positional tier, 5 cards, all Vanara) — DONE. Builds the R27 move/place primitive.** Authority: `docs/WAVE1_ROSTER_v0.2.md`; `docs/GDD_DELTAS.md` **R27** ("row" = the ordered units array, position = index, adjacency = index neighbours via `adjacentUnits()` — no new spatial model). **THE FIVE CARDS (verbatim):** Setu Mason (C P2, "+1 while adjacent to another Vanara"), Drummer of the Host (C P2, "ON PLAY: adjacent Units +1 this round"), Gavaksha (U P3, "ON PLAY: may swap places with a friendly Unit"), The Setu Stones (E artifact, "your Units enter adjacent to any chosen friendly"), Vault of the Sky (E mantra **RATNA**, "move a friendly Unit anywhere; +2 this round").
    - **STEP 0 — THE POSITIONAL SURFACE (findings):** **(a)** `playCard`'s `position` param does `pl.units.splice(position,0,c)` (insertion INDEX, bounds `0..length`, else append); passed by `aiPlacement` (Vanara → `bestIdx+1`, after the strongest ally) and the UI slot-picker. **(b)** `adjacentUnits(pl,unit)` = `[units[i-1],units[i+1]].filter(Boolean)` (index neighbours, edge-safe, self-excluded); consumers = Mainda, `bestLeap`/Leap, and Vayu's displace. **(c) ⚠ FINDING: no shared move primitive existed** — **Riksha's printed "Move to any position on the row" is UNIMPLEMENTED** (its case only does the Hanuman +3; like Shukracharya was — **→ REPAIRED in Task 10.5, wave-1-gated**), and **Vayu's displace** is a bespoke move-to-END (`splice(ti,1); push(t)`) on the ENEMY, not a shared helper. **(d)** splicing a unit out/in leaves uid/power/all state untouched (same object reference) — verified. **THE PRIMITIVE:** one shared `moveUnit(g,pi,unit,toIndex)` (splice-out + **clamped** splice-in, ghost-safe, state-preserving) + `swapUnits(pl,a,b)` (index exchange, for Gavaksha) + `boardEff(g,pi)` (AI heuristic). Vault routes through `moveUnit`; Gavaksha through `swapUnits`; the Stones ride `playCard`'s existing `position` param — **no third move path** (Vayu's move-to-end is left as-is; it's `moveUnit(…,length)`-equivalent, both splice-based, no divergence — confirmed).
    - **IMPLEMENTATION:** *Setu Mason* = read-time `effPower` branch (`c.id==='setumason' && adjacentUnits(...).some(!ghost)` → +1), non-recursive presence check, self-excluded (Task-4 pattern). **⚠ READING (logged) — SUPERSEDED by R40(b), see below:** with contiguous-array adjacency, Setu Mason (as first shipped) had ≥1 neighbour whenever there were **2+ Units** → "+1 while not alone". **→ R40(b) (Task R38–R41 append) SHARPENED it to "+1 while adjacent to Vanaras on BOTH sides"** (interior-only formation value): the `effPower` branch now checks both index neighbours exist and are non-ghost; edge/lone/ghost-side → no bonus. Def txt updated. The ghosts-block-formation reading (a ghost neighbour on one side blocks the flank, per R40's non-ghost filter) is CANONICAL and carries into batch 11 (Gaja/Living Bridge counts). *Drummer* = on-play current-power +1 to `adjacentUnits` (R34 emergent round scope); 0 neighbours → no-op. *Gavaksha* = on-play optional swap: `targetUid` names the partner (human/story); the AI tries each swap and keeps the best `boardEff` improvement. **⚠ FINDING (logged):** no `effPower` passive in the current pool is position-DEPENDENT (Setu Mason/Warrior are presence/count based), so a swap never changes `boardEff` → **the AI swap is effectively always a no-op** (holds its ground); the swap mechanism is live for humans/story via `targetUid` and future-proof for position-dependent cards. *Vault* = `castMantra` branch: lift the `targetUid`/strongest Unit +2 (R34) and `moveUnit` it adjacent to the WEAKEST ally (a Leap-setup heuristic — the buffed strong unit becomes a Leap target); gated in `playableIndices` (needs a friendly Unit). *The Setu Stones* = a `position` override in `playCard`: with the artifact active AND a `targetUid` anchor, the entering Unit's insertion index = `anchorIndex+1` (adjacent). **HONESTY CLAUSE:** the engine honors `targetUid` (story/AI-with-target/sim fully live); the AI/default path already places after-strongest (`aiPlacement`, adjacent to a friendly); **the human anchor-PICKER is a UI-layer task — DEFERRED, logged** (not silently faked). Likewise Vault's free-"anywhere" human destination is UI-deferred (the AI uses the Leap-setup heuristic).
    - **PARITY (flag-off):** the primitive (`moveUnit`/`swapUnits`/`boardEff`) has **NO flag-off callers** (Vault/Gavaksha/Vault are wave-1, id-gated); Setu Mason's `effPower` branch is `c.id`-gated; the Stones `position` override is `artifact.id==='setustones'`-gated (never in the launch pool) → the `position` param is used exactly as before. Proven: full suite green (invariants · **scenario 50 — guards the playCard positioning edit** · venom 38 · story 48 · **0 console.log**), all 10 matchups byte-identical (Deva-vs-Naga **40.9/59.1**). **ASTRA_DMG invariant UNCHANGED.** **PER-CARD BOTH-WAYS (29 checks):** moveUnit clamp-high/clamp-low/state-preserved/array-integrity; Setu Mason lone→2 / neighbour→3 read-time / neighbour-dies→2 no-sticky / adjacency-follows-array-after-move; Drummer 0/1/2 neighbours; Gavaksha targetUid-swap + partner-state-preserved + AI-no-op finding; Vault +2 + moved-adjacent-to-weakest + legality + targetUid; Stones mid-row + edge-clamp + no-anchor-fallthrough. **PROOF 4 (cross-batch):** a moved unit's **Leap adjacency reflects its NEW position**; a **Rakta token** (real unit) is moveable; **Vayu's displace** and `moveUnit` agree (object preserved). **PROOF 6 (order-integrity):** play→swap→move→Stones-place dumped step-by-step → final `[b,new,gavaksha,a]` exactly, no dup uids. **Smoke sim:** 500 games (10×50) `wave1:true` — **500/500 terminated, 190 exercised, 0 crashes, 0 bad-units-array games** (splice integrity held — no dupes/holes). **Exports:** `moveUnit`/`swapUnits`/`adjacentUnits` for tests (benign). Scope: exactly these five; no Living Bridge / Sushena / Kumuda (adjacency round-end tier next), no Saranyu (power-exchange, later).
  - **✅ ENGINE TASK 10.5 (launch-repair #2 — Riksha's printed move, wave-1-gated) — DONE. PARITY INVERTED: DEAD flag-off, ALIVE flag-on.** The batch-10 STEP-0 finding surfaced that **Riksha's def txt** (verbatim: "ON PLAY: Move to any position on the row. Gains +3 power while Hanuman is on the board.") had its MOVE unimplemented (only the +3 fired). **ASSET CHECK:** `assets/cards/Vanaras_Unit_Riksha_P4_rRare.png` — `P4_rRare` matches the def (p:4, r:'R'). **⚠ FRAME-vs-DEF WATCHLIST (new, cosmetic):** the CardRoster frame phrasing is "…on the **Yuddha Row**" while the engine def says "…on the **row**" — trivial, no mechanical difference; the DEF wins (it's the engine); logged as a minor cosmetic re-export note (non-blocking). **HANUMAN +3 (stated as-found, UNCHANGED):** it is an **ENTRY-TIME check** — a one-time `c.power+=3` at play IF Hanuman is on the board (NOT a persistent read-time aura). The repair is the MOVE only; the +3 stays exactly as-is and is NOT gated on wave1 (it's launch behavior). Note Riksha with Hanuman gains BOTH the general Vanara `hanumanEntryBonus` (+1) AND this +3 → 4+1+3=8 (both pre-existing). **THE MOVE (R41-pending readings):** *Timing* — ON PLAY, AFTER insertion: Riksha enters at his played (or Stones-overridden) slot, THEN relocates ONCE. Not persistent. *Destination channel* — **`playCard`'s signature did NOT accommodate a move destination** (`position` = entry slot, `targetUid` = target/Stones-anchor), so the move needed its OWN channel → added `movePosition` (6th optional param, defaults null; no launch caller passes it → byte-identical flag-off). *Destination* — rides the batch-10 `moveUnit` primitive (**no parallel path**), bounds-clamped; an explicit `movePosition` is honored, else the AI **Leap-setup heuristic** = relocate adjacent to the highest-effPower friendly (Riksha becomes a leaper beside a strong target). *Human picker* — UI-DEFERRED (same status as Vault/Stones), engine live. **DEAD flag-off:** the whole move block is `if (g.wave1)`-gated (the 9.5 mechanism) → launch Riksha enters in place, exactly what testers play. **STRUCTURAL PARITY:** proven — all 10 matchups byte-identical (Deva-vs-Naga **40.9/59.1**) with the Vanara AI still playing Riksha, and a direct test (play him flag-off into a 3-unit board → **position unchanged**). **BOTH-WAYS (11 checks, flag-on):** enters-then-relocates-to-heuristic (order dump: `[a,b,c]`+riksha@end → `[a,riksha,b,c]`, adjacent to the strongest `b`) · explicit `movePosition` to front · clamp-high→end · clamp-low→front · lone-Riksha structural no-op (no crash) · Hanuman +1/+3 unchanged (→8) · **Stones-places-then-move COMPOSE** (Stones entry after anchor, THEN his move relocates him adjacent to the strongest — entry ≠ final position) · moved Riksha's **Leap adjacency reads the new position**. **Smoke sim:** 500 games (10×50) `wave1:true` — 500/500 terminated, **79 games exercised his move**, 0 crashes, **0 bad-units-array games** (splice tripwire held). ASTRA_DMG unchanged; scenario 50 / venom 38 / story 48 / 0 console.log green. No new exports (the `movePosition` param is additive to `playCard`). Scope: exactly this one launch-repair.
  - **✅ ENGINE TASK 11 (wave-1 batch 11 — the Leap/round-end tier, 5 cards, all Vanara) — DONE. The Vanara wave core CLOSES here.** Authority: `docs/WAVE1_ROSTER_v0.2.md`; `docs/GDD_DELTAS.md` R21 (every "permanently"), R40 (formation/ghost semantics; Living Bridge's formal SIM flag), R34 ("damaged"). **THE FIVE CARDS (verbatim):** Gaja (U P4, "+1 while your board is wider"), Kumuda (U P3, "Leaps or is Leapt to → +1 permanently"), Sushena the Healer (R P4, "ROUND END: restore 1 to each adjacent damaged Unit"), Rambha the Bold (E P5 **RATNA**, "any friendly Leap → +1 permanently"), The Living Bridge (M artifact, "ROUND END: unbroken adjacent line of 4+ → all +1 permanent" = **4+ Units at round end** under the R40 gapless-board reading).
    - **STEP 0 — findings:** **(a) `doLeap` is the SINGLE leap choke point** — every leap routes through it: the standard AI leap (`bestLeap`→`doLeap(…,false)`, line ~1740), Mainda's FREE leap (`doLeap(…,true)`, ~1277), and each Kishkindha-Crown leap (the Crown is `leapLimit=2` = TWO separate `doLeap` calls + a per-leap +1/+1, NOT a single double). Future Gandhamadana/Makardhwaja will also call `doLeap`. So the listener hook (`onLeap`) goes in `doLeap` ONCE, after `leapsUsed++`, fires for every leap incl. free. **(b) "DAMAGED" = current power < base (logged R34-consistent):** a Mohanastra dent (power<base) is damaged/restorable; a **Surpanakha/Pisacha PERMANENT cut lowers base too → power==base → NOT damaged** (nothing to restore toward, R21/R35-consistent); a buffed-then-drained unit is damaged only below base; "restore 1" = +1 current power **capped at base** (no overheal). **(c) GHOSTS/TOKENS:** ghosts are NOT counted (non-ghost filter, the R40 reading) for Gaja's width or the Bridge's count; **Rakta tokens ARE real units and count for both** (proven).
    - **IMPLEMENTATION:** *`onLeap(g,pi,leaper,target)`* — pure leap listener called once per `doLeap`; **Kumuda** (leaper XOR target → +1 base AND power, R21, one trigger per leap) + **Rambha** (any Rambha on the LEAPING side pi → +1 base+power; her own leap counts; enemy leaps never touch her since the hook runs for side pi only). *Gaja* = read-time `effPower` branch (`c.id==='gaja'`, `mine>theirs` strict non-ghost counts → +1; re-evaluates). *Sushena + Living Bridge* = the batch-3 `roundEndCardEffects` hook (gate extended: `sushena` added to `RE_IDS`, `livingbridge` artifact check added). **DETERMINISTIC HOOK ORDER (reported/logged):** within the per-player pass the order is Savitur → Sentinel/Pisacha/Mahishasura decay → Kamadhenu → **Sushena (restore, capped at base) → The Living Bridge (+1 permanent to all if 4+)**. **Sushena runs BEFORE the Bridge** — consequence: a dented unit is healed toward base FIRST, then the Bridge's +1 base/power lands on top (proven: 6/4 dented → Sushena 4→5 → Bridge → power 6, base 7). The Bridge is **SIM-FLAGGED (R40) but implemented as-written, no haircut yet**.
    - **PARITY (flag-off):** the `doLeap` `onLeap` call is id-gated (kumuda/rambha) → no mutation/rng flag-off, so **launch Leaps (Mainda/Crown/standard) are byte-identical**; Gaja's `effPower` branch is `c.id`-gated; Sushena/Bridge are id/artifact-gated in the already-fast-out hook. Proven: full suite green (invariants · scenario 50 · venom 38 · story 48 · **0 console.log**), all 10 matchups byte-identical — the **four Vanara-involving matchups** (Vanara mirror **52.8**, Deva-vs-Vanara 45.1, Asura-vs-Vanara 42.3, Vanara-vs-Naga 57.3) confirm the leap-hook did not disturb launch Leaps; Deva-vs-Naga **40.9/59.1**. **ASTRA_DMG UNCHANGED.** **PER-CARD BOTH-WAYS (23 checks):** Kumuda leaper/target/Crown-double(+2); Rambha own-leap/uninvolved-friendly/**FREE-leap**/enemy-negative; Sushena Mohanastra-dent-restored / Surpanakha-cut-NOT-restored / cap-at-base / non-adjacent-negative; Gaja wider/tie-negative/narrower-negative/re-evaluate; Bridge exactly-3-nothing / exactly-4-all+1 / **Rakta-token-counts-and-gains**. **PROOF 4 (cross-batch):** Kumuda+Rambha on ONE leap both fire; **Bridge+Kumuda compose** (base tracks both permanence sources: 3+1+1=5); Sushena-before-Bridge order deterministic. **PROOF 6:** Crown double-leap with Kumuda+Rambha dumped → Kumuda base 3→5 (+2), Rambha base 5→7 (+2), counts exact. **Smoke sim:** 500 games (10×50) `wave1:true` — **500/500 terminated, 189 exercised, 0 crashes, 0 bad-units-array games** (leaps+moves+tokens in one sim). No new exports. Scope: exactly these five — **the Vanara wave core is now complete**.
  - **✅ ENGINE TASK 12 (wave-1 batch 12 — the cleanup tier, 5 cards) — DONE.** Authority: `docs/WAVE1_ROSTER_v0.2.md`; `docs/GDD_DELTAS.md` R21–R41 (**R42-PENDING** items carried from batch 11: *damaged = current power < base*, and the round-end *hook order* — both used here, still awaiting a formal ruling number). **THE FIVE CARDS (verbatim):** Dhanvantari (Deva R P4, "ON PLAY: restore one Unit to printed power"), Shakti Spear (Deva E astra, "destroy an enemy Unit ≤4 power"), Dawn Banner (Deva E artifact, "round start: all friendly +1 this round" — SIM-flagged "compounding"), Vidyutastra (Asura R astra, "deal 2; Chaos Surge triggers twice", **`dmgAstra:true`**), Andhaka (Asura R P6, "untargetable while another friendly Unit on board").
    - **STEP 0 — THE ROUND-START SURFACE (findings):** there is **no per-round-start hook** and **no units-present round-start moment** — every round begins with an **EMPTY board** (`endRound` board-reset does `pl.units=[]`, then `g.round++`), and **artifacts CLEAR at round end** (line ~1494). Existing "start" hooks are per-TURN (`onTurnStart`: Tripura, Deva Soldier). **CONSEQUENCE:** a literal "round-start current-power buff" would be inert (no units to buff) and the Banner wouldn't survive to a later round start. **RESOLUTION (weakest FUNCTIONAL reading, logged):** Dawn Banner is a **match-long stamp** `pl.dawnBannerFrom = g.round+1` (set on play; survives the artifact clear) driving a **read-time +1 aura in `effPower`** on ALL your non-ghost Units while `g.round >= dawnBannerFrom`. So: **NO retroactive buff** the round it's played (`g.round < from`); **active from the NEXT round** and **every round thereafter** (the "compounding" the roster SIM-flags — implemented as-written, no haircut). Round-1 note: a Banner played mid-round-1 fires from round 2.
    - **IMPLEMENTATION:** *Dhanvantari* = on-play restore a friendly **damaged** (power<base) Unit to `power=base` (R21 grown base → restores to the GROWN value); AI = most-damaged (largest `base-power`); none damaged → no-op; `targetSpec` friendlyUnit-damaged. *Shakti Spear* = `resolveAstra` DESTROY-class (`destroyUnit`, like Vajra/Gandiva — **NOT dmgAstra**, R22); `targetSpec`/legality require an enemy at **≤4 CURRENT effPower** (R32 symmetry — a drained 5 is legal, a buffed 3 escapes) and respect `astraProtected`; immunity-agnostic (kills Holika). *Vidyutastra* = `resolveAstra` `damageUnit(…,2,'Vidyutastra')` (**`dmgAstra:true`** → routes Patala +1 / Holika-immune-0 / Ratri-prevents by cause). *Andhaka* = an `astraProtected` clause (`unit.id==='andhaka' && any OTHER friendly non-ghost`) → untargetable while flanked; **AoE pierces** (Maya-Veil precedent — AoE never consults `astraProtected`); alone → targetable.
    - **⚠ THE LOUD ASTRA_DMG CHANGE (the Task-2 ceremony, EARNED):** Vidyutastra is `dmgAstra:true`, so the derived `ASTRA_DMG` set grows to **4**. The permanent test.js invariant **FAILED LOUDLY first** (`✖ 1 INVARIANT FAILURES` — derived `{Agneyastra, Lanka Dahan, Pashupatastra, Vidyutastra}` vs expected 3), then the expected list was updated to the four and it **passes**. Demonstrated both states.
    - **⚠ SURGE-COMPOSITION READING (logged for rulings):** Vidyutastra "triggers Chaos Surge twice" = the astra-branch `chaosSurge` count is `(chandrahasActive?2:1) × (c.id==='vidyutastra'?2:1)`. So **Vidyutastra alone → 2 surges; Vidyutastra + Chandrahas → 2×2 = 4 surges** (Chandrahas's own doubling composes multiplicatively). Demonstrated (surge-count dump: 2 and 4). Not designed silently — logged.
    - **PARITY (flag-off):** all five ids `wave:1`-gated; the Dawn Banner aura reads `dawnBannerFrom` (init 0, only stamped by the wave card); the Vidyutastra surge-multiplier is `c.id==='vidyutastra'?2:1` (=1 for every launch astra); Andhaka's `astraProtected` clause and Shakti/Vidyut `resolveAstra`/`targetSpec`/legality are id-gated. Proven: full suite green (invariants · scenario 50 · venom 38 · story 48 · **0 console.log**), all 10 matchups byte-identical (Deva-vs-Naga **40.9/59.1**). **THE `dmgAstra` SUBTLETY (as with Agneyastra, batch 2):** the derivation runs over ALL defs, so `ASTRA_DMG` grows flag-off too — harmless (Vidyutastra can't enter play flag-off; the Patala column is byte-identical, baseline unchanged). **PER-CARD BOTH-WAYS (25 checks)** + **PROOF 4** (Dhanvantari→Surpanakha-cut restores to the CUT base per R21/R35; **Dawn Banner read-time aura vs Living Bridge permanent** — base vs power tracked separately) + **PROOF 6** (surge dump). **Smoke sim:** 500 games (10×50) `wave1:true` — **500/500 terminated, 262 exercised, 0 crashes, 0 bad-units-array games**. No new exports. **⚠ SIM FLAGS carried:** Dawn Banner (compounding) + Living Bridge (R40 body-count) both remain sim-priced. Scope: exactly these five.
  - **✅ ENGINE TASK 13 (wave-1 batch 13 — the venom tier: bounce + mill, 5 cards, all Naga) — DONE.** Authority: `docs/WAVE1_ROSTER_v0.2.md` (the doc's Naga section is authoritative — my task-given names were close but the doc governs). **THE FIVE CARDS (verbatim, per doc):** Uraga Colossus (E P7, "ON PLAY: enters with 2 Venom on itself; sheds 1/round"), Mahapadma (R P5, "enemy Units with Venom cannot receive Dharma Shield"), World-Coil Constrictor (E astra, "bind an enemy until it loses a Venom token"), Rite of Shed Skin (U mantra, "return a friendly Unit to hand; re-enters at printed power"), The Drowned Altar (E artifact, "ROUND END: mill top card; your Units +1 this round if a Unit"). **⚠ NAME NOTE:** the task said "Uraga the Molting" → doc's **Uraga Colossus**; all five ARE in the doc's Naga wave (Serpent's Kiss, Kalakuta Vial, Silt Strangler, etc. are OTHER Naga cards, not this batch).
    - **STEP 0 — THE THREE MAPS:** **(a) VENOM PIPELINE choke points:** `drainAmount` (1 +Patala-round +Vasuki-R3 +Venom-Strike) → `venomLoss(g,pi,u,amt)` (the single power-drain point; Holika +1, Signet floor, Hiranya floor — does NOT touch `u.venom`) → `venomPassive` (Naga drains ALL opp Units) + `venomTokens` (token-bearers drain `venom*dbl`; **tokens are STICKY — never decremented on drain**, R13/R14) → `venomRoundEnd` (passive+tokens+sweep). **(b) BOUNCE:** the v0.2 ruling = **fresh `mkCard(CARD_BY_NAME[u.n])`** → new uid, printed base, `venom=0`, all flags/buffs cleared. **What is deliberately LOST (ruled, demonstrated): R21 permanent changes BOTH ways** — a Dawn-Sentinel GROWTH (base 5→ printed 2) AND a Surpanakha CUT (base 1→ printed 3) BOTH reset to the def's printed power. **(c) MILL:** `pl.deck.shift()` → `pl.discard.push` — a milled Unit lands in discard and is a **legal Gayatri/Shukracharya/Dawn's-Rebirth target** (proven: milled Dawn Sentinel → Dawn's Rebirth revives it); empty deck → `shift()` undefined → no-op, no crash.
    - **IMPLEMENTATION:** *Uraga* = on-play `c.venom=2` (R13 drains it −2 at round end, before scoring → a P7 that scores 5 the round played — the sanctioned self-price); "sheds 1/round" = `venomRoundEnd` decrements each Uraga's venom by 1 after `venomTokens` (mostly moot since Units clear at round end + revivals cleanse — noted). *Mahapadma* = both shield-designation paths (`designateShield` refuse + `designateShields` candidate-exclude) skip an enemy Unit with `venom>0` when the opponent has a Mahapadma; a clean Unit still shields. *World-Coil* = `resolveAstra` bind (`bound=true` + `worldCoilBound=true` marker, Nagapasha targeting), **released in `venomLoss`** when the bound Unit is drained WITH venom present (`u.bound && u.worldCoilBound && u.venom>0`) — "loses a Venom token" = suffers a venom drain while venomed (logged reading; the token count itself is sticky). *Rite of Shed Skin* = `castMantra` bounce → fresh `mkCard`; AI bounces the Unit furthest BELOW printed (reset recovers most). *Drowned Altar* = `roundEndCardEffects` (gate + processing), placed **AFTER The Living Bridge** (deterministic hook order, R42): mill top → discard; if a Unit, all your Units +1 current-power this round.
    - **⚠ TOKEN-BOUNCE RESOLUTION (logged):** a Rakta token has **no deck def** (`CARD_BY_NAME['Rakta']` undefined) — `mkCard` can't reconstruct it. So bounce is **gated to `!u.token && CARD_BY_NAME[u.n]`** — tokens (and ghosts) are excluded from Rite of Shed Skin (legality + dispatch); a board of only a token makes Rite illegal (proven). **⚠ ANTI-LOOP LOCK (added in Task 14, retroactive to this card):** the bounced-fresh card now carries `lockedRound=g.round` so it **re-enters NEXT round, not this one** — this closes a **Brihaspati-copies-a-bounce-mantra self-bounce infinite loop** (Brihaspati re-casts `g.lastMantra`; if that is Rite of Shed Skin it bounced Brihaspati itself → AI replays Brihaspati → copies Rite → loops forever; the log mislabeled it "repeats Pavamana", the batch-8 cosmetic Brihaspati log bug). Verified the batch-13 both-ways proofs (24) still pass after the lock (they assert fresh base/venom/uid, not lockedRound).
    - **PARITY (flag-off):** all five ids `wave:1`-gated; the `venomLoss` World-Coil release is `worldCoilBound`-gated (a field only the astra sets → undefined/falsy flag-off → the hot venom path is byte-identical), the `venomRoundEnd` Uraga shed is `id==='uraga'`-gated, Mahapadma's shield-checks are `id==='mahapadma'`-gated, the Altar is `artifact.id`-gated in the already-fast-out round-end hook. Proven: full suite green (invariants · scenario 50 · **venom 38 — the critical guard, since venomLoss/venomRoundEnd were touched** · story 48 · **0 console.log**), all 10 matchups byte-identical — the **four Naga-involving matchups** (Naga mirror **51.4**, Deva-vs-Naga 40.9, Asura-vs-Naga 49.0, Vanara-vs-Naga 57.3) confirm the venom-pipeline edits did not disturb launch venom; Deva-vs-Naga **40.9/59.1**. **ASTRA_DMG UNCHANGED — the NO-CHANGE CHECK (stated): none of the five is a deal-N astra** (World-Coil = bind, not damage, R22), so the set stays `{Agneyastra, Lanka Dahan, Pashupatastra, Vidyutastra}` (Vidyutastra from batch 12). **PER-CARD BOTH-WAYS (24 checks):** Uraga 2-venom/−2-drain/shed; Mahapadma refuse-venomed/shield-clean/auto-exclude; World-Coil bind/no-venom-no-release/**Long-Patience-releases**; Rite fresh-printed-base/grown-reset/cut-reset/clean/token-gated; Altar mill-Unit-+1/mill-nonUnit-no-buff/empty-deck. **PROOF 4:** milled Dawn Sentinel → discard → Dawn's Rebirth revives (mill→revive composes); token-bounce gated. **PROOF 6:** Patala + Altar + World-Coil-bound Unit dumped — Patala deepens the drain (−2 R1), the drain releases the bind, the Altar mills a Unit and buffs +1. **Smoke sim:** 500 games (10×50) `wave1:true` — **500/500 terminated, 188 exercised, 0 crashes, 0 bad-units-array games** (bounce+mill+venom in one sim). No new exports. **⚠ SIM FLAGS (roster):** Mahapadma is the Deva–Naga 41/59 counter (SIM PRIORITY); Drowned Altar self-deck-out in Gandharva. Scope: exactly these five.
  - **✅ ENGINE TASK 14 (wave-1 batch 14 — the astra/utility tier, 5 cards) — DONE.** Authority: `docs/WAVE1_ROSTER_v0.2.md`; rulings `docs/GDD_DELTAS.md` R21–R45 (**R45** = the read-only World-Coil/Nagapasha bind preamble, findings below). **THE FIVE CARDS (verbatim):** Suryastra (Deva **L astra RATNA**, "Deal 2 to ALL enemy Units", **`dmgAstra:true`**), Vayavyastra (Vanara R astra, "Return an enemy Unit ≤4 power to its owner's hand"), Jatayu's Last Flight (Vanara E astra, "Destroy an enemy Unit with power >6"), Vinata's Talon (Vanara R P4 unit, "ON PLAY: deal 1 per two friendly Vanaras, max 3"), Sampati (Vanara R P5 unit, "ON PLAY: reveal the enemy's highest-power hand card").
    - **R45 PREAMBLE (read-only, reported — World-Coil vs Nagapasha bind):** **(a) SHARED flag, not distinct** — both set the SAME `t.bound=true`; World-Coil additionally stamps a `t.worldCoilBound=true` marker so ONLY its bind self-releases on a venom drain (`venomLoss`). Nagapasha has no such marker → it is released only by its own turn-costing unbind action. **(b) The manual free-action unbind is FLAG-AGNOSTIC** — it clears `t.bound` for whatever bound the unit, so **it DOES release a World-Coil bind** (a World-Coil victim can be freed by the normal unbind, same as Nagapasha). **(c) A World-Coil-bound unit CLEANSED by Pavamana:** Pavamana strips `venom` but does NOT clear `bound`/`worldCoilBound` → the unit stays bound with 0 venom, so its **`venomLoss` self-release path is now DEAD** (no venom left to drain) — but it is NOT permanently stuck: the manual unbind (per (b)) still frees it. Logged as a live but non-fatal edge (a cleansed World-Coil victim needs the manual unbind, not a drain). **No code changed in the preamble — report only.**
    - **STEP 0 — THE ASTRA SURFACES:** Suryastra rides the **Lanka Dahan AoE pattern** — `for (const u of [...opp.units]) if(!u.ghost) damageUnit(g,1-pi,u,2,'Suryastra')` — iterating `opp.units` directly, so it **NEVER consults `targetSpec`/`astraProtected`** → structurally **pierces Maya Veil, Dharma Shield, and Andhaka's untargetability** (all target-layer guards), per-unit cause `'Suryastra'` routing Patala/Holika/Ratri at the `damageUnit` choke. Vayavyastra/Jatayu are **single-target** (`targetSpec` + `playableIndices` legality, respect `astraProtected`); Vinata's Talon is a **unit on-play** (bypasses the astra-targeting layer entirely).
    - **⚠ THE LOUD ASTRA_DMG CHANGE (the FIFTH member — Task-2 ceremony, EARNED):** Suryastra is `dmgAstra:true`, so `ASTRA_DMG` grows to **5**. The permanent test.js invariant **FAILED LOUDLY first** (`✖ 1 INVARIANT FAILURES` — derived `{Agneyastra, Lanka Dahan, Pashupatastra, Suryastra, Vidyutastra}` vs the 4-member expected), then `EXPECTED_ASTRA_DMG` was updated to the five and it **passes**. Both states demonstrated. ⚠ **SIM PRIORITY — Suryastra-in-Patala kill-math:** in Patala the per-unit hit is **2+1 = 3** to EVERY enemy Unit (AoE ×board-wide), so a Patala Suryastra clears any enemy board ≤3 power wholesale — the single strongest damage line in the wave; flagged for balance sim before it leaves wave-1 gating.
    - **IMPLEMENTATION (the four utility cards):** *Vayavyastra* = `resolveAstra` bounce (batch-13 v0.2 ruling): fresh `mkCard(CARD_BY_NAME[t.n])` at printed base, venom/flags cleared, token/ghost-gated — **lands in the OWNER's (enemy's) hand** (`opp.hand.push`, no hand limit) and carries the **`lockedRound=g.round` anti-loop lock** (re-enters next round). ≤4 = **current effPower** (R32 both ways — a drained 5 is legal, a buffed 3 escapes); respects `astraProtected`; gated illegal with no legal target. *Jatayu's Last Flight* = `resolveAstra` **destroy-class** (`destroyUnit`, NOT dmgAstra per R22 → immunity-agnostic, kills Holika); >6 = **strict current effPower** (the inverse of Shakti Spear's ≤4); respects `astraProtected`; gated. *Vinata's Talon* = on-play **N hits of 1** via `damageUnit(…,1,"Vinata's Talon")`, N = `min(3, floor(non-ghost-friendly-count/2))` — **SELF-INCLUSIVE** (Talon is on the board at on-play, so it counts toward its own Vanara tally; logged), **single-target** (all N hits land on the strongest enemy, so **Holika sharpens each: 2 hits on Holika = −4**, the batch-4 loop-closure). *Sampati* = on-play **minimal honest reveal** — logs the enemy's highest-**base** hand card by name; empty enemy hand → logged no-op (no card is moved/discarded — pure information).
    - **⚠ LOGGED READINGS:** Vinata self-inclusive + N-hits-single-target (Holika-sharpen-per-hit) + no-`astraProtected` filter (unit on-play damage is not an astra → **hits Andhaka**, proven); Sampati = log-only reveal (no engine-visible steal/discard); Vayavyastra bounce **lands in enemy hand** and **erases a Surpanakha permanent cut** (returns fresh at printed — the bounce ruling's dark side, weaponizable against your OWN debuff); the anti-loop `lockedRound` lock (see the batch-13 Rite note — same fix, both bounce sites).
    - **PARITY (flag-off):** all five ids `wave:1`-gated; every branch (`resolveAstra`/`targetSpec`/`playableIndices`/on-play/AI-scoring) is id-gated on ids no launch card has. Proven: full suite green (invariants · scenario 50 · venom 38 · story 48 · **0 console.log**), all 10 matchups byte-identical (Deva-vs-Naga **40.9/59.1**, Naga mirror **51.4**, Vanara mirror **52.8**). **THE `dmgAstra` SUBTLETY (as Agneyastra/Vidyutastra):** the derivation runs over ALL defs, so `ASTRA_DMG` grows to 5 flag-off too — harmless (Suryastra can't enter play flag-off; the Patala column is byte-identical, baseline unchanged). **PER-CARD BOTH-WAYS (26 checks):** Suryastra AoE-all / Patala-3-each / Ratri-prevents-all / Holika-takes-0 / Maya-Veil-pierces / shield-pierces; Vayavyastra ≤4-both-ways / astraProtected-respected / gated / fresh-printed-into-enemy-hand / erases-Surpanakha-cut; Jatayu >6-strict-both-ways / destroy-class-kills-Holika / astraProtected / gated; Vinata self-inclusive-count / max-3-cap / single-target / Holika-2-hits-−4 / hits-Andhaka; Sampati highest-base-reveal / empty-hand-no-op. **PROOF 4 (cross-batch):** Suryastra vs a Mahapadma board (AoE ignores shield-denial mechanics, hits all); Vayavyastra bouncing a Surpanakha-cut unit undoes the cut; Vinata's Talon hits Andhaka (astra-untargetability does NOT block a unit's on-play damage). **PROOF 6 (AoE-integrity dump):** Suryastra into a shielded + Holika + venomed + Maya-Veiled board — shield/Maya-Veil pierced (both damaged), Holika took 0 (immune), venomed unit took the 2 on top of its venom, all in one AoE pass. **⚠ ANTI-LOOP FIX (this task, general):** the smoke sim initially HUNG 2/500 (Brihaspati self-bouncing a copied bounce mantra); fixed by the `lockedRound=g.round` lock at BOTH bounce sites (Vayavyastra here + Rite of Shed Skin retroactively) → **500/500 terminated**. **Smoke sim:** 500 games (10×50) `wave1:true` — **500/500 terminated, 240 exercised, 0 crashes, 0 bad-units-array games**. `EXPECTED_ASTRA_DMG` updated to 5 (the only test.js change). No new exports. **⚠ SIM FLAGS carried:** Suryastra-in-Patala board-clear (new, SIM PRIORITY). Scope: exactly these five.
  - **✅ ENGINE TASK 15 (wave-1 batch 15 — the leap-utility tier, 5 cards) — DONE. THE VANARA WAVE IS NOW FULLY CLOSED.** Authority: `docs/WAVE1_ROSTER_v0.2.md` (doc wins); rulings `docs/GDD_DELTAS.md` R21–R44 (R27 row canon, R40 formation, R34 round-scope, R22 damage-boundary). **THE FIVE:** Song of the Crossing (Vanara U mantra), Matanga's Blessing (Vanara R mantra), Gandhamadana (Vanara E P5 unit), Anjaneya's Roar (Vanara L astra, **`dmgAstra:false` EXPLICIT**), Saranyu Cloud Mare (Deva E P5 unit **RATNA**).
    - **STEP 0 — THE LEAP-LEGALITY SURFACE (reported; this batch's parity risk):** adjacency is enforced at **leap-target SELECTION**, NOT in `doLeap`. `doLeap(g,pi,leaper,target,free)` just copies power + fires Crown/onLeap — it never checks adjacency. The AI's `bestLeap(g,pi)` restricts targets to `adjacentUnits(pl,u)` (THE enforcement point); `canLeap` wraps `bestLeap`. The human UI leap-picker enforces adjacency separately (UI-layer). So Gandhamadana's "target from anywhere" is a **`bestLeap` widening** (+ the UI picker is DEFERRED/logged) — and because Gandhamadana is `wave:1` (never in the pool flag-off), the widening's `gandh` list is empty flag-off → `targets = adjacentUnits` exactly → **launch leaps byte-identical** (proven: the ENTIRE `test.js` output diffs empty pre/post batch-15; the four Vanara matchups at baseline — mirror **52.8**, Deva-vs-Vanara 45.1, Asura-vs-Vanara 42.3, Vanara-vs-Naga 57.3).
    - **IMPLEMENTATION:** *Song of the Crossing* = `castMantra` branch — **R27-RESOLVED (logged):** there is exactly ONE row per player (the ordered `units` array), so the GDD "Units in one row" simplifies to **ALL your non-ghost Units +1 (+2 if you have 4+)**, current-power (R34); gated in `playableIndices` (needs a friendly Unit). *Matanga's Blessing* = `castMantra` sets a round-scoped `pl.matangaArmed` flag consumed by the **next `doLeap`** (both leaper AND target +2, **STACKS with Crown's +1/+1**; placed after the Crown block, before `leapsUsed++`/`onLeap` so Kumuda/Rambha fire on the same leap); unconsumed → reset at round end; flag set only by this card → `doLeap` consume is a no-op flag-off. *Gandhamadana* = `bestLeap` widening (targets = `adjacentUnits(u)` ∪ any friendly Gandhamadana, deduped by a `seen` set); leaping FROM him is UNCHANGED (uses his own `adjacentUnits`) per the printed text. *Anjaneya's Roar* = `resolveAstra` branch: AoE −1 to all enemies via `damageUnit(cause "Anjaneya's Roar")` (iterates `opp.units` directly → pierces shield/Maya Veil) + friendly pairs +1; gated legal only if either effect would land. *Saranyu* = on-play unit case: two OTHER friendly Units swap `u.power` (highest↔lowest); needs 2+ others.
    - **LOGGED READINGS:** **Song R27-resolution** — "one row" = the whole array = your Units (the R21+ "may simplify" note settled by R27). **Anjaneya's Roar "adjacent pairs" — ⚠ SUPERSEDED BY R49(a) (owner-ruled 2026-07-13): now INTERIOR-ONLY (a Unit flanked by non-ghost friendlies on BOTH sides gets +1, the R40 Mason pattern); def txt → "All enemy Units −1 this round; your Units flanked on both sides gain +1."** The original shipped reading below (each friendly Unit with **≥1 non-ghost neighbour gets +1 ONCE**, R40 non-ghost-neighbour precedent) was the weakest-pending reading; R49(a) sharpened it. The −1 is a `dmgAstra:false` debuff so **Holika sharpens it to −2 (R22)**, **Patala does NOT amplify** (not in `ASTRA_DMG`), **Ratri does NOT prevent** (not dmgAstra) — all proven. SIM flag (Roar vs Rama Naam's crown) stands. **Saranyu "exchange current power"** = swap `u.power` only; **bases UNTOUCHED** (an R21-grown base does not move) and **Venom Tokens stay with the Unit** (only power trades) — proven; board-total-neutral (the "positional cut", invariant preserved). **Saranyu TWO-TARGET status:** AI/auto pairing = highest↔lowest-current-power other Units (board-neutral, Gavaksha precedent); the **human two-target picker is UI-DEFERRED** (`targetUid` carries ONE target; a two-target flow is not built — logged). Gandhamadana's **human leap-picker widening is UI-DEFERRED** (engine `bestLeap`/sim live).
    - **PARITY (flag-off):** all five ids `wave:1`-gated; `matangaArmed` is a new field only Matanga sets (doLeap consume inert); the `bestLeap` widening's `gandh` list is empty; Song/Anjaneya/Saranyu branches are id-gated. Proven: full suite green (invariants · scenario 50 · venom 38 · story 48 · **0 console.log**), **the ENTIRE test.js output is diff-identical pre/post batch-15** (all 10 matchups + counter lines), Deva-vs-Naga **40.9/59.1**. **ASTRA_DMG UNCHANGED — the NO-CHANGE CHECK (stated): an astra (Anjaneya's Roar) IS added this batch but is `dmgAstra:false` (doc pre-ruled), so the derived set stays exactly `{Agneyastra, Lanka Dahan, Pashupatastra, Suryastra, Vidyutastra}` and the invariant passed with Anjaneya's Roar present-and-untagged.** **PER-CARD BOTH-WAYS (29 checks):** Song 3→+1 / **exactly-4→+2 (threshold)** / 5→+2 / current-power / legality gate; Matanga arm→consume→one-shot→second-leap-no-buff→round-end-expiry; Gandhamadana target-anywhere / from-needs-adjacency / no-Gandhamadana-adjacent-only; Roar all-enemies−1 / pair+1 / lone-no-buff / **Holika−2** / **Patala-no-amplify**; Saranyu swap / **bases-untouched** / **venom-stays** / board-neutral / <2-others-gated. **PROOF 6 — the full one-leap stack (Blessing+Crown+Kumuda+Rambha) dumped:** Kumuda leaper copy6 +Crown1 +Matanga2 =9 then Kumuda-perm +1 → **power 10 / base 4**; target +Crown1+Matanga2 → **9**; Rambha any-friendly-leap +1 → **power 6 / base 6**; Matanga consumed. **Smoke sim:** 500 games (10×50) `wave1:true` — **500/500 terminated, 222 exercised, 0 crashes, 0 bad-units-array games** (leaps + widening + swaps + AoE in one sim). No new exports. Scope: exactly these five — **the Vanara wave is COMPLETE**.
  - **✅ RULINGS R45–R50 APPEND + R49(a) DELTA (docs + one flag-gated engine change) — DONE (2026-07-13).** Appended R45–R50 verbatim to `docs/GDD_DELTAS.md` (batch-5 subsection) from the authority `docs/rulings_R45-R50.md`; five as-shipped ratifications cross-checked against the engine (R45 shared `t.bound`/flag-agnostic unbind, R46 `lockedRound` at both bounce sites, R47 Talon ignores `astraProtected`, R48 Song all-units, R50 Talon/Sampati) — all matched. **R49(a) engine delta:** Anjaneya's Roar friendly buff sharpened from "≥1 neighbour" → **INTERIOR-only** (`flankedBothSides` helper, R40 Mason pattern); def txt → "All enemy Units −1 this round; your Units flanked on both sides gain +1." Roster annotations appended (Song→R48, Roar→R49(a)). Flag-off byte-identical (Roar wave-gated; helper only called from wave-gated sites), ASTRA_DMG unchanged, 11 both-ways checks (3-unit→middle only, 2-unit→none, ghost-flank→not-interior, 5-unit→3 interior, enemy −1 with Holika −2 / Patala no-amplify).
  - **✅ ENGINE TASK 16 (wave-1 batch 16 — the artifact/counter tier, 4 cards) — DONE. THE FIRST HERO OF THE WAVE (Vritra) scouts the hero surface.** Authority: `docs/WAVE1_ROSTER_v0.2.md` (doc text wins on all four); rulings `docs/GDD_DELTAS.md` R21–R50 (R24+R37+erratum pre-rule Brahmadanda; R42 hook order governs the Crucible slot; R45 bind-unbind; R47 targeting-layer). **THE FOUR:** Kalpavriksha (Deva RATNA, Mythic artifact), The Iron Crucible (Asura RATNA, Mythic artifact), Brahmadanda (Asura RATNA, Epic astra), Vritra (Asura Legendary hero, P8).
    - **STEP 0(a) — THE HERO SURFACE:** `mkPlayer` does NOT specially select heroes — the deck is the whole faction pool (`src = wave1 ? rawSrc : rawSrc.filter(c=>!c.wave)`), shuffled, drawn 10; heroes are ordinary cards, `max 1 hero/round` enforced by `heroPlayedThisRound` in `playableIndices`. Vritra joins as a `wave:1` 4th Asura hero → **flag-off filtered out BEFORE map+shuffle → launch pool/UID/rng-shuffle byte-identical** (the established wave-flag parity). Hero rules Vritra inherits: astra-immunity by default (except Sudarshana/Takshaka), persists between rounds, positionless (own `heroes` row), `totalPower` counts non-bound heroes, Swarga +1.
    - **STEP 0(b) — LOST-POWER TRACKING (Iron Crucible):** per-round **`pl.lostPowerUids` Set** (reset each round), populated (g.wave1-gated → hot paths pristine flag-off) at every power-DECREASE site: `damageUnit`, `venomLoss` (both branches), Surpanakha's permanent cut, Pisacha/Mahishasura round-end decay, and the R33 warded-survivor drop-to-1. **Reading (broad, logged):** ANY decrease this round qualifies regardless of net. **"Regain 1" = +1 CURRENT power, UNCAPPED** (NOT capped at base) — required so a permanent-cut/decay unit whose power==base still regains (the anti-decay intent; a capped restore would leave Pisacha's decay uncounteracted). **R42 slot: adjacent to Sushena** (a restore), AFTER Pisacha/Mahishasura decay (same hook, earlier) so decayed units are seen; logged.
    - **STEP 0(c) — BRAHMADANDA (R24+R37+erratum):** its OWN negation source (round-stamp `pl.brahmadandaArmed`), **distinct from Manasa's `surgeNegated`** — sets `effectNegated=true` ONLY (does NOT set `surgeNegated`), so the enemy caster's **Chaos Surge still procs** (R37 erratum) and **Blueprint's extra action still fires** (R37, placed after the negation branch, ungated). Consumed by one enemy astra; expires unused at round end. R24 sim fallback ("deals no damage" vs full negate) PREPARED not implemented.
    - **IMPLEMENTATION:** *Vritra* on-play (hero dispatch): bind the strongest enemy Unit (`t.bound=true` + distinct `t.vritraBound` marker — NOT `worldCoilBound`, so venom-drain does NOT free it); **R47: a hero on-play is NOT an astra → IGNORES `astraProtected`** (binds shielded/Andhaka/Maya-Veil units). *Brahmadanda* `resolveAstra` case arms; negation wired in the astra branch. *Iron Crucible* + *Kalpavriksha* in `roundEndCardEffects` (fast-out gate extended). Order: Savitur → decay → Kamadhenu → Sushena → **Iron Crucible** → Living Bridge → Drowned Altar → **Kalpavriksha** (last, equalises to the settled highest via `effPower`, so Dawn Banner's read-time +1 folds in automatically).
    - **⚠ VRITRA BIND FINDING (reported):** NOTHING destroys a hero in the current pools — only **Sudarshana temporarily removes** one (→ the Vritra bind lifts, wired at the removal); the bound enemy UNIT clears at round end regardless (units don't persist), so the bind is **round-bound in practice**. The **R20 manual unbind frees a Vritra bind** (R45 flag-agnostic `u.bound=false`) — demonstrated. **⚠ PROOF-6 FINDING:** Chandrahas and Blueprint are BOTH artifacts (1 slot) → "Vidyutastra under Chandrahas WITH Blueprint" is impossible; dumped as two separate composable halves.
    - **PARITY (flag-off):** all four ids `wave:1`-gated (filtered out); the `lostPowerUids` writes are `g.wave1`-gated (choke points pristine); `brahmadandaArmed`/`vritraBound` are new fields only wave code reads; the round-end/on-play/scoring branches are id-gated. Proven: full suite green (invariants · scenario 50 · venom 38 · story 48 · **0 console.log**), **the ENTIRE test.js output is diff-identical pre/post batch-16** (hero-pool composition inert) — the **four Asura-involving matchups** (Asura mirror **53.6**, Deva-vs-Asura 47.7, Asura-vs-Vanara 42.3, Asura-vs-Naga 49.0) and Deva-vs-Naga **40.9/59.1** all at baseline. **ASTRA_DMG UNCHANGED — Brahmadanda is negation, NOT damage (no `dmgAstra` tag, doc-ruled) — set stays `{Agneyastra, Lanka Dahan, Pashupatastra, Suryastra, Vidyutastra}`.** **PER-CARD BOTH-WAYS (29 checks):** Vritra bind-strongest/0-contribution/R47-ignores-protection/R20-unbind-frees/Sudarshana-lifts; Brahmadanda arm/Vajra-negated/Pashupatastra-AoE-negated/**Surge-still-procs**/**Blueprint-still-acts**/consume-one/round-expiry; Crucible Dhumraksha-price/**Pisacha-uncapped**/venom-drain/clean-no-regain; Kalpavriksha lowest=highest/lone-no-op/**+Dawn-Banner composition**. **PROOF 4:** Blood Oath DEAD-victim-no-regain vs **warded-survivor-regains-to-2**; **Brahmadanda vs Vidyutastra+Chandrahas = negated effect + 4 surges (+12) fire** (R44 count on a negated cast); Vritra-bound Unit's power still Saranyu-swappable (bind orthogonal, still 0-contribution). **PROOF 6:** two negation-integrity dumps (Chandrahas 4-surge / Blueprint extra-turn). **Smoke:** 500 (10×50) + **200 Asura-mirror** `wave1:true` — 500/500 + 200/200 terminated, 211 + 187 exercised, 0 crashes, 0 bad-array. **⚠ SIM FLAGS carried:** Kalpavriksha+Dawn Banner wide board; Iron Crucible anti-Naga drift; Brahmadanda mirror warp. No new exports. Scope: exactly these four.
  - **✅ ENGINE TASK 17 (wave-1 batch 17 — heroes part 1, 4 cards) — DONE. Vritra scouted the hero surface (batch 16); these four walk it.** Authority: `docs/WAVE1_ROSTER_v0.2.md` (doc text wins); rulings `docs/GDD_DELTAS.md` R21–R50 (R23 governs Kartikeya's resolution-trigger class, R37+erratum its cast-trigger contrast; R42 hook order governs Padmavati). **THE FOUR (all Legendary heroes):** Kartikeya (Deva P8), Makardhwaja (Vanara P7), Anjana (Vanara P6), Padmavati (Naga P7).
    - **STEP 0(a) — HERO PERSISTENCE × ROUND-END (Padmavati):** heroes SURVIVE the board clear (units don't), so **Padmavati's round-end Venom fires EVERY round she is on board — a cross-round venom engine unlike any unit's round-end effect** (a unit's round-end effect happens once then the unit clears; she stays and recurs, accumulating sticky tokens on the enemy over rounds 1→2→3). **R42 slot (logged): LAST** — after the entire per-player round-end loop settles (after Kalpavriksha), so she reads the enemy's SETTLED strongest; it's a token APPLICATION (not damage), riding the real pipeline, draining at the NEXT tick. **⚠ RE-SLOTTED BY R54 (owner-ruled 2026-07-13):** applying AFTER `venomRoundEnd` meant the token landed after the round's only drain, onto units that then clear/cleanse — applied into the void. R54 amends R42 with a **PRE-DRAIN TOKEN APPLICATIONS** slot: Padmavati moved to a new `preDrainTokens(g)` called immediately BEFORE `venomRoundEnd` in `endRound`, so her Venom now **ticks THAT SAME round end** (Patala-Throne-deepened / sarpaDouble-doubled per the normal pipeline). See the batch-6 rulings line below.
    - **STEP 0(b) — THE HERO-LEAP QUESTION (Makardhwaja):** `doLeap` operates on `pl.units`; heroes live OUTSIDE it → Makardhwaja is implemented as a **DIRECT copy** (`c.power = effPower(source)`, the §9 rule) NOT routed through `doLeap`. Source = Hanuman if on board, else the strongest friendly UNIT, else printed 7 (no-op). **LISTENER READINGS (weakest, logged):** because it is NOT a formal Leap action — it does **NOT fire `onLeap`** (Kumuda/Rambha read "friendly UNIT Leaps"; he is a hero), the source is **NOT "Leapt to"** (gets nothing), it does **NOT consume the leap limit** (FREE, Mainda precedent), does **NOT consume Matanga's arm**, and takes **NO Crown +1**. Pure effPower copy (folds in read-time auras like Dawn Banner, R43).
    - **STEP 0(c) — LEAP-LIMIT COMPOSITION (Anjana):** `leapLimit` rewritten ADDITIVE (logged): **base 1 + Crown +1 + Anjana +1 → up to 3**. Read-time (Anjana leaves → reverts same round, like Crown). Flag-off (no Anjana) = `crown?2:1`, byte-identical.
    - **KARTIKEYA DETECTION READING (R23, weakest-defensible, logged):** an enemy Astra "RESOLVES vs your side" is detected **STATE-based, NOT via the observational event stream** (rule #7 — the stream must stay non-causal): snapshot the Kartikeya-owner's (opp = 1-pi) Units BEFORE `resolveAstra`; **"resolved vs your side" = ≥1 of those Units was REMOVED (destroyed/bounced) OR lost power OR gained venom OR became bound** by the astra → ALL their surviving Units +1 PERMANENT (base+power), **ONCE per Astra** (not per hit). A **negated** Astra skips `resolveAstra` → no state change → **no trigger** (R23 negated≠resolved); a **whiffing** Astra (Maya-Veiled / no targets) → no change → no trigger; **your OWN** Astra hits the opponent not you → never triggers your Kartikeya. Detection is HERO-owner-gated (`hasKart`) → zero overhead flag-off / when no Kartikeya on board. Reads UNIT changes only (a hero-only hit — e.g. Sudarshana — does not trigger it). SIM flag (vs the Asura wave) stands.
    - **PARITY (flag-off):** all four ids `wave:1`-gated (filtered out of the pool); Kartikeya's snapshot/check is `hasKart`-gated (no Kartikeya → skipped); Anjana's `leapLimit` term is `anjana`-presence-gated (flag-off = `crown?2:1`); Padmavati's fast-out entry + pass are `padmavati`-gated; Makardhwaja is id-gated. Proven: full suite green (invariants · scenario 50 · venom 38 · story 48 · **0 console.log**), **the ENTIRE test.js output is diff-identical pre/post batch-17** (the hero-pool composition across THREE factions is the parity risk — inert), all mirrors at baseline (Deva **49.8**, Asura **53.6**, Vanara **52.8**, Naga **51.4**) and Deva-vs-Naga **40.9/59.1**. **ASTRA_DMG UNCHANGED — no astras in this batch** (all four are heroes) — set stays `{Agneyastra, Lanka Dahan, Pashupatastra, Suryastra, Vidyutastra}`. **PER-CARD BOTH-WAYS (34 checks):** Anjana base/Crown/Anjana/both/reverts; Makardhwaja Hanuman-first / strongest-unit / no-source-7 / **no-onLeap** / **no-leapsUsed** / **no-Matanga** / Dawn-Banner-copy; **KARTIKEYA MATRIX** — triggers on Agneyastra / Suryastra-AoE / Mohanastra-debuff / Nagastra-venom / Vajra-destroy, does NOT on whiff / own-Astra / negated, **once-per-Astra** (3-hit AoE → each +1 once); Padmavati strongest / tie-first-found / no-enemy-no-op / **recurs-2-tokens**. **PROOF 4 (cross-batch):** Padmavati+Mahapadma (venomed enemy can't be shielded); **Padmavati+Vishalakshi** (her venom → venom-death → feeds Vishalakshi +2, the Naga engine assembling); Makardhwaja copies a Dawn-Banner-buffed Unit (R43); **Anjana+Crown+Matanga triple-leap** (limit 3, Matanga consumed by leap #1 only, all leaps counted). **PROOF 6 — Kartikeya class-contrast dump:** a Brahmadanda-negated enemy Astra → **NO Kartikeya** (R23) but the enemy **Blueprint's extra action STILL fires** (R37) in one printed sequence. **Smoke:** 500 (10×50) + **200 Deva-mirror + 200 Vanara-mirror** (hero-density collision zones) `wave1:true` — 500/500 + 200/200 + 200/200 terminated, 318 + 177 + 147 exercised, 0 crashes, 0 bad-array. **⚠ SIM FLAG carried:** Kartikeya vs the Asura wave. No new exports. Scope: exactly these four.
  - **✅ RULINGS R51–R54 APPEND + R54 PADMAVATI RE-SLOT (docs + one flag-gated engine change) — DONE (2026-07-13).** Appended R51–R54 verbatim to `docs/GDD_DELTAS.md` (batch-6 subsection) from the authority `docs/rulings_R51-R54.md`; three as-shipped ratifications cross-checked against the engine (R51 Vritra shared `t.bound`/R47/Sudarshana-lift/R20-unbind, R52 Crucible UNCAPPED regain, R53 `lostPowerUids` at every per-unit decrease site) — all matched. **R54 engine delta (amends R42):** Padmavati's round-end Venom **moved OUT of `roundEndCardEffects` (the R42 hook, which fired AFTER `venomRoundEnd` → token applied into the void) into a new `preDrainTokens(g)` called immediately BEFORE `venomRoundEnd` in `endRound`** — so her Venom now **ticks THE SAME round end** (a text-preserving re-slot; the card text is unchanged, only WHEN it fires). The new "PRE-DRAIN TOKEN APPLICATIONS" slot is canon for future round-end token appliers. **⚠ LESSON:** the batch-17 both-ways proof drove `roundEndCardEffects` in isolation (driven-moment blindness) and missed that the full `endRound` lifecycle applied her token after the drain; the R54 proof drives **`endRound` itself**. Flag-off byte-identical (Padmavati `padmavati`-presence-gated; `preDrainTokens` no-op absent), ASTRA_DMG unchanged, Deva-vs-Naga **40.9/59.1**, all suites green, **8 lifecycle both-ways checks** (token ticks −1 same round end on the strongest / Patala-Throne deepens −2→−3 [realm 'patala' is astra-only — the ARTIFACT deepens venom] / no-enemy no-op / no-Padmavati structurally absent / **Vishalakshi fed by a venom-death across a real endRound** / **Mahapadma shield-block on her poisoned target** at the new call site). `preDrainTokens` exported for tests (benign). Smoke 500/500 + 200/200 + 200/200 wave1-on crash-free (Padmavati exercised via the real `endRound`).
  - **✅ ENGINE TASK 18 (wave-1 batch 18 — the Naga remainder, part 1, 10 cards) — DONE.** Authority: `docs/WAVE1_ROSTER_v0.2.md` (doc text wins); rulings R21–R54 (R22 damage-boundary, R31 tie-breaks, R42/R54 hook order, R46 bounce, R47 targeting-layer).
    - **STEP 0 — ENUMERATION & TRIAGE (the doc's Naga wave = 22 cards; 9 already implemented: coilsentry, longpatience, vishalakshi, uraga, mahapadma, worldcoil, shedskin, drownedaltar, padmavati; 13 unimplemented):** **(A) PRECEDENTED — implemented this batch (10):** Patala Hatchling (C P2, on-play 1 Venom to a RANDOM enemy — Nagastra precedent + g.rng) · Molting Naga (C P2, DEATH → 1 Venom to highest enemy — `onUnitDeath` listener, Vishalakshi precedent) · Venom Harvester (U P3, on-play +1 per Venom on enemy's strongest, max 3) · Shankhapala (U P4, ROUND END move 1 Venom between enemy Units — **R54 pre-drain slot**) · Depth Caller (U P3, on-play +2 if a friendly Unit in discard) · Grave-Tide Naga (R P4, on-play +1 per Unit in EITHER discard, max 4) · Kalakuta Vial (R astra, 2 Venom to one enemy — **`dmgAstra:false`**) · Hymn of the Depths (R mantra, all Venom drains trigger immediately once — Karkotaka timing precedent) · Serpent's Kiss (E astra, destroy an enemy with 2+ Venom — destroy-class, **`dmgAstra:false`**) · Silt Strangler (R P4, on-play enemy loses power = its Venom count, tokens remain). **(B) DEFERRED (3):** Nahusha (needs a NEW surface — per-side realm-effect application), Kulika (batch 19 with Mahishi), Throne of the Second King (needs a NEW surface — venom-drains-steal-to-friendly; SIM PRIORITY most-likely-broken).
    - **LOGGED READINGS (weakest-defensible):** *Patala Hatchling* random via `g.rng`, not damage. *Molting Naga* fires in `onUnitDeath` (already spliced → `foe` is the enemy), venom application. *Venom Harvester* reads the enemy STRONGEST's current venom (public), max 3. *Shankhapala* — **R54 pre-drain slot** (so the moved token drains THIS round): moves 1 Venom from the MOST-venomed enemy to the highest-effPower OTHER enemy (concentrate on the threat; needs 2+ enemy Units + a venomed source; one move per Shankhapala). *Silt Strangler* routes through **`damageUnit`** (cause 'Silt Strangler', NOT dmgAstra → **Holika sharpens +1**, Patala does NOT amplify, counts for `lostPowerUids`); R47 — a unit on-play ignores `astraProtected`; **tokens remain** (damageUnit never strips venom). *Kalakuta Vial* single-target venom → respects `astraProtected` (Mohanastra pattern); Patala does NOT amplify the APPLICATION (venom ≠ damage), Ratri doesn't stop it. *Serpent's Kiss* destroy-class (immunity-agnostic — destroys Holika), respects `astraProtected`, gated to enemies with 2+ Venom. *Hymn of the Depths* reuses `venomPassive`/`venomTokens`/`sweepDeaths` (the drain half of the pipeline) ONCE mid-round, respects Astika's pause, can cause venom-deaths; gated legal only if a drain would land. *Grave-Tide* counts non-ghost Units in BOTH discards (mantras excluded), max 4. *Depth Caller* +2 if any friendly Unit in own discard.
    - **PARITY (flag-off):** all 10 ids `wave:1`-gated (filtered out); every branch (on-play/`onUnitDeath`/`preDrainTokens`/`resolveAstra`/`castMantra`/`targetSpec`/`playableIndices`/scoring) is id-gated. Proven: full suite green (invariants · scenario 50 · venom 38 · story 48 · **0 console.log**), **the ENTIRE test.js output is diff-identical pre/post batch-18** — the **Naga-involving four** (Naga mirror **51.4**, Deva-vs-Naga 40.9, Asura-vs-Naga 49.0, Vanara-vs-Naga 57.3) all at baseline, Deva-vs-Naga **40.9/59.1**. **ASTRA_DMG UNCHANGED — the NO-CHANGE CHECK: TWO new astras (Kalakuta, Serpent's Kiss) are added but BOTH `dmgAstra:false`** (venom-application / destroy-class, not deal-N), so the derived set stays exactly `{Agneyastra, Lanka Dahan, Pashupatastra, Suryastra, Vidyutastra}` and the invariant passed with both present-and-untagged. **PER-CARD BOTH-WAYS (23 checks), venom cards driven in the FULL `endRound` LIFECYCLE (the R54 lesson):** Patala Hatchling random-1 / no-enemy; Molting Naga death→highest; Venom Harvester +2 / cap-+3; Depth Caller +2 / empty; Grave-Tide +3 / cap-+4; Silt Strangler −venom-tokens-remain / **Holika-sharpened-−3**; Kalakuta 2-venom / **Patala-no-amplify** / astraProtected-gated; Serpent's Kiss 2+-venom-gate / **destroys-Holika** / 1-venom-illegal; Hymn passive+token drains-now; **LIFECYCLE Shankhapala** (moved token drains dst THIS round) + a token drains its bearer at endRound. **PROOF 4/6 — THE NAGA MACHINE (driven dump):** Padmavati (R54 re-slot) venoms the strongest → **Mahapadma** blocks its shield → the pipeline drain hits → and (2nd game) a venom-death across a real `endRound` feeds **Vishalakshi +2** — the Naga engine end-to-end. **Smoke:** 500 (10×50) + **200 Naga-mirror** `wave1:true` — 500/500 + 200/200 terminated, 195 + **200** exercised, 0 crashes, 0 bad-array. **Exports:** `destroyUnit`/`damageUnit` added for tests (benign). Scope: exactly these 10 (A); no Kulika/Nahusha/Throne, no new surfaces.
  - **✅ RULINGS R55–R57 APPEND + ENGINE TASK 19 (wave-1 batch 19 — deferred heroes + full-roster ledger hunt) — DONE (2026-07-13).** Appended R55–R57 verbatim to `docs/GDD_DELTAS.md` (batch-7 subsection, batch-18 readings canonized) after an engine cross-check that PASSED CLEAN (R55 Shankhapala pre-drain/src=most-venom/dst=highest-eff-other/degenerate-break; R56 Silt Strangler damageUnit-routing/Holika/Patala-realm-no-amplify/Crucible/astraProtected/no-op; R57 Hymn one-pass + `destroyUnit`-line-684 `deathsThisRound++`+`onUnitDeath` + Astika-respected + round-end-drain-unconsumed) — roster annotations appended (Shankhapala→R55, Hymn→R57, Silt Strangler→R56).
    - **STEP 0 — THE LEDGER HUNT (full 88-card roster diff, reported):** per faction total/implemented/remaining — Devas **22/21/1**, Asuras **22/21/1**, Vanaras 22/22/0, Nagas **22/19/3** → **remaining = exactly 5** (the batch arithmetic held): **Garuda** (Deva H P7 — the UNACCOUNTED card), **Mahishi** (Asura H P7), **Kulika** (Naga H P8), **Nahusha, Fallen King** (Naga E P6 — RESERVED), **Throne of the Second King** (Naga M artifact — RESERVED). Factions doc-confirmed (not assumed).
    - **TRIAGE — all three implementable are (A) precedented:** **Garuda** (Pavamana venom-clause: strip all friendly Venom, +1 power per token removed FROM each cleansed Unit — the Garuda-self-buff reading was REJECTED in favour of the per-unit model, logged) · **Kulika** (venom-strip [Pavamana] + application [Nagastra] + g.rng: transfer all friendly Venom to random enemies; a transfer needs a destination → **no enemy Units = no-op, Venom STAYS**, logged) · **Mahishi** (Makardhwaja copy-power + round-end hero recurrence [Padmavati]: at round end `h.power = strongest FRIENDLY Unit's effPower`, power-only/no-abilities, stays a hero). **R42 SLOT (Mahishi):** LAST in the per-player round-end loop (after Kalpavriksha) so it reads the SETTLED strongest; reported. Nahusha/Throne untouched (reserved).
    - **HERO CEREMONY (T17):** all three `wave:1`-gated (filtered from the pool flag-off — proven: absent flag-off / present flag-on for all three); **persistence** proven (survive the board clear); **Mahishi recurs** every round (R1 copies 9 → R2 copies the new strongest 13); **mulligan** keeps the only Hero (`aiMulliganPlan` does not toss Mahishi); once-per-match is structural (a Hero enters once). AI scoring added for all three.
    - **PARITY (flag-off):** on-play/`roundEndCardEffects`/scoring branches are id-gated; **the ENTIRE test.js output is diff-identical pre/post batch-19**, Deva-vs-Naga **40.9/59.1**, all mirrors at baseline. **ASTRA_DMG UNCHANGED — no astras this batch** (all three are heroes) — set stays `{Agneyastra, Lanka Dahan, Pashupatastra, Suryastra, Vidyutastra}`. Suites green (invariants · scenario 50 · venom 38 · story 48 · **0 console.log**). **PER-CARD BOTH-WAYS + PERSISTENCE + LIFECYCLE (16 checks):** Garuda strip-all/+1-per-token/no-op; Kulika transfer-random/no-enemy-stays/no-venom-no-op; **LIFECYCLE Mahishi** (real `endRound` copies strongest / no-unit-keeps / recurs); persistence (Garuda/Kulika survive clear) + mulligan; **CROSS-BATCH LIFECYCLE** — Kulika transfers Venom → venom-death across a real `endRound` feeds **Vishalakshi +2** (the Naga machine). **Smoke:** 500 (10×50) + **200 Deva-mirror + 200 Asura-mirror + 200 Naga-mirror** `wave1:true` — all terminated, 0 crashes, 0 bad-array; **per-card exercise: Garuda 159 · Mahishi 175 · Kulika 182**. No new exports. Scope: exactly Garuda/Mahishi/Kulika; Nahusha/Throne reserved.
    - **⚠ NAMED SIM FLAGS (campaign hot list, added this task):** **(i) Kalakuta Vial → Serpent's Kiss** — a TWO-CARD UNCONDITIONAL DESTROY (Kalakuta applies 2 Venom, Serpent's Kiss destroys any 2+-Venom Unit): no power/shield/immunity check, kills anything a Naga can touch across two cards — sim whether this over-answers the go-tall factions. **(ii) Hymn of the Depths drain-acceleration** — in venom-stacked boards Hymn fires an extra full pipeline pass mid-round (the round-end drain still occurs), so a stacked Naga board can drain twice a round; sim the swing in Naga-mirror and Deva-vs-Naga.
  - **✅ RULINGS R58–R62 APPEND + ENGINE TASK 20 (wave-1 batch 20 — NAHUSHA + THRONE OF THE SECOND KING) — DONE (2026-07-13). THE WAVE IS CLOSED: 88/88 (22 defs per faction).** Appended R58–R62 verbatim to `docs/GDD_DELTAS.md` (batch-8 subsection). **⚠ STEP-0 RATIFICATION CAUGHT A DRAFTING ERROR:** the R58 v1 parenthetical "(R21 base+power)" contradicted the batch-19 Garuda engine (power-only), the Pavamana precedent, and R58's own "evaporate at the clear" rationale — I halted per the STOP-AND-REPORT discipline; the owner reconciled to R58 v2 (power-only, engine unchanged, ruling text corrected), and R62(d) was amended to POWER-ONLY steal gains (printed text lacks "permanently"). Re-ran the check clean, then implemented. Roster annotations appended for all five (Garuda→R58, Mahishi→R59, Kulika→R60, Nahusha→R61, Throne→R62). **NAME DISCIPLINE:** the launch **Patala Throne** (`id:'patala'`, venom deepener) is untouched; this batch's **Throne of the Second King** (`id:'secondthrone'`, drain-steal) is the new card — never conflated.
    - **STEP 0 (carried forward + ratified):** **(a) REALM SCATTER (R61):** 6 realm sites — effPower/swarga(410), damageUnit/patala(735), Vishwakarma/yaksha(1404), castMantra/rishi(1670), endRound/kalki(1805), endRound/gandharva(1847) — NO single choke point (each has a different beneficiary side: hero-owner / astra-caster=1-pi / artifact-defender / mantra-caster / last-card-owner / each-drawer). Confirmed: realm patala sharpens ASTRAS only. **(b) DRAIN CHOKE POINT (R62):** `venomLoss` is the SINGLE venom power-removal site (passive/tokens/Ananta/Hymn/deepened all route here); victim side (pi) and actual-removed (overkill-excluded) both knowable → one steal hook. **(c) R58/R59/R60 ratification:** R59 (friendly/last-in-hook/power-only) ✓, R60 (no-op-stays) ✓; R58 mismatch caught+reconciled (above). **(d) doc text quoted verbatim.**
    - **NAHUSHA (R61, Naga E P6 unit):** per-side `realmSuppressedRound` round-stamp + `realmActiveFor(g,side)` helper; on-play SUPPRESSES the ENEMY side's realm this round (subtraction — "yours only"); the 6 realm sites each gate on `realmActiveFor(<beneficiary>)`. Round-bound auto-revert (stamp === g.round); Mrityulok = no-op; single realm → automatic (no picker, nothing joins R50 list); mirror = realm dead both sides (R61e); R46 bounce lock is the loop guard. **Flag-off byte-identical:** stamp 0 → `realmActiveFor` always true → every site behaves as before.
    - **THRONE OF THE SECOND KING (R62, Naga M artifact):** `secondThroneSteal(g, victimPi, before, u)` hooked at BOTH `venomLoss` exit points (Signet-return + normal), `g.wave1`-gated. Steal fires when `g.players[1-victimPi].artifact.id==='secondthrone'` — this AUTOMATICALLY excludes the owner's own/Uraga self-drains (for own drains the victim's owner IS the Throne owner, so 1-victimPi is the other side, no Throne). Amount = `before − max(0, u.power)` (R62c overkill excluded); recipient = the owner's highest-effPower friendly incl. heroes, re-evaluated per event (R62d); **POWER-ONLY gains** (R62d amended; hero recipients still compound because heroes survive the clear); no friendly presence → steal LOST (R62e); uncapped (R62f).
    - **⚠ FINDING (reported):** **Second King × launch Patala Throne CANNOT co-exist** — both are Mythic ARTIFACTS (one slot), and the deepener (Patala Throne) and the stealer (Second King) must sit on the SAME side (the draining Naga) → impossible. The R62 sim-line-(i) deepened-steal scaling is therefore realized via **Vasuki** (the hero R3 deepener, which co-exists with the artifact) — proven: R3+Vasuki drainAmount 2 → the deepened 2 is stolen.
    - **PARITY (flag-off):** all realm gates are `realmActiveFor` (always-true flag-off); the steal is `g.wave1`-gated (venomLoss hot path pristine); both card ids `wave:1`-gated. Proven: **the ENTIRE test.js output is diff-identical pre/post batch-20**, Deva-vs-Naga **40.9/59.1**, Naga-involving four at baseline. **ASTRA_DMG UNCHANGED — no astras this batch.** Suites green (invariants · scenario 50 · **venom 38** [venomLoss touched] · story 48 · **0 console.log**). **BOTH-WAYS MATRIX (18 checks):** NAHUSHA through real rounds (one-sided Patala sharpen: my astra 4, enemy's 3, same round / R61b revert next round / Mrityulok no-op / R61e mirror realm-dead-both / R46 bounce-lock + re-trigger); SECOND KING through real `endRound` (basic steal / R62c overkill-excluded / R62d per-event recipient / hero-recipient PERSISTS+COMPOUNDS across the clear / R62e no-friendly-steal-lost / **R62a Uraga self-drain NOT stolen** [negative] / × Hymn mid-round window / × Vasuki-deepened steal / both-own-a-Throne mirror sanity). **Smoke:** 500 (10×50) + **400 Naga-mirror + 200 Deva-vs-Naga** `wave1:true` — all terminated, 0 crashes, 0 bad-array; **per-card exercise: Nahusha 345 · Throne of the Second King 306.** **Exports:** none new (`damageUnit`/`venomRoundEnd`/`destroyUnit`/`castMantra`/`playableIndices` already exported).
    - **⚠ NAMED SIM FLAGS (campaign hot list — R62's three, added this task):** **(i) Second King × Patala Throne / Vasuki scaling** — deepened drains steal deepened amounts (Patala Throne can't share the slot, so the vector is Vasuki R3 → steal 2 per drain; sim whether a deepened board runs away). **(ii) Hero-recipient compounding** — steal gains land on a hero, persist across the clear, and stack every round the Throne is re-played (the unbounded compounding vector; R62f uncapped). **(iii) Second King × Hymn double window** — Hymn adds a mid-round steal event on top of the round-end one → two steal windows per round in a venom-stacked board.
  - **✅ ENGINE TASK 21 — THE 500-SIM WAVE-1 BALANCE CAMPAIGN (measurement only; engine byte-identical, R62-E appended) — DONE (2026-07-13). ⚠ RESULT: 88/88 measured, TWO TARGET-A BREACHES — the wave table is NOT adopted as the new flag-on baseline; a balance pass is required (future ruled deltas, NOT tuned here).** Built `tools/sim_campaign.js` (KEEPER — seasonal waves reuse it). Engine + index.html UNMODIFIED (git-clean); the flag-off launch baseline stays FROZEN and byte-identical (Deva-vs-Naga **40.9/59.1** proven); appended R62-E verbatim to `docs/GDD_DELTAS.md` (the one doc edit). Runtime: RUN1 2.6s (5000 games) + RUN2 4.1s (9600 games) — no chunking needed.
    - **RUN 1 — CORE TABLE (500×10, wave ON, mrityulok; wave-WR / launch / Δ):** Deva-mirror 51.4/49.8/+1.6 · Asura-mirror 52.4/53.6/−1.2 · Vanara-mirror 49.9/52.8/−2.9 · Naga-mirror 46.9/51.4/−4.5 · Deva-Asura **36.1/47.7/−11.6** · Deva-Vanara 54.8/45.1/+9.7 · **Asura-Vanara 61.1/42.3/+18.8 (the epicenter)** · Deva-Naga **42.3/40.9/+1.4 (anchor — counter held)** · Asura-Naga 57.7/49.0/+8.7 · Vanara-Naga **44.8/57.3/−12.5**.
    - **TARGET A (faction cross-aggregate ≤±3):** Deva 44.4/44.6 **−0.2 PASS** · **Asura 60.9/47.9 +13.0 BREACH** · **Vanara 43.0/56.6 −13.6 BREACH** · Naga 51.7/50.9 **+0.8 PASS**. **TARGET B (Deva-Naga ~41/59):** wave **42.3/57.7** vs launch 40.9/59.1 — the designed counter HELD (+1.4). Garuda/Dhanvantari/Mahapadma on this matchup: **Garuda cleanse essentially NEVER fires (2/5000 games — a non-factor)**; Dhanvantari 158, Mahapadma 143 (of ~500 Deva-Naga games) — the counter's weight is Mahapadma/venom, not Garuda.
    - **RUN 2 — RATNA AUDIT (Gupta-only vs Ratna-inclusive, matched seeds/opponents; target ≈0 = build-arounds not upgrades):** Deva −1.5 · **Asura −5.6 (NON-TRIVIAL, but SAFE direction: Ratna DILUTES the strong Gupta core, not an upgrade — Asura Gupta-only 65.4% > Ratna-incl 59.8%)** · Vanara +1.6 · Naga +2.0. Intent holds all four (no positive Ratna upgrade advantage anywhere).
    - **RUN 3 — FLAG VERDICTS (freq / magnitude → BENIGN/WATCH/BROKEN):** none BROKEN in isolation; the imbalance is AGGREGATE. **WATCH:** Second King (8.9% games, 3146 power stolen total ≈7/game, 2475 events — round-scoped per R62-E, Naga balanced so absorbed) · Mahishi (11.2%, 1024 copies — Asura-wave contributor) · Iron Crucible (5.8%, 717 regains — Asura-wave contributor). **BENIGN:** Kalakuta→Serpent's Kiss (2.4% — low freq) · Hymn (12.6%) · Mahapadma (13.7%, counter held) · Vidyutastra×Chandrahas 4-surge (0.9% — rare) · Mohanastra-doubled (0.5% — rare) · Dawn Banner (13.5%) · Kalpavriksha+Dawn Banner (2.1%) · Living Bridge (3.9%, 1040 buffed — Vanara UNDERpowered, not OP) · Suryastra-in-Patala (26% of the Patala batch, 3-AoE — Deva balanced) · Kartikeya (6.6%, 442 astra-punishes — Deva balanced) · Anjaneya's Roar (12.2%) · **Mahishasura hunger NOT trivially fed (15.1% games, 548 unfed ≈73% of appearances DECAY − the downside works)** · Brahmadanda-mirror (1.6%, Asura-mirror 52.4 — no warp) · **Garuda (0.04% — non-factor)**.
    - **ATTRIBUTION (reported, NO fixes):** **Asura BREACH (+13.0):** the Asura wave is collectively strong — the Gupta core (RUN-2 Gupta-only 65.4%) of aggressive bodies (Mahishasura P7, Holika, Andhaka P6, Atikaya P6, Vritra P8 bind) plus the Mahishi + Iron Crucible payoff engines. **Vanara BREACH (−13.6):** the Vanara wave collectively underdelivers — it now LOSES to both Asura (−18.8) and Naga (Vanara-vs-Naga −12.5); the formation/leap payoffs (Living Bridge 3.9% uptime, Setu/Anjana/Rambha) do not keep pace with Asura bodies or Naga venom. Epicenter = Asura-vs-Vanara (42.3→61.1). **NEW-BASELINE RECOMMENDATION: DO NOT adopt the wave table as the flag-on reference** — two breaches require a balance pass (Asura wave down, Vanara wave up); the flag-off launch baseline remains the frozen, byte-identical reference (not in question). Deva/Naga waves are in-band and could ship as-is.
  - **✅ ENGINE TASK 21b — ATTRIBUTION PROBE (card-intrinsic vs AI-capability split of the two breaches; measurement only, engine byte-identical) — DONE (2026-07-13). ⚠ HEADLINE: BOTH breaches are LARGELY AI-CAPABILITY ARTIFACTS, not card-intrinsic imbalance — the D1 lesson at design scale. A raw number nerf/buff would treat a symptom whose cause is the AI failing to pilot the Vanara positional/formation kit.** Extended `tools/sim_campaign.js` (keeper); engine + index.html UNMODIFIED (git-clean); Deva-vs-Naga **40.9/59.1** anchor + suites green + 0 console.log. Runtime 3.9s.
    - **STEP 0(a) win-attribution (Asura-vs-Vanara, winrate-when-present):** top Asura lifters Mahishi 77.7% · Vritra 65.8% · Vidyutastra 63.1% · Holika 60.9%. Bottom Vanara: **Setu Stones 20.8% · Living Bridge 28.1% · Setu Mason 31.5%** — the formation payoffs sit BELOW Vanara's 37.7% base rate (anti-correlated with winning → dead/counterproductive in AI hands). **(b) GARUDA SCANNER CORRECTION (loud):** RUN-3's "0.04%" was FIRES, not PLAYS. Measured: **Garuda PLAYS=627, FIRES(cleansed)=2** across 2000 Deva games. Garuda is drafted+played in ~30% of games but its venom-cleanse fires **2 times** — venom accrues at round-end drains, so a mid-round hero play finds clean units. **Garuda functions as a vanilla P7 hero; its anti-venom clause is inert.** (The RUN-1 non-factor verdict stands; the FREQUENCY-of-play was under-reported, the cleanse-impact was correct.)
    - **PROBE 1 — ABLATION LADDER (Asura-vs-Vanara, matched seeds; control Asura 62.3/Vanara 37.7):** No single Asura card carries the breach — biggest drops on removal are **Mahishi −3.0, Vritra −2.8**; the rest are neutral-to-DILUTIVE (**Iron Crucible +2.5, Holika +0.7, Mahishasura +0.2** — removing them HELPS Asura). Vanara side: removing the formation payoffs **HELPS** Vanara (**Living Bridge +3.4, Setu Mason +2.0**) — they are dead weight in AI hands; only **Anjana −2.5** (extra leap) is a real Vanara card. → the imbalance is DISTRIBUTED (aggregate), not one broken card; Iron Crucible was over-flagged (it is a drag).
    - **PROBE 2 — AI-SENSITIVITY SPLIT (the key signal; Death Match excluded):** cell1 advanced/advanced **Asura 61.1**; cell2 **Asura BEGINNER → 42.0 (−19.0)**; cell3 **Vanara BEGINNER → 78.5 (+17.4)**. Degrading the Asura AI COLLAPSES the edge (beginner Asura LOSES) → **the Asura edge is DECISION-DEPENDENT, the cards do NOT play themselves.** The matchup swings ~±18-19 with either AI degraded → outcome is dominated by relative AI execution, i.e. the advanced AI extracts far more from Asura's simple aggro kit than from Vanara's complex positional kit = a CAPABILITY ASYMMETRY.
    - **PROBE 3 — VANARA EXECUTION-GAP (turn-by-turn):** Leaps 1.49/game (AI greedily takes every gain≥3 leap — NOT a skip-gap). Formation peak width 5.16, **line-of-4 rate 84.8% mid-round** (ceiling 91% — width IS achieved). BUT **Living Bridge fires only 29.3% when on board** (its round-end line-of-4 rarely survives the drain/removal — a timing-fragile condition). **movePosition: Vault 0.29/game, Riksha 0.93/game, but Setu Stones anchor 0.000/game — the AI NEVER uses the Stones positional anchor (it is UI-deferred → AI-inert), which is exactly why Setu Stones is the worst Vanara card (20.8% win).**
    - **THE SPLIT (estimated shares + evidence):** **Vanara −13.6 ≈ 60% AI-capability / 40% card-intrinsic** — capability: Setu Stones anchor 0.000 (UI-deferred, literally unusable by the AI), Living Bridge 29.3% fire-rate (timing), formation cards net-negative in AI hands (PROBE 1); card-intrinsic: leap/formation payoffs modest even when executed (PROBE 2 cell3 Vanara still loses at competent play). **Asura +13.0 ≈ 40% card-intrinsic / 60% AI-capability-asymmetry** — card-intrinsic: Mahishi + Vritra real ~3-pt lifters, aggregate aggro tempo; capability: the edge collapses when Asura AI degraded (−19), the advanced AI pilots simple aggro far better than the Vanara positional kit; several Asura cards are dilutive.
    - **RECOMMENDED BALANCE-PASS SHAPE (design recommendations for ruling — NO implementation, NO numbers changed):** (1) **Close the AI-capability gap FIRST, before any number change** (the D1 lesson): implement the Setu Stones AI anchor heuristic (currently UI-deferred → the AI can't use the card) and reconsider the Living Bridge's fragile round-end line-of-4 condition. A chunk of the Vanara gap is the harness, not the cards — buffing numbers now would over-compensate for an artifact. (2) Asura: no single-card nerf is indicated (ablation max −3); if a target is wanted, Mahishi (round-end copy) is the cleanest, but the edge is largely the AI's competence asymmetry, not raw card power. (3) Garuda: re-time or re-theme the inert anti-venom clause (a card-design finding, orthogonal to the breaches).
    - **DEVA/NAGA IN-BAND VERDICT UNTOUCHED:** every 21b probe is inside Asura-vs-Vanara + Garuda validation; nothing re-opens Deva 44.4 (−0.2) or Naga 51.7 (+0.8). The Garuda-inert finding REINFORCES the Deva-Naga counter holding (Garuda never helped Deva). Confirmed: the in-band verdict stands. **GATES:** engine.js/index.html unmodified, only `tools/sim_campaign.js` + this CLAUDE entry changed; flag-off parity byte-identical; suites green; 0 console.log.
  - **✅ ENGINE TASK 22 — THE AI-CAPABILITY PASS + RE-MEASURE (AI-layer only; full parity ceremony) — DONE (2026-07-13). ⚠ HEADLINE: the three 21b-flagged capability gaps are now CLOSED + proven, and the RE-MEASURE moved the aggregates by ≈0 (Vanara 43.0→43.0, Asura 60.9→60.9). This DISCHARGES the D1 "close capability first" caveat: the residual breaches are now confirmed DOMINANTLY CARD-INTRINSIC, and the balance pass will correct cards, not a harness artifact.** Three heuristics added STRICTLY in the AI section (`aiScoreCard`/`aiTakeTurn` + a helper by `aiPlacement`); zero card-text/engine-semantics change. `git diff` on engine.js is confined to lines 2014/2119/2237/2286/2309 (all AI-section + the test export).
    - **STEP 0 diagnosis:** decision path = `aiTakeTurn`→leaps→`aiMove` (argmax over `aiScoreCard`)→`aiPlacement`→`playCard`; difficulty via `difficultyOf(pl)` (beginner wraps, deathmatch is a `pl.difficulty==='deathmatch'` scoring overlay — constants UNTOUCHED). (b) Setu Stones has no AI default because the AI passes `tgt=null` for plain units (no `targetSpec`), so playCard's `setustones && targetUid!=null` branch never fires — Vault/Riksha have `aiScoreCard`/`movePosition` defaults, Stones' `targetUid` anchor channel was never set (the 0.000/game root). (c) Living Bridge's line-of-4 predicate (round-end hook) is read-only, reusable. (d) Garuda AI site = `aiScoreCard` case; friendly-venom state = `pl.units` venom. (e) all three gate on wave ids (`setustones`/`livingbridge`/`garuda`, all `wave:1`) → unreachable flag-off.
    - **THE THREE HEURISTICS:** (1) **Stones anchor** — in `aiTakeTurn` (advanced/deathmatch only; beginner keeps its random misplacement), when playing a Unit while our own Setu Stones is active, set `targetUid` = `stonesAnchor()` = the anchor maximizing INTERIOR formation value (units with both non-ghost neighbours, Setu Mason double-weighted per R40b; tie-break strongest-ally→lowest-index, deterministic, NO rng). (2) **Bridge sequencing** — in `aiScoreCard`, while our own Living Bridge is on board and we are `<4` non-ghost units, `+2` to Unit plays (build toward the line-of-4). Play-selection weight ONLY — NO pass/hold change (per task scope). (3) **Garuda hold** — in the `garuda` case, when `fv===0 && isNaga(opp) && g.round<3`, `s−=3` (wait for cleanse value); BOUNDED (released round 3 → always deploys; modest penalty so a clearly-best P7 still plays; no infinite hold).
    - **PARITY CEREMONY (the risk, discharged):** the **full `node src/test.js` output is BYTE-IDENTICAL pre/post** (diff empty — all 10 matchups + counter lines); **the difficulty ladder was measured flag-off, so byte-identical parity carries it** (beginner/advanced/deathmatch paths untouched flag-off — the Stones block is gated on `randomPlayP===0 && setustones` artifact, both unreachable flag-off). Deva-vs-Naga **40.9/59.1** anchor; ASTRA_DMG unchanged `{Agneyastra, Lanka Dahan, Pashupatastra, Suryastra, Vidyutastra}`; scenario 50 · venom 38 · **story 48 (flag-off → proves the story AI is untouched)** · 0 console.log.
    - **PER-HEURISTIC BEHAVIORAL PROOFS (11/11, wave1-on):** Stones — trailing Setu Mason chosen as anchor (→ Mason interior), no-Mason tie → strongest ally, no-unit → null; **end-to-end via `aiTakeTurn`** the entry lands `[scout, setumason, warrior]` (the anchor override beat aiPlacement's after-strongest). Bridge — `+2` while own-Bridge + `<4` units, OFF at `4+`. Garuda — `fv=0`+Naga+r1 → held (7−3=4); `fv=2` → cleanse 7+2=9; r3+`fv=0` → deploys (7); non-Naga+`fv=0` → no hold (7).
    - **PRE/POST PROBE-3 STATS (same 21b seeds):** **STONES anchor OPPORTUNITIES = 0.002/game** (post-22 sets the anchor on each; pre = 0.000, code absent) — near-zero because the AI rationally almost never PLAYS Setu Stones (its `aiScoreCard`≈1, far below unit scores); the anchor gap is proven-correct but nearly UNREACHABLE. **BRIDGE fire-rate = 29.3% (UNCHANGED, 41/140)** — the build weight did not move it; the bottleneck is round-end SURVIVAL, not reaching 4 mid-round (the AI already peaks 5.16 wide). **GARUDA plays 627→620, fires 2→27 (13×)** — the hold works mechanically (catches friendly venom), still deploys.
    - **THE RE-MEASURE (500×10, wave ON, same seed policy):** the entire core table is IDENTICAL to the pre-22 wave table EXCEPT **Deva-vs-Naga 42.3→42.7 (Deva +0.4, the Garuda hold)** — every Vanara cell unchanged (Asura-Vanara **61.1→61.1**, Vanara mirror 49.9→49.9), the sim-level confirmation that the Stones/Bridge heuristics are INERT at aggregate (as probe22 predicted). **TARGET A:** Deva 44.5 (−0.1) PASS · **Asura 60.9 (+13.0) BREACH (unchanged)** · **Vanara 43.0 (−13.6) BREACH (unchanged)** · Naga 51.6 (+0.7) PASS. **TARGET B:** Deva-vs-Naga **42.7/57.3** — the counter HELD (Garuda +0.4 did NOT disturb it). **Updated flag lines:** Second King 8.9% (3136 stolen/2465 events, WATCH, absorbed) · Mahishi 11.2% (1024 copies, WATCH, the one above-noise Asura nerf target) · **Living Bridge 3.9% (1045 buffed, fire-rate 29.3% UNCHANGED → timing fragility is structural, a card-text ruling not a placement fix)** · **Garuda 0.5%/27 fires/mag 51 (was 0.04%/2) → hold fires 13× but +0.4 WR → intrinsically low-value cleanse, card-design finding**.
    - **THE RESIDUAL + VERDICT:** post-22 Asura **60.9 (+13.0)**, Vanara **43.0 (−13.6)** — essentially unchanged. The capability heuristics are IN, PROVEN, and shown to have **near-zero aggregate leverage** (Stones 0.002 opp — the card isn't played; Bridge fire-rate flat — survival not build; Garuda +0.4 — cleanse is low-value). So 21b's "≈60% AI-capability" attribution for the Vanara breach is **REFRAMED**: the SPECIFIC-CARD gaps (Stones anchor, Bridge timing) were NOT the lever — closing them changed nothing, so those cards' low win-rates (Setu Stones 20.8%, Living Bridge 28.1%) are **CARD-INTRINSIC** (weak/awkward/fragile regardless of AI skill). 21b PROBE-2's ±18 AI-sensitivity is a GENERAL whole-AI-vs-whole-kit competence property, not fixable by card-specific heuristics. **RULED-DELTA RECOMMENDATIONS (design, NO numbers changed; ±2.2pp noise floor respected — no rec on a sub-noise ablation):** (a) **Vanara BUFF** — Living Bridge: change the fragile round-end line-of-4 condition (count 4+ at any round point, or make the +1 current-power that round so survival isn't required) — a CARD-TEXT ruling that addresses the 29.3% structurally; Anjana (−2.5 on removal, the one above-noise Vanara card) is the safe build-around anchor; lift the leap/formation payoffs THEMATICALLY (no single card above noise except Anjana). (b) **Asura NERF** — light touch on the two above-noise lifters, Mahishi (−3.0, round-end copy, cleanest single target) then Vritra (−2.8); do NOT touch the DILUTIVE cards (Iron Crucible/Holika/Mahishasura — removing them HELPS Asura). (c) **Garuda** — re-time/re-theme the low-value cleanse (orthogonal to the breaches). **NEW-BASELINE RECOMMENDATION (round two): STILL DO NOT ADOPT** the post-22 wave table — the two breaches persist — but the capability caveat is now DISCHARGED (heuristics closed + proven inert at aggregate), so the balance pass is cleanly warranted and will correct cards. The flag-off launch baseline stays frozen/byte-identical. **DEVA/NAGA IN-BAND SURVIVES:** Deva 44.5 PASS, Naga 51.6 PASS, counter 42.7/57.3 held — nothing here re-opens it. **GATES:** engine.js diff confined to the AI section (2014/2119/2237/2286/2309); index.html rebuilt; only these + `tools/sim_campaign.js` + this entry changed; scratch proof files removed. Total sim runtime ≈4s (probe22 1.1s + run1 2.7s).
  - **✅ ENGINE TASK 23 — BALANCE ROUND ONE: R63 (Living Bridge re-text) + R64 (Mahishi P6) + R65 (Vritra P7, ARMED) — DONE (2026-07-13). ⚠ HEADLINE: all three lightest-touch ruled deltas landed with the full parity ceremony, but BOTH breaches PERSIST (Asura +11.8, Vanara −13.5) — the delta ladder is EXHAUSTED and insufficient; the residuals need a broader DESIGN ROUND (distributed across the kit), not single-card deltas.** These rulings CHANGE SHIPPED WAVE CARDS (R40b/R49a/R54 precedent: doc append + engine delta + parity ceremony in one task). All three wave-gated → flag-off structurally safe, ceremony ran anyway.
    - **STEP 0 (a) BRIDGE MUTATION MAP:** every unit-add/reorder site (playCard push, 9 `onUnitRevive` choke sites, Rakta spawn, moveUnit, swapUnits, Sanjivani steal) occurs INSIDE a turn action that ends in `afterAction` → a single `checkBridgeLine(g,pi)` for both sides at the top of afterAction (after `sweepDeaths`, on the settled row) subsumes them all. doLeap is power-copy-in-place (engine line 517 — never relocates) so it can't change membership. Shesha's round-start revival is the only non-action mutation and is MOOT (artifacts clear at round end → no Bridge active at round start). Lock = round-stamp `pl.bridgeFiredRound` (auto-invalidates as g.round advances → resets at round start). **(d) HOOK-ORDER CHECK PASSED:** the current engine round-end order (Savitur→decay→Kamadhenu→Sushena→Iron Crucible→**Living Bridge**→Drowned Altar→Kalpavriksha→Mahishi) MATCHED the R42/R54 pre-amendment statement exactly — canon and engine agreed, so the amendment was safe.
    - **R63 (Living Bridge = formation-moment trigger):** removed from the round-end hook (+ its fast-out gate entry); `checkBridgeLine` fires the FIRST time the owner's row holds an unbroken run of 4+ non-ghost units this round (ghosts BREAK the run R63f; Rakta tokens count), buffing the WHOLE run +1 base+power (R63e), once/round (R63c lock). Wave1-gated. **R64:** Mahishi def `p:7→6`. **R65 (ARMED):** the post-R63+R64 re-measure showed Asura +12.7 (> +3) → Vritra def `p:8→7` applied.
    - **PARITY CEREMONY (full):** the entire `node src/test.js` output is BYTE-IDENTICAL to the frozen baseline (R63+R64+R65 cumulative; verified via git-stash diff); Deva-vs-Naga **40.9/59.1** anchor; ASTRA_DMG unchanged `{Agneyastra, Lanka Dahan, Pashupatastra, Suryastra, Vidyutastra}`; invariants pass; scenario 50 · venom 38 · story 48 · **0 console.log**.
    - **R63 BEHAVIORAL MATRIX (18/18, wave1-on, real rounds):** fires on the 4th-unit PLAY (all 4 +1) · Bridge onto an existing 4-line fires immediately · line completion via moveUnit (ghost-gap merge) → afterAction fires · doLeap does NOT change membership (power-copy) + afterAction-after-leap fires on a 4-line · revival completes the line → fires — **VALIDATED WITH THE REAL VANARA CARD (Sanjeevani Call, the Vanara launch astra = revival choke #5): cast it to revive a Vanara Unit from discard onto a 3-row → the 4-line forms → "The Living Bridge spans — the 4-Unit line rises +1" (the 3 base-3 units → 4, the revived base-4 Tara → 5, all +1 base+power). Corrects the batch-proof note that had claimed no Vanara same-faction revival exists — Sanjeevani Call is exactly that** · **Rakta spawn** (real destroyUnit→onUnitDeath, 2 REAL ghost:false tokens) completes the run → fires · **5th unit same round does NOT re-fire (lock)** · **lock resets next round → fires again** · **ghost gap BLOCKS** (4 non-ghost total but max run 2 → no fire) · whole **5-run** gains when 5 form at once · **+1 is base+power** (survives a revival's power=base reset; a power-only buff would be lost) · **round-end hook no longer fires it** (cleared units carry base 3, no round-end +1). **R64 (2/2):** def P6 + R59 lifecycle intact at P6 (copies strongest Unit power 9).
    - **RE-MEASURE ONE (R63+R64, 500×10):** Deva 44.7 (+0.1) PASS · **Asura 60.6 (+12.7) BREACH** · **Vanara 43.1 (−13.5) BREACH** · Naga 51.6 (+0.7) PASS. → Asura > +3 armed R65; Vanara < −3 triggered STOP-AND-REPORT. **RE-MEASURE TWO (R63+R64+R65):** Deva 45.0 (+0.4) PASS · **Asura 59.7 (+11.8) BREACH** · **Vanara 43.1 (−13.5) BREACH** · Naga 52.2 (+1.3) PASS. **TARGET B:** Deva-vs-Naga **42.7/57.3** — the counter HELD (none of the three deltas touch the Deva-Naga cards). **FLAG LINES:** Bridge fire-rate **29.3% → 52.6%** (R63 nearly doubled it — the mechanical fix WORKED) but Vanara aggregate moved +0.1 (played 6.5%, small buff → near-zero leverage, confirming Task-22 card-intrinsic); Mahishi 543→551 games / copies 1024→1016 (P6); Second King 8.9% (3164 stolen, WATCH, absorbed).
    - **VERDICT:** Rungs fired: **R63 ✓, R64 ✓, R65 ✓ (armed).** Final aggregates: Deva 45.0 PASS, **Asura 59.7 BREACH (+11.8)**, **Vanara 43.1 BREACH (−13.5)**, Naga 52.2 PASS; counter 42.7/57.3 held. **NEW-BASELINE RECOMMENDATION (round three): STILL DO NOT ADOPT** the post-23 wave table — the delta ladder (three lightest-touch rulings) moved the aggregates ~1pt each and both breaches remain far out of band. **ART GATE:** Mahishi P6 (certain, R64) and Vritra P7 (armed, R65) frames are now gated — re-export `Asuras_Hero_Mahishi_P6_rLegendary` and `Asuras_Hero_Vritra_P7_rLegendary` (filename bakes P{power}). **STILL BREACHES — attribution (no further tuning this task):** *Asura +11.8* is DISTRIBUTED across the aggressive Gupta kit (Mahishasura/Holika/Andhaka/Atikaya/Vidyutastra/Blood Oath tempo) — the two nerfed heroes were the only above-noise single cards (21b ablation) and cost ~1pt aggregate each; no further single-card lever exists above the ±2.2pp noise floor → a broader Asura design round is required. *Vanara −13.5* is the collectively weak leap/formation payoff suite — R63 fixed the Living Bridge mechanically (fire-rate doubled) but the card's aggregate leverage is ~0; per the ladder discipline no further Vanara buffs were stacked (a design round, not a delta ladder). Deva/Naga in-band untouched. **GATES:** engine.js diff = the 3 def/hook edits (Bridge round-end removal + checkBridgeLine + afterAction call + bridgeFiredRound init + Mahishi P6 + Vritra P7 + export); index.html rebuilt; docs/GDD_DELTAS.md (R63–R65 verbatim) + docs/WAVE1_ROSTER_v0.2.md (3 annotations) + docs/rulings_R63-R65.md (authority) + CLAUDE.md + tools/sim_campaign.js (Bridge scanner string R63-updated); scratch removed. Total sim runtime ≈8s (2× run1 + probe22).
  - **✅ ENGINE TASK 24a — DESIGN-ROUND DATA PULL (per-card evidence table for the Asura/Vanara design round; measurement only, engine byte-identical) — DONE (2026-07-14).** Extended `tools/sim_campaign.js` (keeper, `24a` mode: `cardTable`/`tempoProfile`/`leapProfile`); engine + index.html UNMODIFIED (git-clean), Deva-vs-Naga **40.9/59.1** anchor, suites green. Every metric names its source (the Indra's-Net rule at balance scale). Population: each faction table pools its 3 CROSS matchups (the population that defines the faction base win%; mirror excluded so Δbase is comparable), sorted win%-when-played ASC. Runtime 2.5s.
    - **VALIDITY NOTES (the Bridge-scanner discipline):** (1) `FIRE_KEYS` are apostrophe-free substrings VERIFIED against `src/engine.js` source (dodges curly/straight-quote drift). (2) **⚠ MAYA SHADE is a MEASUREMENT ARTIFACT:** it self-overwrites its `id` on copy (Maricha full-overwrite, engine 1471 `c.id=t.id`), so end-state id-detection misses ~90% of its plays — drawn 39 / played 38 are only the whiffed/unplayed cases (win% 26.3%* unstable, LOW-N); its **fires=408** is the true play signal. The only self-overwriting wave card. (3) LOW-N threshold played<100 → win% flagged `*` (only Maya Shade in the pooled tables). (4) **Anjaneya's Roar fires (2277) over-counts** — the `'Anjaneya'` key matches multiple log lines per cast (AoE −1 + interior-buff); it's an activity proxy, not cast count. (5) Target-gated astras have honest low play-rates (Jatayu 56%: needs an enemy >6; Vayavyastra 84% / Mohanastra 90%). (6) Passive read-time cards (Holika/Shumbha/Nishumbha/Andhaka/Setu Mason/Gaja/Anjana/Gandhamadana) and silent vanillas (Ash Legionnaire/Kishkindha Runner/Setu Stones) have no fire log → fires=—. (7) win%-when-played is a WEAK signal for ~98%-ubiquitous cards (win% ≈ base by construction); it's strongest for the selective cards that deviate. Pooled base win% Asura 59.6 / Vanara 43.1 — matches RUN-1 aggregates (validates the population).
    - **ASURA WAVE (base 59.6%) — the above-base LIFTERS (nerf targets):** **Mahishi 69.6% (+10.0, 375 played) — STILL the top lifter at P6 (R64 was insufficient)** · Blood Oath 64.5% (+4.9, card-advantage engine) · Vritra 63.7% (+4.1, at P7). Everything else is AT/BELOW base — many DILUTIVE (Blueprint 46.9%/−12.7, Shumbha −8.4, Ash Legionnaire −8.1, Pisacha −7.8, Raktabija −7.2, Surpanakha −7.2, Iron Crucible −6.6). So the Asura edge rides **3 selective cards (Mahishi/Blood Oath/Vritra) + the early-tempo body count**, not the dilutive filler.
    - **VANARA WAVE (base 43.1%) — the below-base DEAD PAYOFFS (buff targets) vs the above-base ANCHORS:** dead weight (−7 to −14): **Setu Stones 28.7% (−14.4)** · Living Bridge 33.2 (−9.9) · Matanga 33.7 (−9.4) · Kumuda −7.4 · Gavaksha −7.3 · Setu Mason −7.2 · Vault −6.9 — the whole formation/positional/leap-SETUP suite. Above-base anchors to PRESERVE: **Makardhwaja 50.0 (+6.9)** · Swayamprabha 48.2 (+5.1, card dig) · Anjana 47.8 (+4.7) · Gandhamadana 46.4 (+3.3) · Rambha 45.2 (+2.1) · Sampati 45.0 (+1.9) — the heroes + big leap BODIES + the dig, not the formation payoffs.
    - **EPICENTER (Asura-vs-Vanara only) — same shape, sharper:** Asura base 60.6 → Mahishi **+13.0 (73.6%)**, Blood Oath +7.4, Vritra +4.2; Vanara base 39.4 → Setu Stones **−19.0**, Matanga −11.3, Living Bridge −10.3.
    - **TEMPO SUITE (Asura, turn-by-turn, avgRound / avgΔboard the turn it lands):** Mahishasura 1.44 / **+7.56** · Andhaka 1.55 / +6.46 · Atikaya 1.61 / +6.64 · Vidyutastra 1.66 / +6.42 · Nishumbha 1.75 / +4.31 · Blood Oath 1.66 / **−1.11** (card-advantage, sacrifices board for cards). → the Asura kit lands **big bodies EARLY (round ~1.4–1.8, +6 to +7.6 board)** — the aggressive tempo that outruns the Vanara's slow formation build. (Kalanemi excluded — not a wave card.)
    - **LEAP SUITE (Vanara, leaps/game when played vs 1.44 base [21b: 1.49]):** Anjana 1.57 (+0.13, the only real enabler) · Gandhamadana 1.52 · Song 1.39 · Matanga 1.36 · Anjaneya's Roar 1.36. → the leap ENABLERS add ~0.1 leaps/game — negligible; the AI already takes every beneficial leap greedily, so more leap-CAPACITY finds no extra beneficial leaps. (Saranyu is a DEVA card, not Vanara — excluded, flagged.)
    - **DESIGN-ROUND HANDOFF (each ruling can now cite a line):** Asura nerf → Mahishi (still +10 at P6 — needs an ability change, not just power), then Blood Oath (+4.9 card-engine) / Vritra (+4.1); leave the dilutive filler alone. Vanara buff → the formation/positional suite (Setu Stones −14.4, Living Bridge −9.9, Setu Mason/Vault/Matanga/Kumuda/Gavaksha) needs a rework, not a number bump (leap CAPACITY is already saturated); build around the above-base anchors (Makardhwaja/Anjana/Swayamprabha/Gandhamadana/Rambha). **GATES:** src/engine.js + index.html UNMODIFIED; only tools/sim_campaign.js + this entry; flag-off parity byte-identical (40.9/59.1); suites green; runtime 2.5s.
  - **✅ ENGINE TASK 25 — DESIGN ROUND ONE: R66–R71 (2 Asura nerfs + 4 Vanara lifts) + RE-MEASURE — DONE (2026-07-14). ⚠ HEADLINE: both breaches NARROWED (~3pts each: Asura 59.7→57.0 / +9.1, Vanara 43.1→46.2 / −10.4, epicenter 60.6→55.6) but STILL breach ±3 — design round one made real progress; a design round TWO is needed. Counter held, all GUARDS satisfied.** Six shipped wave cards re-texted (R40b/R49a/R54 precedent: doc + engine + full ceremony + re-measure in one task); all wave-gated.
    - **STEP 0 (quoted each site):** (a) Mahishi `h.power=sp` could copy DOWN → R66 guards `sp > h.power`; once-flag `h.mahishiSpent` per-match on the hero. (b) Blood Oath draw `deck.splice(0,2)`, real `destroyUnit`. (c) Setu Mason effPower both-sides `p+=1`. (d) Drummer on-play `u.power+=1` (temp). (e) Vault `t.power+=2` (temp). (f) Matanga `matangaArmed` flag (init/set/doLeap-consume/reset); onLeap fires only Kumuda+Rambha; bestLeap side-effect-free + already Gandhamadana-widened; `doLeap(...,free=true)` skips leapsUsed. No contradictions → no STOP-AND-REPORT.
    - **THE SIX:** R66 Mahishi ONCE-PER-GAME gain-only (`h.mahishiSpent`); R67 Blood Oath draw 2→1; R68 Setu Mason interior +1→+2; R69 Drummer this-round→permanent (base+power); R70 Vault this-round→permanent (base+power); R71 Matanga REWORK (flag removed; castMantra now `bestLeap → doLeap(free) → both +2 this round`; AI score gated on a beneficial bestLeap). Setu Stones QUEUED for seasonal redesign (−14.4 Δbase, 0.002 anchor opp/game — a nudge can't save it; no delta).
    - **PARITY CEREMONY:** full `node src/test.js` BYTE-IDENTICAL to the R65 baseline (git-stash diff); Deva-vs-Naga **40.9/59.1**; ASTRA_DMG unchanged `{Agneyastra, Lanka Dahan, Pashupatastra, Suryastra, Vidyutastra}`; invariants pass; scenario 50 · venom 38 · story 48 · **0 console.log**.
    - **BEHAVIORAL PROOFS (21/21, wave1-on, real rounds):** R66 — fires on first GAINFUL round end (6→9); HOLDS when the copy would not increase her (not spent); spent EXACTLY once/match (2nd gainful end holds); degenerate no-unit holds; **copy COUNTS toward the round score** (constructed as the deciding margin → p0 wins the round). R67 — draws exactly 1 (deck −1); destroy feeds deathsThisRound; **COMPOSED: real destroy feeds Raktabija → 2 Rakta tokens**. R68 — interior effPower 4 (2+2) / edge 2. R69 — both neighbours +1 base+power (survives a revival power=base reset); single-neighbour; no-neighbour no-op. R70 — Vault target +2 base+power (5→7). R71 — real doLeap (7→9); **COMPOSED Kumuda +1 (leaper) + Rambha +1 fire**; leapsUsed still 0 → a normal leap still available; Gandhamadana widening reachable (non-adjacent target); no-legal-pair logged no-op; the +2 is TEMPORARY (leaper base unchanged).
    - **RE-MEASURE (500×10 wave ON). vs launch / vs task-23:** Asura-Vanara **55.6** (was 60.6, −5.0, the epicenter) · Deva-Asura 40.1 (was 37.7) · Deva-Vanara 53.1 (was 54.7) · Vanara-Naga 47.3 (was 44.6) · Asura-Naga 55.5 (was 56.1) · Deva-Naga **42.7** (held). **TARGET A:** Deva 45.3 (+0.7) PASS · **Asura 57.0 (+9.1) BREACH** · **Vanara 46.2 (−10.4) BREACH** · Naga 51.5 (+0.6) PASS. **TARGET B:** Deva-Naga **42.7/57.3** HELD. **GUARDS:** Naga in band (52.2→51.5) moving BOTH directions (up vs Asura, down vs Vanara); Deva undisturbed (45.0→45.3); counter held → all satisfied. **FLAG LINES:** Mahishi fires 1016→492 (once/game) but Δbase +10.0→**+14.2 (STILL top lifter** — an ability problem, base-relative); Blood Oath **+4.9→−3.0** (epicenter +7.4→−0.2, R67 cleanly worked); Setu Mason −7.2→−6.7 · Drummer −5.0→−5.2 · Vault −6.9→−8.5 (correlational noise for ~ubiquitous cards; the AGGREGATE moved +3.1) · **Matanga −9.4→−4.7, leaps/game 1.36→2.20** (rework halved the deficit, doubled leap contribution); Bridge fire-rate ~50.8% (R63 unchanged); Second King 8.9% (WATCH, absorbed).
    - **VERDICT:** per-ruling — R67 (Blood Oath) and R71 (Matanga) are the clean wins; R68–R70 lift the Vanara aggregate collectively (+3.1); R66 (Mahishi once/game) shaved fires + aggregate but Mahishi REMAINS the top Asura lifter (+14.2) → the residual Asura lever is a deeper Mahishi ABILITY rework, not power. **NEW-BASELINE RECOMMENDATION (round four): STILL DO NOT ADOPT** — both breaches narrowed but persist (Asura +9.1, Vanara −10.4). **ART GATE (6 frames, text change):** re-export `Asuras_Hero_Mahishi_P6_rLegendary`, `Asuras_Mantra_BloodOath_rUncommon`, `Vanaras_Unit_SetuMason_P2_rCommon`, `Vanaras_Unit_DrummeroftheHost_P2_rCommon`, `Vanaras_Mantra_VaultoftheSky_rEpic`, `Vanaras_Mantra_MatangasBlessing_rRare`. **STILL BREACHES — attribution (no further tuning in-task):** Asura +9.1 = Mahishi (+14.2, needs an ability rework) + the aggressive-tempo body count; Vanara −10.4 = the formation/leap suite is still collectively below base (R68–R71 helped, more structural lift needed). Deva/Naga in band. **GATES:** engine.js = 6 def-text + 6 logic edits (all wave-gated), index.html rebuilt; docs/GDD_DELTAS.md (R66–R71 verbatim + Setu Stones NOTE + GUARDS) + docs/WAVE1_ROSTER_v0.2.md (6 annotations + Stones note) + docs/rulings_R66-R71.md (authority) + CLAUDE.md; **tools/sim_campaign.js UNCHANGED — the six fire-log substrings were PRESERVED (verified all keys survive), so no scanner rename needed**; scratch removed. Runtime ≈6s (2× run1 + probe22 + 24a).
  - **✅ ENGINE TASK 26 — DESIGN ROUND TWO: R72–R75 (Mahishi persistence kill + 3 Vanara lifts) + RE-MEASURE — DONE (2026-07-14). ⚠ HEADLINE: R72 finally moved Mahishi's ABSOLUTE win-when-played (71.2→62.0, the first nerf to do so — the cross-round CARRY was the ability, exactly as the ruling diagnosed). Both breaches CONVERGED again ~2.5pts/side (Asura 57.0→54.4 / +6.5, Vanara 46.2→48.4 / −8.2, epicenter 55.6→51.3) but STILL breach ±3. Counter held, all GUARDS satisfied.** Four shipped wave cards changed; all wave-gated.
    - **STEP 0 (quoted each site):** (a) Mahishi post-R66 (1832): `h.power=sp`+`h.mahishiSpent`; R72 revert point = the board-reset loop (1886), reached ONLY when the match continues (endRound returns before reset at match-over → a deciding-round spike persists into g.over, R72b); stash `h.mahishiPreCopy`. (b) Runner def `p:3`. (c) Talon (1533) `n=Math.min(3,Math.floor(count/2))`, self-inclusive, per-hit damageUnit (Holika sharpens each). (d) Kumuda onLeap (528) `u.base+=1;u.power+=1` — shipped class IS base+power, matches printed "permanently" → **RATIFIED, no mismatch, no STOP-AND-REPORT**.
    - **THE FOUR:** R72 Mahishi THIS-ROUND-ONLY (`h.mahishiPreCopy` stash + revert at board clear; `mahishiSpent` never refunded); R73 Runner p:3→4; R74 Talon uncap (`min(3,…)→floor(…/2)`, mechanic + AI score); R75 Kumuda onLeap +1→+2 (base+power, constant-only). HOLDS: no further Asura targets; Vault −8.5 needs one more measurement; Setu Stones stays in the seasonal queue; Bridge holds (R63).
    - **PARITY CEREMONY:** full `node src/test.js` BYTE-IDENTICAL to the task-25 baseline (git-stash diff); Deva-vs-Naga **40.9/59.1**; ASTRA_DMG unchanged; invariants pass; scenario 50 · venom 38 · story 48 · **0 console.log**.
    - **BEHAVIORAL PROOFS (17/17, wave1-on, real endRounds):** R72 LIFECYCLE MATRIX — SPIKE counts toward the round score (deciding-margin construction → p0 wins round 1); REVERT proven (next round at pre-copy 6); pre-copy ACCRUED gains preserved (buffed to 8, copy to 10, reverts to 8 not base 6); once-flag consumed at fire (a later gainful round end does NOT re-fire, stays 6); gain-only hold unchanged; degenerate no-unit holds; hero persistence intact. R73 def P4 + P4 body. R74 uncapped (8-wide→4 hits, 10-wide→5 hits, BEYOND the old cap); **Holika composition (4 hits ×2 sharpened = 8, 10→2)**; self-inclusive floor(n/2); single target. R75 real doLeap fires +2 (base 3→5, power 7→9, base+power).
    - **RE-MEASURE (500×10). vs launch / vs task-25:** Asura-Vanara **51.3** (was 55.6, −4.3, epicenter) · Deva-Asura 41.1 (was 40.1) · Asura-Naga 53.0 (was 55.5) · Vanara-Naga 49.4 (was 47.3) · Deva-Naga **42.7** (held). **TARGET A:** Deva 45.5 (+0.9) PASS · **Asura 54.4 (+6.5) BREACH** · **Vanara 48.4 (−8.2) BREACH** · Naga 51.6 (+0.7) PASS. **TARGET B:** Deva-Naga **42.7/57.3** HELD (R72's Asura cross-cells + R74's Holika path — Vanara-vs-Asura only — did not touch Deva-Naga). **GUARDS:** Naga 51.5→51.6 in band moving BOTH directions (up vs Asura, down vs Vanara); Deva 45.3→45.5 undisturbed; counter held → all satisfied. **FLAG LINES:** **Mahishi absolute win-when-played 71.2→62.0** (Δbase +14.2→+7.7, fires ~once/game 491) — R72 finally moved her absolute; Runner Δbase −10.0→−6.1 (P4); **Talon hit distribution 1×66.9% / 2×32.0% / 3×1.0%, N>3 = 0.0% (0/387 casts) — the uncap NEVER fires in AI play** (human-ceiling change, ~0 aggregate); Kumuda Δbase −7.4→−6.5, fires 144→227; Bridge fire-rate ~52.8% (R63 unchanged); Second King 8.8% (WATCH, absorbed).
    - **VERDICT:** per-ruling — R72 (Mahishi persistence kill) is the standout (absolute −9, the ability finally addressed); R73 (Runner P4) a clean stat lift (−10.0→−6.1); R75 (Kumuda +2) a modest lift riding the new leap economy; R74 (Talon uncap) PROVEN honest — 0.0% AI casts exceed the old cap → a human-ceiling/design-correctness change, ~0 AI-aggregate effect. **CONVERGENCE:** design round one closed ~2.7/3.1, design round two closed ~2.6/2.2 — steady ~2.5pts/side/round; from +11.8/−13.5 (task-23) to **+6.5/−8.2** now. **NEW-BASELINE RECOMMENDATION (round five): STILL DO NOT ADOPT** — both breaches persist but converge steadily; a design round THREE would likely bring them in band. **ART GATE (4 frames):** re-export `Asuras_Hero_Mahishi_P6_rLegendary` (3rd change, text), `Vanaras_Unit_KishkindhaRunner_P4_rCommon` (P4 renumber), `Vanaras_Unit_VinatasTalon_P4_rRare` (text), `Vanaras_Unit_Kumuda_P3_rUncommon` (text). **STILL BREACHES — attribution (no further tuning in-task):** Asura +6.5 = Mahishi still above base (+7.7, though absolute dropped 9) + aggressive-tempo body count (no further single-card Asura lever above noise, per HOLDS); Vanara −8.2 = the formation/leap suite still collectively below base despite the lifts. Deva/Naga in band. **GATES:** engine.js = 4 def-text + 5 logic edits (Mahishi stash+revert, Runner P4, Talon uncap mechanic+score, Kumuda +2 — all wave-gated), index.html rebuilt; docs/GDD_DELTAS.md (R72–R75 verbatim + HOLDS + GUARDS) + docs/WAVE1_ROSTER_v0.2.md (4 annotations) + docs/rulings_R72-R75.md (authority) + CLAUDE.md; **tools/sim_campaign.js UNCHANGED — the Kumuda substring "Kumuda swells with the Leap" (and all others) preserved, no scanner rename needed** (verified); scratch removed. Runtime ≈6s.
  - **✅ ENGINE TASK 26b — DATA REFRESH FOR DESIGN ROUND THREE (per-card pull on the post-R75 engine; measurement only, engine byte-identical) — DONE (2026-07-14).** Reran the 24a pull with a new **vs24a column** (Δbase-now − Δbase-at-24a, `*`=card CHANGED by R66–R75); `tools/sim_campaign.js` extended (BASE_24A map + column), engine UNMODIFIED, Deva-vs-Naga **40.9/59.1** anchor, suites green. Runtime 2.4s. **⚠ INTERPRETATION NOTE (Bridge-scanner discipline — name what the metric measures):** Δbase is FACTION-RELATIVE, and the bases shifted (Asura 59.6→54.4 / Vanara 43.1→48.4). So vs24a = card-movement RELATIVE to its faction's overall shift: an unchanged card ≈ 0 ± noise; because the Vanara base ROSE, unchanged Vanara cards trend slightly-negative vs24a (their absolute didn't outpace the risen base) and buffed cards show positive above that; Asura is the mirror. For the two divergent cases (Mahishi), the absolute-win metric (task-26) is the cleaner read.
    - **ASURA WAVE (base 54.4%) — the two CHANGED cards moved, all else ±noise:** **Blood Oath vs24a −7.2*** (Δbase +4.9→−2.3 — R67 draw 2→1, now BELOW base, the biggest Asura mover). **Mahishi vs24a −2.3*** (Δbase +10.0→+7.7 — the vs24a movement is MUTED by the −5.2 base shift; her ABSOLUTE win-when-played dropped 69.6→62.0, the real R72 signal). STILL the top Asura lifter (+7.7). Every UNCHANGED Asura card moved within ±2.8 (noise), so the nerfs are isolated — no collateral.
    - **VANARA WAVE (base 48.4%) — the buffs moved, led by the two reworks:** **Matanga vs24a +4.3*** (−9.4→−5.1, R71 rework, biggest Vanara mover) · **Kishkindha Runner vs24a +3.9*** (−10.0→−6.1, R73 P4) · **Kumuda +0.9*** (−7.4→−6.5, R75, muted by base shift) · **Drummer +0.7*** (−5.0→−4.3, R69) · **Setu Mason +0.3*** (−7.2→−6.9, R68, tiny) · **Vinata's Talon −1.4*** (R74 uncap, 0 AI effect, noise). **⚠ VAULT vs24a −1.1* (Δbase −6.9→−8.0): the HOLDS "one more measurement" verdict — R70's permanent +2 did NOT lift Vault's win-correlation; it stays ~−8 (a move+buff mantra played into losing positions). A design-round-three candidate (permanence alone was insufficient).** Living Bridge vs24a −2.6 (unchanged card; its ABSOLUTE rose 33.2→35.9 but lagged the risen base — a base-shift artifact, not a regression).
    - **EPICENTER (Asura-vs-Vanara):** same shape sharper — Mahishi +8.5, Blood Oath now +0.6 (was +7.4 → neutralized), Vritra +3.3; Setu Stones −18.5, Living Bridge −16.8, Vault −13.5; anchors Makardhwaja +12.1, Swayamprabha +8.1, Anjana +6.4.
    - **TEMPO SUITE (Asura) — STABLE (untouched bodies):** Mahishasura 1.41/+7.56 · Andhaka 1.58/+6.48 · Atikaya 1.63/+6.63 · Vidyutastra 1.68/+6.31 · Nishumbha 1.76/+4.34 · Blood Oath 1.69/−1.26 (slightly deeper than −1.11, the draw-1 sacrifice). The early-tempo body count is intact.
    - **LEAP SUITE (Vanara) — ROSE:** base leaps/game **1.44→1.69** (the R71 Matanga rework raised the whole leap economy — Matanga 1.36→2.18/game when played); Anjana 1.81, Gandhamadana 1.77, Song 1.64, Anjaneya 1.56. This is why R75 Kumuda now has fuel (fires 144→227).
    - **DESIGN-ROUND-THREE HANDOFF (measured lines):** Asura residual +6.5 → Mahishi (+7.7, an ability rework already done R72 — a further light touch or a tempo-body nudge is the only above-noise lever; the filler is dilutive, untouchable). Vanara residual −8.2 → the formation/positional core is still the drag (Setu Stones −15.6 [seasonal queue], Living Bridge −12.5, **Vault −8.0 [R70 permanence insufficient — needs more]**, Gavaksha −9.2); the leap/hero anchors (Makardhwaja/Anjana/Swayamprabha/Rambha) are healthy. **VALIDITY:** FIRE_KEYS source-verified (unchanged); only LOW-N card is Maya Shade (39 played, `^`; still the self-overwrite artifact — fires 397 is its real play signal). **GATES:** src/engine.js + index.html UNMODIFIED; scope = tools/sim_campaign.js (vs24a column) + this entry; flag-off parity byte-identical (40.9/59.1); suites green (invariants · scenario 50 · venom 38 · story 48 · 0 console.log); runtime 2.4s.
  - **✅ ENGINE TASK 27 — DESIGN ROUND THREE, THE CLOSER: R76–R81 (2-rung ladder) — DONE (2026-07-14). ⚠ HEADLINE: the closer converged both breaches further (Asura +6.5→+3.8, Vanara −8.2→−5.7, epicenter 51.3→48.3) but NEITHER landed in ±3 → per the ladder discipline this is a STOP-AND-REPORT, NO adoption recommendation. The delta ladder is at its floor; the residual is structural (Vanara's formation core + Setu Stones seasonal redesign; Asura's distributed tempo).** Six shipped wave cards changed (R76–R79 rung one; R80/R81 both ARMED); all wave-gated.
    - **STEP 0 (quoted each site):** (a) Mahishi def p:6 — only ref; aiScore reads c.p (no stale P6). (b) Vault post-R70 castMantra (985): destination neighbours read via `adjacentUnits(pl,t)` AFTER moveUnit; R63 composition via afterAction/checkBridgeLine (automatic — Vault is a mantra→playCard→afterAction). (c) Bridge grant (1758) `u.base+=1;u.power+=1` — constant point. (d) Gavaksha def p:3 + swap branch (1550, unchanged). (e) Vritra def p:7 / Mason effPower +2 — held until armed. **⚠ FINDING: `adjacentUnits` does NOT filter ghosts** (returns Boolean-truthy neighbours) → R77 filters. **⚠ FINDING: the Living Bridge DEF TXT was STALE since R63** (still read the old ROUND-END +1; R63 changed the mechanic but not the txt, and the Bridge frame was missing from R63's art gate) — corrected here + added to the art gate.
    - **RUNG ONE (R76–R79):** R76 Mahishi P6→P5; R77 Vault formation-native (moved unit + non-ghost destination neighbours +2 base+power, after moveUnit); R78 Bridge grant +1→+2 (constant + stale-txt fix); R79 Gavaksha P3→P4. **RUNG TWO (both conditional rungs ARMED):** R80 Vritra P7→P6 (Asura +5.3 >+3), R81 Setu Mason interior +2→+3 (Vanara −6.5 <−3).
    - **PARITY CEREMONY (both rungs):** full `node src/test.js` BYTE-IDENTICAL to the task-26 baseline (git-stash diff, rung 1 and rung 1+2); Deva-vs-Naga **40.9/59.1**; ASTRA_DMG unchanged; invariants pass; scenario 50 · venom 38 · story 48 · **0 console.log**. Scanner keys preserved ("The Living Bridge spans", "Vault of the Sky lifts") → tools/sim_campaign.js UNCHANGED.
    - **BEHAVIORAL PROOFS (14/14 rung one, wave1-on):** R76 def P5 + ability intact (copies then reverts, R72 stands). R79 def P4 + P4 body. R77 — moved unit +2 base+power; BOTH destination neighbours +2 (revival-reset contrast: survives power=base); edge = one neighbour; lone = self only; **COMPOSED: Vault +2 (A,B) AND R63 Bridge +2 (all 4) both land in one action, correct amounts (A,B=7 / C,D=5)**. R78 — +2 per unit in the run (base+power); lock unchanged (5th does NOT re-fire). R80/R81 def+read-time proofs (Vritra P6; Mason interior 2+3=5, edge 2).
    - **RE-MEASURE LADDER (500×10). RUNG ONE:** Asura 54.4→53.2 (+5.3), Vanara 48.4→50.1 (−6.5) — both breach → both conditional rungs armed. **RUNG TWO (R80+R81):** Asura-Vanara **48.3** (was 51.3) · Deva-Asura 43.1 · Vanara-Naga 51.3 · Deva-Naga **42.7** (held). **TARGET A:** Deva 45.4 (+0.8) PASS · **Asura 51.7 (+3.8) BREACH** · **Vanara 50.9 (−5.7) BREACH** · Naga 52.0 (+1.1) PASS. **TARGET B:** 42.7/57.3 HELD. **GUARDS all satisfied:** Naga 52.0 in band (Vanara-Naga climbing toward launch), Deva 45.4 in band (Deva-Vanara falling toward launch), counter held. **FLAG LINES:** Mahishi absolute win-when-played **62.0→58.0** (P5), Δbase +7.7→+6.4, fires once/game (~490); Vault Δbase −8.0→−9.6 (correlational noise on a ~ubiquitous card; absolute barely moved — the rework's neighbour-buff didn't lift its win-correlation); **Living Bridge absolute 35.9→40.6, Δbase −12.5→−10.3 (R78 +2 helped, still 6% uptime)**; Gavaksha −9.2→−5.9 (P4, vs24a +3.3); Vritra +2.2→−0.6 (P6); Second King 8.8% (3076 stolen, WATCH, absorbed, unchanged).
    - **VERDICT (STOP-AND-REPORT — the closer did NOT close):** rungs fired R76 ✓ R77 ✓ R78 ✓ R79 ✓ R80 ✓(armed) R81 ✓(armed); final aggregates Deva 45.4 PASS, **Asura 51.7 BREACH (+3.8)**, **Vanara 50.9 BREACH (−5.7)**, Naga 52.0 PASS; counter 42.7/57.3 HELD. **CONVERGENCE LEDGER (Asura/Vanara Δlaunch across all three design rounds):** task-23 +11.8/−13.5 → DR1(25) +9.1/−10.4 → DR2(26) +6.5/−8.2 → DR3-r1(27) +5.3/−6.5 → **DR3-r2(27) +3.8/−5.7**. **NO ADOPTION** (both bands must land; neither did). **ATTRIBUTION (no improvised 3rd rung):** Asura +3.8 is 0.8 outside the band, WITHIN the ±2.2 noise floor of the edge — residual = the DISTRIBUTED aggressive-tempo body count (Mahishasura/Andhaka/Atikaya/Vidyutastra land ~round 1.4–1.7 for +6–7 board) + Mahishi's irreducible hero value (+6.4 at P5); no single card above noise → a structural tempo lever, not another delta. Vanara −5.7 is 2.7 outside — the formation/positional core is the persistent drag: **Setu Stones (−15, still in the seasonal queue, UNADDRESSED) + Living Bridge (−10.3 at 6% uptime) + Vault (−9.6, low-impact even reworked)**; a card-delta ladder cannot close it → needs the Setu Stones seasonal REDESIGN + a Bridge uptime mechanic (structural). **RECOMMENDATION (future ruling, NOT in-task):** stop the delta ladder; close Vanara via the Setu Stones redesign + a Bridge uptime fix, and accept Asura at the noise-edge or apply one structural tempo touch. **CUMULATIVE ART GATE (R63→R81, 12 frames, final versions):** Asuras_Hero_Mahishi_P5_rLegendary · Asuras_Hero_Vritra_P6_rLegendary · Asuras_Mantra_BloodOath_rUncommon · Vanaras_Unit_SetuMason_P2_rCommon · Vanaras_Unit_DrummeroftheHost_P2_rCommon · Vanaras_Unit_KishkindhaRunner_P4_rCommon · Vanaras_Unit_Gavaksha_P4_rUncommon · Vanaras_Unit_VinatasTalon_P4_rRare · Vanaras_Unit_Kumuda_P3_rUncommon · Vanaras_Mantra_VaultoftheSky_rEpic · Vanaras_Mantra_MatangasBlessing_rRare · Vanaras_Artifact_TheLivingBridge_rMythic (⚠ this last was MISSED in R63 — first flagged here). **GATES:** engine.js = 6 def-text + 4 logic edits (Vault rework, Bridge +2, Mason +3 constant, + the P5/P6/P4 def numbers — all wave-gated), index.html rebuilt; docs/GDD_DELTAS.md (R76–R81 verbatim + HOLDS + LADDER + GUARDS) + docs/WAVE1_ROSTER_v0.2.md (6 annotations) + docs/rulings_R76-R81.md (authority) + CLAUDE.md; tools/sim_campaign.js UNCHANGED (scanner substrings preserved); scratch removed. Runtime ≈9s (2× run1 + rung-1 run1 + probe22 + 24a).
  - **✅ ENGINE TASK 28 — R82 WAVE BASELINE ADOPTED (campaign close-out; doc-only, engine byte-identical) — DONE (2026-07-14). ⚠ THE WAVE-1 BALANCE CAMPAIGN (Tasks 21–27, R63–R81) IS CLOSED. R82 ADOPTS the post-Task-27 wave table as the ACCEPTED FLAG-ON REFERENCE ("WAVE BASELINE") — the new diff reference for ALL future wave-on work. The 88-frame art gate opens.** Appended R82 verbatim to docs/GDD_DELTAS.md; engine + index.html UNMODIFIED; flag-off parity byte-identical (Deva-vs-Naga **40.9/59.1**); suites green.
    - **THE WAVE BASELINE (the new flag-ON diff reference; 500×10, wave ON, advanced AI, mrityulok, campaign seed policy):** Deva mirror 51.4 · Asura mirror 55.2 · Vanara mirror 50.5 · Naga mirror 46.9 · Deva-Asura 43.1/56.9 · Deva-Vanara 50.3/49.7 · Asura-Vanara 48.3/51.7 · Deva-Naga **42.7/57.3** · Asura-Naga 49.9/50.1 · Vanara-Naga 51.3/48.7. Faction aggregates vs launch: Deva 45.4 (+0.8) · Asura 51.7 (+3.8) · Vanara 50.9 (−5.7) · Naga 52.0 (+1.1). **⇒ FUTURE WAVE-ON WORK (wave-1.1, seasonal) DIFFS AGAINST THIS TABLE, not launch.**
    - **TWO DOCUMENTED DEVIATIONS (accepted with adoption):** (a) **Asura +3.8** — 0.8 outside the ±3 target, WITHIN the ±2.2 noise floor → statistically indistinguishable from compliant; Mahishi corrected four ways (P7→P6→once-bound→persistence-killed→P5, absolute 69.6→58.0). (b) **Vanara −5.7** — real but attributed to AI-underdriven positional cards (21b showed ±18-pt pilot-quality swings on the Vanara kit); buffing to AI-parity risks human overshoot (the D1 lesson inverted) → **resolution DEFERRED TO LIVE TELEMETRY.**
    - **WAVE-1.1 QUEUE (ruled against real-player data, NOT further AI sim):** Setu Stones REDESIGN (seasonal queue, formalized) · Living Bridge uptime mechanic (6% uptime at mythic rarity) · Garuda cleanse re-theme (structurally-inert clause, Task-22 finding). **SURVIVING WATCH FLAGS → live telemetry:** Throne of the Second King (8.8%, ~3.1k stolen/5000 games, absorbed, round-scoped per R62-E) foremost; Mahishi (fixed, monitor); Vidyutastra/Chandrahas + Dawn Banner + Kalpavriksha composition lines (all BENIGN, watch-listed).
    - **RUNNER (`tools/sim_campaign.js`) — NO CHANGE NEEDED (verified, not edited):** its only hardcoded reference is `MATCHUPS.launch` = the flag-off LAUNCH baseline (49.8/53.6/…), which R82 keeps FROZEN and byte-identical. The wave baseline is documented here + in GDD_DELTAS; it gets wired into the runner (a second reference column) when wave-1.1 measurement actually begins — not required by the adoption itself.
    - **PERMANENT:** the flag-off LAUNCH BASELINE stays frozen and byte-identical — proven across all 27 tasks, NOT subject to this or any future adoption. Convergence ledger (public record): Asura/Vanara Δlaunch +13.0/−13.6 (T21) → +11.8/−13.5 (23) → +9.1/−10.4 (25) → +6.5/−8.2 (26) → **+3.8/−5.7 (27, adopted)**.
    - **ENGINE LANE STATUS: CLOSED** pending wave-1.1 (structural: Setu Stones redesign + Bridge uptime + Garuda re-theme, ruled on live telemetry) and the ship checklist. No further wave-1 balance tuning. **GATES:** src/engine.js + index.html UNMODIFIED (0 changed lines by task 28); scope = docs/GDD_DELTAS.md (R82 verbatim) + docs/rulings_R82.md (authority) + this CLAUDE entry; tools/sim_campaign.js NOT edited (no hardcoded wave reference existed to update); flag-off parity byte-identical (40.9/59.1); suites green (invariants · scenario 50 · venom 38 · story 48 · 0 console.log); runtime <10s.
- **✅ TASK D1 (opts.difficulty ∈ {beginner, advanced, deathmatch}) — DONE. Engine only (no UI, no META_CONFIG — that's D2).** Additive opt: **absent OR 'advanced' = the launch AI, byte-identical** (proven: the ENTIRE `node src/test.js` output is diff-identical between the committed pre-D1 engine and post-D1 — every matchup, every counter-card line; plus 200/200 + 100/100 game-fingerprint identity between absent and explicit `'advanced'`). Stored as **`pl.difficulty`** (per-player); `opts.difficulty` sets both sides, `opts.p0Difficulty`/`opts.p1Difficulty` override per side (for AI-vs-AI sims). Unknown/absent → `'advanced'`. **ALL difficulty randomness draws from `g.rng`** (seed discipline preserved).
  - **STEP 0 FINDINGS (reported before edits):** **(a) The AI selection surface is NOT a pure argmax** — `aiTakeTurn` first takes free Vanara Leaps (gain≥3), then `aiMove` runs: an R20 unbind branch, an opp-passed flip/concede block, then **strategic early-passes** (the card-advantage bluff `round1 && lead≥10 && fewer cards`; the Mahabali/deficit concede `lead≤-14`, or `-8` with Mahabali), and ONLY THEN the argmax over `aiScoreCard`. Targeting: `aiTakeTurn` passes `null` target → the engine auto-picks the **optimal** target (`targetSpec … ? find : reduce`), so both advanced AND a naive random-card AI get optimal aim. **(b) `aiMulliganPlan` is NOT invoked in PvAI sims** — the test.js harness just loops `aiTakeTurn` (no mulligan); the UI driver (index.html) DOES mulligan the AI in PvAI (`mulligan(G,1,aiMulliganPlan(G,1))`), story skips it. **(c) Round-concession logic EXISTS today** (the -14/-8 deficit pass + the bluff pass) → Death Match's concession is a **sharpening/addition on top**, not a from-scratch heuristic.
  - **ONE TUNING BLOCK (`DIFFICULTY`, just above `aiMove`):** `beginner {randomPlayP:0.5, autoMulligan:false, extraCard:0, concession:false}` · `advanced {0, false, 0, false}` (zero deviation) · `deathmatch {0, autoMulligan:true, extraCard:1, concession:true}`. `difficultyOf(pl)` resolves it (default advanced).
  - **BEGINNER:** an early return in `aiMove` — with **p=randomPlayP (g.rng)** play a **uniformly-random legal card**, else the argmax; **NO strategic early-pass** (skips the entire bluff/concede/coast structure — passes ONLY when out of legal cards); **skips the mulligan** (`aiMulliganPlan` returns `[]` for beginner, driver-safe). Two ADDED honest weaknesses (needed to clear the >65% bar — the single card-randomness lever plateaus ~60–65%): it also **MISAIMS** (a random legal target from `targetSpec.options` instead of null→optimal) and **MISPLACES** (a random slot for Vanara Units), and **skips beneficial Leaps**. All use only `targetSpec`'s public legal options — **no NEW hidden-information read** (the same surface the advanced AI reduces over).
  - **ADVANCED:** zero code-path deviation — structurally asserted (byte-identical to absent), demonstrated on Deva-vs-Naga digits + Vanara-vs-Asura with both sides explicitly `'advanced'`.
  - **DEATHMATCH:** **(1) mulligan GUARANTEED** — `newGame` auto-invokes `mulligan(g,pi,aiMulliganPlan(g,pi))` for a deathmatch player (driver-independent; a later UI re-call no-ops via `pl.mulliganed`). **(2) the 11th starting card** — drawn in `newGame` AFTER the normal 10 (and after the mulligan), `g.rng`-ordered (the deck is already shuffled; `deck.splice(0,1)` consumes no rng). **(3) a SHARPER round-concession** on top of the launch passes — **RULE (verbatim):** *in a non-final round we do not need to win, pass when the round deficit exceeds the AI's MAX remaining single-play swing (no one card can flip it) AND we hold ≥2 FEWER committed cards than the opponent (banking the surplus for a later round).* Uses only public state (board + visible card counts).
  - **HONEST-AI RULE (restated, holds at every tier):** no difficulty path reads hidden information — the AI never inspects the opponent's hand CONTENTS or deck order; it decides only from public board/round state + visible card COUNTS, exactly as the launch AI does. Beginner's random targeting/placement draws only from `targetSpec`'s already-public legal options; deathmatch's mulligan reads only the AI's OWN hand.
  - **PARITY (absent/advanced):** every difficulty branch is gated on `pl.difficulty` (beginner via `randomPlayP>0`, deathmatch via `autoMulligan`/`extraCard`/`concession`) → for absent/advanced NONE fire, NO extra rng is consumed. Proven: full suite green (invariants · scenario 50 · venom 38 · story 48 · **0 console.log**); **the ENTIRE test.js output is diff-identical pre/post D1** (all 10 matchups + counter lines); Deva-vs-Naga **40.9/59.1**; ASTRA_DMG unchanged `{Agneyastra, Lanka Dahan, Pashupatastra, Suryastra, Vidyutastra}`.
  - **THE DIFFERENTIAL (the feature's reason to exist) — measured on MIRROR matchups (same faction both sides → pure skill, no faction imbalance; 4 factions × 2 seatings × 150 = 1200 games/pair):** advanced beats beginner **65.3%** · deathmatch beats advanced **64.2%** · deathmatch beats beginner **79.0%** — a clean **monotonic ladder** (beginner ‹ advanced ‹ deathmatch), 11th card confirmed present. **⚠ MEASUREMENT NOTE:** mixed-faction duels read LOWER (~62%) because the large faction imbalance (Deva-vs-Naga is 41/59 at equal skill) partially cancels the skill edge — the mirror is the correct skill-isolated measurement. **⚠ LEVER NOTE:** card-choice randomness alone plateaus ~60–65% (the engine's decision surface is shallow — playing almost any card adds power); the misaim + misplace + leap-skip weaknesses were added to clear >65% honestly (no hidden info). Smoke: 800/800 terminated at EACH difficulty, 0 crashes, 0 bad-array. Scope: exactly this — no UI, no META_CONFIG.
- **✅ TASK D2 (difficulty — UI · persistence · rewards) — DONE. UI + META_CONFIG only; `src/engine.js` GIT-CLEAN (0 lines changed vs HEAD — the D1 opts are the whole interface; the double-mulligan needed NO engine change).** Suites green (invariants · scenario 50 · venom 38 · story 48 · Deva-vs-Naga **40.9/59.1**), index.html **0 console.log**, 0 browser console errors.
  - **THE SELECTOR (`#diffopts` on the faction-select screen — PvAI quickplay ONLY):** three styled buttons — **Beginner** "Learn the war" (green) / **Advanced** "Fight it" (gold) / **Death Match** "No mercy" (crimson). A permanent disclosure line (`#diffdisclose`) under the selector shows the selected tier's text; **Death Match's is the mandatory, permanent transparency line "The AI starts with an extra card. No mercy. Rewards ×1.5." (`.warn` crimson — not fine print).** Default = **Advanced**; the choice persists to `dy_meta.settings.difficulty` on click and is **preselected next match** (`showFactionSelect` reads it into `selectedDifficulty`, `renderDiffSelector`).
  - **ENGINE WIRING (the ONLY interface):** `startGame` passes **`p1Difficulty: selectedDifficulty`** to `newGame` — **only the AI (p1) takes the tier; the human (p0) stays advanced** (no self-handicap, no 11th card). Verified: beginner AI hand=10 (no extra card, `difficulty:'beginner'`); deathmatch AI hand=**11** + human=10.
  - **§2 REWARD MULTIPLIER (`META_CONFIG.DIFFICULTY_REWARD_MULT = {beginner:0.8, advanced:1.0, deathmatch:1.5}`):** applied at **ONE point** — a rewrite of `recordMatchResult`'s income block computes `mult` from `G.players[1].difficulty`, then grants **`Math.round(baseAmount × mult)`** for the four income streams (XP via `grantXp`, coins, Amsha, Sadhana via `sadhanaOnWin(M,mult)`). **NOT scaled:** level-up rewards (reconciled INSIDE `grantXp` at their own fixed rate), quest CLAIM rewards (panel), story (never reaches `recordMatchResult`). **PROVEN via the real path (3 tiers from an identical controlled meta, no level-crossing):** advanced 35xp/5c/10a/10s (×1.0) · beginner 28/4/8/8 = `round(base×0.8)` · deathmatch **53/8/15/15 = `round(base×1.5)`** — each stamped **exactly once**, no level-reward contamination (`no_level_contamination:true`). "no double-application" holds.
  - **§5 TELEMETRY:** the `lastEarn` stamp now carries **`difficulty` + `mult`** fields (one field each on the existing schema, no new pipeline) so future D2/D4 telemetry can segment by tier.
  - **§3 IN-MATCH LABEL (`#diffchip`, realm-chip pattern):** a subtle header chip beside `#realmchip` — 🌱 BEGINNER / ⚔ ADVANCED / 💀 DEATH MATCH, tier-colored — shown for PvAI (`applyDiffChip` in `startGame`), **hidden for story** (added to the hide-lists in `startStoryChapter`, `showStorySelect`, `showLanding`). Makes screenshots self-documenting for telemetry.
  - **§4 THE DOUBLE-MULLIGAN FIX (D1's flagged edge) — resolved WITHOUT an engine change:** deathmatch's `newGame` auto-mulligan sets `p1.mulliganed=true`; the engine `mulligan()` early-returns on `pl.mulliganed`, so the UI driver's later `mulligan(G,1,aiMulliganPlan(G,1))` at mullconfirm **NO-OPs** → **exactly one AI mulligan per deathmatch game.** Proven on a live deathmatch game: after `newGame` p1 hand=11 + `mulliganed=true`; the UI's second call returns `[]` and hand STAYS 11.
  - **PERSISTENCE (`dy_meta` schema → v5):** additive `settings.difficulty` (`freshMeta` + `loadMeta` v5 upgrade; a corrupt/unknown persisted value guards to 'advanced'). **Migration proven both ways:** a v4 store loads clean keeping xp(200)/coins(30)/sadhana(40) → schemaVersion 5 + default difficulty; the new field persists ('deathmatch' round-trips); corrupt value → 'advanced'.
  - **BROWSER GATE (390px, screenshots captured):** selector renders with all three tiers + Death Match disclosure/warn; a deathmatch match shows the 💀 chip + opponent **DECK 11 vs your DECK 12** (the 11th-card fingerprint) and the AI having already played from its 11-card hand; a beginner match shows the 🌱 chip + opponent DECK 12 (no extra card) + a visibly sloppy AI play (Tataka's removal into an empty board); the **shipped build reproduces the differential** (advanced beats beginner 60.5% in a 120-game mirror, consistent with D1's 65.3% at 1200 games); story select (Book of Order) has **no selector anywhere** and its launch passes NO difficulty (both sides 'advanced', chip hidden). Scope: exactly this — UI/persistence/rewards, engine untouched.
- **✅ TASK D3 (Death Match teeth) — DONE. Engine layer; opts unchanged; full parity ceremony.**
  - **⚠ MEASUREMENT-CONTEXT CORRECTION (lead):** D1's differential (deathmatch 64.2% over advanced) was measured **harness-advanced-with-NO-mulligan** vs deathmatch — INVALID for live, where the UI driver mulligans EVERY AI. **All D3 numbers use the LIVE-EQUIVALENT harness: BOTH sides mulliganed** (`for s in [0,1]: mulligan(g,s,aiMulliganPlan(g,s))` — advanced now mulligans, deathmatch's auto-mulligan no-ops the 2nd call, beginner returns [] by design). **STEP 0 honest live baseline (1200-game mirror ladder): deathmatch was only 57.8% over advanced** — the advanced mulligan neutralizes the raw card lead, exactly as the diagnosis predicted. Deathmatch had to earn its name in THAT setting.
  - **HONEST-AI RULE (restated, holds at every tier incl. the overlay):** no path reads hidden information — the AI never inspects the opponent's hand CONTENTS or deck order; every overlay lever reads only PUBLIC board state + visible hand/deck COUNTS.
  - **THE SCORING OVERLAY (`aiScoreCard` tail, gated `pl.difficulty==='deathmatch'` → advanced/beginner path BYTE-UNTOUCHED; all constants in `DIFFICULTY.deathmatch.overlay`):** **(a) SEQUENCING** — `seqEnabler:4` boosts Chandrahas/Blueprint (`DM_ENABLERS`) while Astras are in hand (land the engine-piece before the payoff); `seqSurgeHold:2` nudges a lone Kali to wait a beat while enablers/Astras queue. **(b) THREAT ANSWERS** — an "answer" card (`DM_ANSWERS`: vajra/gandiva/agneyastra/shaktispear/jatayu/mohanastra/sudarshana/vayavyastra/surpanakha/meghnad/tataka/vayu/ashvatara/nagapasha/worldcoil/vinatastalon/anjaneyaroar/naraka/sarpasatra) gains `threatScale:0.6 × best-enemy-effPower` (public board). **(c) HOLD DISCIPLINE** — if no enemy Unit reaches `holdThreshold:5` effPower, an answer is devalued `holdPenalty:3` (keep removal for real threats). **(d) ROUND ECONOMY** (in `aiMove`, `dmO`-gated): the launch concession sharpened to `concedeAt:-10` (from −14 — concede a lost non-final round earlier, bank the surplus) PLUS **THE BLEED** — when ahead by `bleedLead:8` on board AND holding MORE cards in a non-final round, pass to bank cards (force the opponent to over-spend or concede).
  - **THE 2ND DISCLOSED STRUCTURAL EDGE:** the 11th setup card STAYS, plus **the deathmatch AI draws +1 at the start of rounds 2 & 3** (`extraDraw:1` in `endRound`, deck-permitting; `deck.splice` consumes NO rng → advanced/absent byte-identical). Visible in the deck counter (self-documenting like the 11th card). **UI touch (the ONLY one, exact line changed — index.html `DIFF_META.deathmatch.disclose`):** now `'The AI starts with an extra card, draws deeper every round, and shows no mercy. Rewards ×1.5.'` (kept the D2 reward-transparency suffix).
  - **HONEST LADDER (live-equivalent, 1200 games/pair, WITH the teeth):** **deathmatch beats advanced 78.6%** (≥70% ✓, up from 57.8%), deathmatch beats beginner 91.8%, advanced beats beginner **68.8%** — **monotonic: beginner ‹ advanced (68.8%) ‹ deathmatch (78.6%)**. **BEGINNER RE-CHECK (measure only, no change):** 68.8% did NOT widen past ~70% → no softening; telemetry decides later.
  - **PARITY / ACCEPTANCE:** advanced's OWN strength UNTOUCHED — **the ENTIRE test.js output is diff-identical pre/post D3** (HEAD had batch-16, so the stash-diff isolates D3 exactly; test.js runs advanced games → the overlay does not leak), and **explicit 'advanced' is fingerprint-identical to absent** (250 mixed games, 0 mismatch). Deva-vs-Naga **40.9/59.1**; **ASTRA_DMG unchanged**; suites green (scenario 50 · venom 38 · story 48 · **0 console.log**); story isolation intact (story path passes no difficulty). **SMOKE (all crash-free, 0 bad-array):** launch-pool 10×50 at beginner/advanced/deathmatch (500/500 each), **wave-1-ON deathmatch 10×50 (500/500 — the overlay coexists with the 69-card wave pool)**, 200 deathmatch-vs-advanced live-equivalent (200/200). No new exports; opts unchanged. Scope: exactly the overlay + the extra-draw edge + the one disclosure string.

## FEATURE-COMPLETE CHECKLIST (core single-player game loop)
**Core loop is COMPLETE** — mulligan → faction-select → best-of-3 rounds under a Cosmic Realm → match result, with AI opponent and full event choreography.
- ✅ All 4 factions, 88 cards, every §9 cross-faction ruling (R1–R20)
- ✅ Best-of-3 round loop, round-lead (R2), between-round persistence
- ✅ Balance FROZEN for launch (LAUNCH BASELINE table; all counters fidelity-correct)
- ✅ Event stream + sequential UI choreography (rule #7), faction toasts, keyword tooltips, `prefers-reduced-motion` bypass
- ✅ **Mulligan (GDD §2.2)** — up to 3 pre-Round-1, tap-to-mark UI + AI heuristic + choreographed redraw
- ✅ **7 Cosmic Realms (GDD §10)** — `g.realm` engine modifier at chokepoints; match banner + tappable header chip; Mrityulok regression byte-identical to baseline, Swarga/Patala swing measured
- ✅ **Per-realm battle boards — COMPLETE, 7/7 realms mapped (UI only, engine untouched, baseline byte-identical, 38 tests green)** — `REALM_BOARDS` maps all seven `realm id → { img, overlay, ambience? }`: `mrityulok`→`board_bg.jpg`, `patala`→`board_patala.jpg`, `swarga`→`board_swarga.jpg`, `kalki`→`board_kalki.jpg`, `yaksha`→`board_yaksha.jpg`, `rishi`→`board_rishi.jpg`, `gandharva`→`board_gandharva.jpg`; `BOARD_FALLBACK` (board_bg.jpg) covers any unmapped realm; each entry has a commented `ambience` slot RESERVED for a future per-realm audio track (not implemented). **⚠ ASSET PENDING: `board_gandharva.jpg` is not yet in `assets/img/`** — its map entry is wired and ready, but until the file lands a Gandharva match 404s → gracefully falls back to `board_bg` (`#realmboard` opacity 0). The other six board images are present and verified. `applyRealmBoard(G.realm)` runs at match start (`startGame`): sets the per-realm `--board-overlay` strength on `#field::after` and, for a non-fallback realm, **lazy-loads only that realm's image** into `#realmboard` (a z-0 layer above the base `#field::before` board_bg, below the overlay), crossfading opacity 0→1 on load. Until it loads — or if it 404s — the base board_bg shows (no flash). `boardImgLoaded` caches per session (a rematch in the same realm refetches 0). **Per-realm overlay** for readability: base/Mrityulok `.30`, Patala/Kalki/Rishi `.32`, Yaksha `.33` (dark ash/cavern/temple floors), **Gandharva `.40`** (light tan stage) and **Swarga `.44`** (brightest grey-marble board) — the two bright boards deepened so power numbers + Venom badges read over them. Verified at 390px with a full board, chip agreeing, power/Venom badges legible on every present floor: Patala fetch=1 / rematch=0; Swarga (.44), Kalki, Yaksha (.33), Rishi each fetch=1; Gandharva fetch=1 but 404 → fallback to board_bg (opacity 0) since its asset is pending. **NOTE:** `applyRealmBoard` swaps the board only; the header realm-chip label comes from `showRealmIntro` at real match start (they agree in real play — a dev force-swap of `G.realm` alone leaves the chip stale).

- ✅ **Dharma Shield player-designation** — human designates by hand (🛡 SHIELD button, sticky, Kavacha=2, no auto-fallback); AI unchanged, harness byte-identical
- ✅ **Game-feel pass (UI/audio only, engine untouched → harness byte-identical)** — AI think-time 1.2–3.5s scaled by decision weight + "thinking" indicator; opponent-card **showcase** (large center reveal 1.4s) → resolve → **result summary** (plain-language, from event `text`); pacing retune (callout 700ms / stagger 280ms / destroy 600ms dwell; significance & round-end theatre with total count-up); **Settings** (gear): Relaxed/Normal/Fast + music/SFX/volume, localStorage-persisted; tap-to-skip in all modes; reduced-motion keeps audio.
- ✅ **Board VFX** — destroy crack + faction-colored shards, damage impact flash + heavy arc numeral, buff rising ring, venom drip + wet badge, Mythic/Legendary screen-shake + board color-wash + showcase shimmer + low boom, shield gold ring, Leap arc. All transform/opacity, ≤1 shake/sec, through the choreography queue.
- ✅ **Audio: real assets (3rd pass, `assets/audio/`)** — **synthesized music fully retired.** Two-track **gapless file music** (`music_menu` on faction-select, crossfade ~1.5s to `music_battle` on match start, back to menu on return) — decoded via `decodeAudioData` into a looping `AudioBufferSourceNode` (NOT `<audio loop>`, which has an MP3 seam gap); default **ON**, routed through the music-volume/mute. **Astra impacts are files** — every Astra resolution plays `sfx_astra` (Brahmastra → `sfx_brahmastra`) synced with the screen-shake, on a non-limited SFX bus (files are pre-normalized: music −20 LUFS, astra −13, brahmastra −11 — **no per-sound gain compensation**), ducking music −4dB/1.5s. All **other SFX stay synthesized** (through the limiter). **Separate SFX-volume slider (80%)** + **▶ Test all SFX** (previews the two files too). Showcase-clipping fix retained (height-driven width + portrait `750/1050` art).
- **Menu background** — `assets/img/menu_bg.jpg` on the faction-select screen: **frameless** (no modal panel — selection sits directly on the image), image shown at ~90% brightness (no blur/desat), soft bottom gradient; each faction tile + BEGIN get a `rgba(0,0,0,.45)` + backdrop-blur scrim and text-shadows for legibility over the sunburst. Faction grid is **4-col ≥480px, 2×2 below** (no portrait overflow).
- **Pacing/audio device fixes (real-device pass)** — music track bug fixed (faction-select plays `music_menu` 33.0s, match plays `music_battle` 11.4s; the manager is strictly gesture-driven — no pre-gesture AudioContext). Choreography baseline slowed 30% via a single **`CHOREO_SPEED=1.3`** constant applied in `cDelay` (× the user Relaxed/Normal/Fast setting) + the result-summary hold; SFX trigger times shift with the steps but sound playback is untouched.
- **Battle board background** — `assets/img/board_bg.jpg` on `#field` (`::before`, center/cover so the mandala lands on the divider; `::after` ~30% dark overlay). Each card `.row:not(.empty)` gets a barely-there `rgba(4,2,1,.24)` + backdrop-blur scrim so minis/power/Venom badges stay readable over the stone — arena reads through. Verified: teal Naga minis + Venom stacks over the mandala at 390px & desktop.
- **Unit-clash SFX + match-end silence** — Unit/Hero placements play `sfx_unit_clash` (file, SFX bus) on the SAME `play` event as the landing dust/ember VFX (one beat); each call is a fresh buffer source so back-to-back placements OVERLAP, not cut off. At match end (`showGameOver`) `Audio2.stopMusic(2)` fades `music_battle` to SILENCE (no crossfade) so the result screen has room; `music_menu` resumes only on return to faction-select. `stopMusic` never silences the menu track (re-entry-safe).
- **Full file-SFX set (completes card-play retirement + event stingers)** — 11 new pre-normalized files decoded into the file-SFX buffer set on the existing bus (fresh `AudioBufferSourceNode` per trigger → overlaps layer); engine untouched, baseline byte-identical. **Card-play (synth `cardplay` fully retired):** `cardPlaySound(c)` routes Astra→`astraSfx` · Unit/Hero→`clashSfx` · **Mantra→`sfx_mantra` (om chant) EXCEPT Gayatri Mantra + Pavamana → `sfx_heal`** (Deva restoration; the dark revivals Sanjivani Corruption + Mrityunjaya deliberately stay on the om chant) · Artifact→`sfx_artifact`. Used in both the choreographed play branch and the reduced-motion audio path. **Events** (`resolveSound(ev)` → `{file}` or `{synth}`; each file replaces its synth counterpart, all other synth SFX stay): Venom Token APPLIED (`token`)→`sfx_venom_apply` (the per-round drain tick `venom` stays synth & quiet) · any Unit destroyed (`destroy`)→`sfx_unit_destroy` (chained destroys overlap) · Leap toast→`sfx_leap` · Dharma Shield applied (`shield` event)→`sfx_shield_apply` (its accompanying `Dharma Shield` toast is suppressed to avoid a double) · Chaos Surge toast→`sfx_chaos_surge`. **Shield-break:** the engine models Dharma Shield as astra-target-prevention (no dedicated break event), so `sfx_shield_break` is wired to the `block` event — a protective effect absorbing an astra (Hero immunity / floor-at-1 / Rama's Signet), the engine's "protection consumed" signal. **Result stingers:** on the result reveal, over the music fade-out, match win→`sfx_victory`, loss→`sfx_defeat` (stalemate keeps synth). **Test-SFX = 30** (14 synth incl. the now-unused `cardplay` def + 16 files). **Dev "Test VFX" button** next to Test-SFX in Settings — fires `cardLand` → `astraBurst` → full Mythic lightning at board center 1.5s apart (needs an active match for `#field` size) so effects can be reviewed on a real device without engineering a Brahmastra. Added page-weight of the 11 new files ≈ 648 KB; total audio assets ≈ 1.61 MB. (Loudness/mix and loop-seam not verifiable headless — all 16 files fetch 200 + decode; a real device is needed to judge the mix.)
- **Landing page (`#landing`) — the app's first screen, before faction-select** — UI layer only, engine untouched, baseline byte-identical, 38 tests green. Frameless treatment shared with faction-select (`#factionselect, #landing` reuse the `menu_bg.jpg` cover + darken + bottom-gradient), mobile-first 520px column, every panel gets the `rgba` dark + `backdrop-blur` scrim so the art reads through. Content: settings gear top-right (`#landgear` → opens the existing `#settings`, which was bumped to `z-index:90` so it sits above the landing; `.mutetoast` → 95); title block (Devanagari `दिव्य युद्ध` gold / `DIVYA YUDDHA` Cinzel wide-tracked / `BATTLE OF THE CELESTIALS` muted); a **profile card**; and four stacked mode buttons. **Mode buttons:** `Player vs AI` and `Faction Introduction` are ACTIVE (gold border + tint + `›` chevron → `showFactionSelect()` / `showFactionIntro()`); `Story Mode — Divya Yatra` and `Multiplayer` stay **locked** (~55% opacity, inline-SVG padlock, not navigable — a delegated handler shows a `Coming soon` toast). No emoji icons. **Flow:** boot is now `showLanding()` (was `showFactionSelect()`); Landing → PvAI → faction-select (unchanged) → match; the post-match return path (gameover **MAIN MENU** button, formerly "CHANGE FACTIONS") now calls `showLanding()` with fresh stats — faction-select keeps its own BEGIN flow. **Music:** `showLanding()` calls `Audio2.playTrack('menu')`, so menu music covers landing + faction-select (still gesture-driven — first tap starts it); the battle crossfade + match-end `stopMusic` are unchanged. Verified full cycle: landing `menu` → faction-select `menu` → BEGIN `battle` → match-end `stopMusic` (silence) → MAIN MENU `menu` resumes.
- **Player profile + persistent stats (`localStorage` key `dy_profile`, single JSON blob)** — `{name, matches, wins, winsDay, winsToday}`. Name defaults to `Warrior`, tap-to-edit inline (Enter/blur commits, Escape cancels, ≤16 chars) → persists; avatar circle shows the initials. **Counters** increment in `recordMatchResult()`, called from `showGameOver()` and guarded by a one-shot `matchRecorded` flag (re-armed in `startGame()`) because `render()` re-calls `showGameOver()` on every re-render while `G.over` — so each match counts once: `matches` always +1, `wins`/`winsToday` +1 only when the human (p0) wins. `winsToday` is day-keyed (`todayKey()`) and resets on date change. Verified in-browser: name edit persists across reload; a win → `1/1/1`, double `showGameOver` does NOT double-count, a loss → `matches 2, wins 1, today 1`. **UPDATE (M1 slice 1):** the provisional level formula `1 + floor(wins/5)` is **RETIRED** — Level + the XP bar now derive from `dy_meta.xp.total` (see the M1 bullet below). The `matches`/`wins`/`winsToday` counters stay as the **matches-per-day telemetry feeding the Amsha economy** and as the **rank source of truth** (rank derives from `dy_profile.wins`).
- **M1 META-FOUNDATION — SLICE 1: store · XP/levels · ranks (UI/data only, engine untouched, baseline byte-identical; test.js invariants + baseline / scenario 50 / venom 38 / story 48 green; index.html 0 console.log).** Spec = `docs/M1_DESIGN.md`. **⚠ DOC-INTEGRITY NOTE:** the committed `M1_DESIGN.md` is still headed **v0.1 with decisions D1–D6 shown OPEN**, even though the commit that added it (`45c476e`) is titled "M1 design v0.2: all decisions ruled" — the file body and the commit message disagree (working tree clean). The build followed the **task-ruled values** (which pin the slice-1 numbers); reconcile the doc to v0.2 when convenient.
  - **`META_CONFIG` (single tunable object, all of §1–§6 even unused-this-slice):** XP awards (match 20 · win +15 · story first-clear 60 · bonus first-earn 40 · daily-first +25), curve `100+25×(N−1)` (`levelCost(n)`; L1..L10 sum = 2,125 ✓ doc), the 9-rank ladder (Seeker 0 → Disciple 10 → Adept 30 → Rishi 60 → Maharishi 100 → Devarishi 150 → Brahmarishi 220 → Avatara 300 → Parashakti 400), coins (perMatch 5 [slice-5 hook], **perLevel 25 — ⚠ ASSUMPTION: §1 says "coins at every level" with no amount; tunable**), Amsha (perWin 10 / firstWinOfDay 20 [slice-5 hooks], **milestone 60 every 5 levels [WIRED]**, storyBonusRetro 15 [slice-5], costs C40/U80/R160/E400/L1000/M1500), quests (daily 15c/10a/20xp ×3 · weekly 60c/50a/100xp ×2 + pools) and sadhana (10/win) — both config-only, slices 4–5.
  - **`dy_meta` store (§7 — THIS SCHEMA IS THE FUTURE M3 SERVER PROFILE):** one JSON-only, additively-schema-versioned blob (no functions/Dates/Maps). Fields: `schemaVersion` (bump+migrate in `loadMeta`); `xp.total` (lifetime XP — **level + bar are PURE DERIVATIONS, level is never stored**); `wallet.{coins,amsha}` (accrue now, spend UI is a later slice); `rewards.lastLevelRewarded` (idempotent level-up payout cursor, **init 1** = no reward for the birth level); `daily.{day,firstMatchAwarded}` (day-keyed +25 guard, same `todayKey()` precedent as `winsToday`); `story.{clearXp,bonusXp}` (per-chapter one-shot XP ledgers — **survive a `dy_story` reset**, so XP is play-once). **Rank derives from `dy_profile.wins`**; at M3, `dy_profile` (identity + win counts) and `dy_meta` (progression + wallet) **merge into one server row**.
  - **Derivations:** `levelFromXp(total)`→`{level,intoLevel,toNext}` (subtract `levelCost` per level); `rankForWins(wins)` (highest ladder threshold ≤ wins); `reconcileLevelRewards(M)` pays `coins.perLevel` per newly-crossed level + `milestoneAmount` Amsha when the level %5===0; `grantXp(M,amt)` is the single XP entry point (adds then reconciles).
  - **Hooks (engine untouched — read match result / story-completion only):** `recordMatchResult()` (PvAI match-result hook) grants match 20 + win 15 + daily-first 25; **story matches are NOT double-awarded** the 20/15 (they route through `showStoryResult`, not `recordMatchResult`) — story XP comes only from `markChapterComplete()` (first-clear 60 + first-bonus 40, ledgered so **replays grant nothing**).
  - **MIGRATION (runs once, on first `dy_meta` creation):** `retroXP = matches×20 + wins×15` from existing `dy_profile`, then back-pays the level-up rewards those levels earned — **no playtester resets to zero** (one-time story/daily bonuses can't be reconstructed, intentionally omitted). Verified: veteran 50 matches / 30 wins → **Level 8** (1,450 XP), wallet **175 coins + 60 Amsha**, rank **Adept**; idempotent on re-load.
  - **UI:** profile card gains a gold **rank chip** (`.lp-rank`, Cinzel small-caps, `--gold`/`--gold-dim`) in a `.lp-namerow` beside the name (name-edit's `replaceWith` on `#lpname` leaves the chip intact); Level label + XP bar now real. **Verified at 390px:** fresh → Level 1 / Seeker / 0%; rank boundaries 9→Seeker, 10/11→Disciple, 30→Adept, 400→Parashakti; daily-first fires once/day, re-arms on day change; match win → +60 XP (first of day), re-entry guard no double-count, loss → +45; story clear+bonus → +100, replay idempotent; screenshot shows the L8/Adept card. **Slice scope:** only level-up rewards accrue to the wallet this slice — per-match/win Amsha+coins income, quests, Sadhana, and the wallet display + spend UI are slices 2/4/5 (config present, not wired).
- **M1 SLICE 2 — QUEST ENGINE + PANEL (UI/data only, engine untouched, baseline byte-identical; test.js invariants + baseline / scenario 50 / venom 38 / story 48 green; index.html 0 console.log).** Spec = `docs/M1_DESIGN.md` **v0.2 (RULED)** §3 (the doc has since been reconciled from the v0.1/commit-message mismatch flagged in slice 1 — D1–D6 all ruled).
  - **`dy_meta` schema → v2** (additive): `quests:{ day, week, daily[], weekly[] }`. `loadMeta` bumps v1→v2 and `ensureQuests()` fills it — **a slice-1 store keeps its xp/wallet/rewards** (verified: v1 blob → v2 keeps 1450 XP + 175c/60a, gains quests). Each stored quest instance is minimal: `{id, prog, seen?(set-quests), claimed}` — label/target/reward/measure all live in config, not the store.
  - **Draw/reset:** `ensureQuests(M)` redraws **3 daily** on `todayKey()` rollover and **2 weekly** on `isoWeekKey()` rollover (ISO-8601 week, Mon–Sun; UI-layer `new Date()` is fine — the `Date`/`Math.random` ban is ENGINE-only). Draw = `Math.random` pick from the §3 pools (distinct). loadMeta persists when a draw happens. **Weekly persists across a day boundary** (same ISO week → weekly untouched + progress kept while dailies redraw — verified).
  - **`QUEST_DEFS` (the §3 pool, RULED):** daily `win1·play2·winby8·play6units·faction3·herosurvives·venom3·storychapter`; weekly `win7·twofactions·anystar·threerealms·destroy10`. Each = `{scope,domain,kind:'count'|'set',target,label,measure(ctx)}`. Rewards from `META_CONFIG.quests[scope]` (daily 15c/10a/20xp · weekly 60c/50a/100xp).
  - **Predicates reuse the STORY_PREDICATES machinery (not forked):** pure functions of final `g` + a match `ctx` (uid→{side,card} index over all zones + won/story/bonus/faction/realm). `count` quests add a per-match number; `set` quests union distinct keys (`twofactions`/`threerealms`). **Faction-mechanic (`faction3`) resolves per chosen faction** off the event stream — Devas=`shield` on own unit · Vanaras=`buff`/Leap by side-0 · Asuras=`buff`/Chaos Surge on own unit · Nagas=`token` applied to enemy (verified 3/2/2/3 respectively). **`venom3`** = Venom Tokens applied to enemy + the player's Pavamana casts — **⚠ APPROXIMATION:** per-token cleanse emits no event, so one Pavamana cast counts as 1 cleanse (the closest the stream supports; documented, tunable).
  - **Domain gating (isolation + anti-farm):** `pvai`-domain quests ignore story matches; `story`-domain quests (`storychapter`,`anystar`) only progress on story chapters — and **count REPLAYS** (the sanctioned replay-income ruling: story matches are excluded from PvAI telemetry but a cleared/replayed chapter is legitimate quest income). Verified: a story win moves only storychapter/anystar; a PvAI win never touches them.
  - **Hooks (one-shot-guarded by their callers):** `recordMatchResult()` → `questsOnMatchEnd(G,false,false)` (PvAI); `showStoryResult()` win branch → `questsOnMatchEnd(G,true,bonus)` (story). Claims are **manual** — `claimQuest(scope,id)` guards against early/double claim, pays coins+Amsha into `wallet` and XP via `grantXp` (so a claim can trigger a level-up reward), marks `claimed`.
  - **UI:** a **Quests button** on the landing (below the profile card) with a gold **claimable-count badge** (`#lpq-badge`, hidden at 0; sub-label flips to "N ready to claim"); `updateQuestBadge()` runs in `showLanding`. The **`#quests` panel** (shares the frameless `menu_bg` treatment) renders DAILY/WEEKLY sections, each row = label + gold progress bar + `prog/target` + reward line, with a **CLAIM** button on completed-unclaimed rows (✓ when claimed, dimmed row). **Verified at 390px:** full day cycle (draw → a **real PvAI match** advanced `play2` 0→1 through the genuine `showGameOver→recordMatchResult→questsOnMatchEnd` path → claim → wallet 0→15c/10a + XP+20) · reset redraws · weekly persists across a day · every measure exact (win1/play2/winby8/play6units/herosurvives/destroy10/set-quests) · faction-mechanic per faction · no double-claim · badge clears on claim; panel screenshot captured. **Slice scope:** Amsha/coins *income from wins* (perWin 10 / firstWinOfDay 20), Sadhana, the retro story-star Amsha, and the wallet **display + spend UI** remain slices 4/5 — only quest claims + level-ups move the wallet today.
- **M1 SLICE 3 — MATCH INCOME · WALLET DISPLAY · SADHANA · STORY-STAR RETRO (UI/data only, engine untouched, baseline byte-identical; test.js invariants + baseline / scenario 50 / venom 38 / story 48 green; index.html 0 console.log).** Spec = `docs/M1_DESIGN.md` v0.2 §5–§6.
  - **`dy_meta` schema → v3** (additive): `sadhana:{pin,banked}`, `story.bonusAmsha:{}` (retro ledger), `daily.firstWinAwarded` (own day-key guard). `loadMeta` upgrades v1/v2→v3 keeping xp/wallet/quests (verified: a v2 store keeps 200 XP + 30 coins + its quests). The retro sweep is gated `prevVer<3 || created` so it's one-shot, and idempotent regardless (ledgered).
  - **§5 MATCH INCOME (PvAI only — story never reaches `recordMatchResult`):** wired beside the XP awards — **5 coins/match**, **10 Amsha/win**, **+20 Amsha on the first WIN of the day** (guarded by `daily.firstWinAwarded`, **distinct from the XP daily-first-MATCH `firstMatchAwarded`**; both re-arm on day rollover). Verified independence: on a fresh day a LOSS consumes the +25 XP daily-first but leaves the Amsha first-win intact, so the next WIN still collects +20. All income is one-shot via the existing `matchRecorded` guard (re-entry no-ops).
  - **§5 STORY-STAR RETRO (15 Amsha/star):** `reconcileStoryStars(M)` pays 15 Amsha for every already-earned `dy_story` bonus star not in `story.bonusAmsha` (idempotent) — runs on the v3 upgrade so **no earned star goes unpaid** (verified: 3 earned stars → +45 Amsha, idempotent on reload). Future first-earns pay via `grantStarAmsha(M,id)` in `markChapterComplete` — **same ledger**, so a star never double-pays (retro vs first-earn).
  - **§6 SADHANA (Option A — parallel win-fed stream):** `+10` banked progress per **PvAI win** toward a pinned target; **banked persists per-id across pin-switches** (verified: pin A→20, switch to B→10 while A stays 20, re-pin A resumes at 20), **one active pin**, caps at the target. **Pin pool = per-faction-wave** (`SADHANA_POOL`, 4 entries) because the "revealed-but-unowned pool" is today the anonymous Faction-Intro hidden-slot teasers (no card identity yet); **when real wave-1 cards ship the pool becomes the card list and `banked` (keyed by id) carries over.** Target = `META_CONFIG.sadhana.previewCost` (160, the Rare cost — TUNABLE placeholder); **the grant cannot fire yet** — a filled bar reads "Ready — arrives with the next wave".
  - **UI (profile card):** a **wallet row** — coins `◉` + Amsha `✦` (Amsha in the brighter `--hi` gold + its own glyph → visually distinct), both accruing since slice 1, visible now; and a **Sadhana row** (label + gold progress bar + `banked / cost ✦` note) that opens a **pick overlay** (`#sadhanapick`, 4 faction-colored options showing each id's banked progress, current pin highlighted). **Verified at 390px:** a won match moves coins+Amsha+Sadhana together exactly once; first-win-of-day Amsha resets independently of the XP daily-first; retro correct (3 stars → 45, idempotent); pin-switch banks; picker pins + updates label/bar; full-bar "arrives with the next wave"; profile screenshot with `◉145 · ✦90` + Sadhana bar. **Slice scope:** the Amsha **spend UI** (buying wave cards) and the actual Sadhana **grant** stay M4/wave-1-gated — this slice completes all *income + display*.
- **M1 SLICE 4 — EARNING-FEEL POLISH: progress shown at the moment it's earned (UI only, engine untouched, baseline byte-identical; test.js invariants + baseline / scenario 50 / venom 38 / story 48 green; index.html 0 console.log).**
  - **`dy_meta` schema → v4** (additive): `rewards.rankSeen` (last rank acknowledged) + `rewards.rankGlintPending` (rank to glint on next landing). `loadMeta` v4 upgrade initializes `rankSeen=rankForWins(dy_profile.wins)` for existing stores so **a migrated veteran gets no spurious promotion note**; `migrateFromProfile` does the same for fresh creates.
  - **THE STAMP READS THE ACTUAL AWARDS, never recomputes:** `recordMatchResult` snapshots wallet/xp/level/sadhana/claimable-count BEFORE its grants and diffs AFTER (post-`questsOnMatchEnd`), stashing `lastEarn={xp,coins,amsha,sadhana,leveledTo,rankTo,questCompleted}`. `showGameOver` renders it via `renderEarnStamp($go-earn,$go-extra,lastEarn)` — verified the stamp numbers equal the wallet delta EXACTLY (first-win-of-day → `+60 XP · ◉5 · ✦30 · Sadhana +10`; loss → `+20 XP · ◉5`). A level-up that crosses a milestone correctly folds the level-reward coins/Amsha into the delta (honest, not double-counted). One-shot via `goStampDone` (re-armed in `startGame`), so re-renders don't restamp/replay SFX.
  - **CHAPTER-CLEARED EARNINGS (item 2):** `markChapterComplete` stashes `lastStoryEarn` = only what was NEWLY granted (first-clear 60 / bonus 40 XP + 15 ✦), diffed against its own ledgers → **a replay that earned nothing shows nothing** (`sr-earn` empty). `showStoryResult` clears it each result; `storyResultPanel` renders it only on a win with a non-zero delta.
  - **LEVEL-UP MOMENT (item 3):** a `LEVEL N` flourish (`.earn-level`, bright `--hi` gold, pop-in) on whichever result surface caused it — match result, **chapter screen**, or the **Quests panel** (a claim-driven level-up renders `#q-flourish`). Reuses the existing **`sfx_buff`** blip (tier reused, no new sound). The profile XP bar **rolls over** on next render (`levelRolloverPending` → fill to 100% → snap to 0 → animate to the new fill) instead of snapping backward.
  - **RANK PROMOTION (item 4):** on a win crossing a threshold, `lastEarn.rankTo` drives a `Rank attained: ADEPT` line (once — `rankSeen` advances same-match so the 11th win shows nothing), and `rankGlintPending` makes the profile **rank chip shimmer once** on the next landing (`maybeGlintRank` adds `.glint`, then clears the persisted flag → never repeats). Verified: synthetic 9-win profile → 10th win promotes to Disciple once, 11th shows nothing.
  - **QUEST-COMPLETION TICK (item 5):** if a match flipped any quest to claimable (`claimableCount` after > before), one `Quest complete — claim in Quests` line — completion only, no per-progress spam. Verified present on completion, absent when nothing newly completed.
  - **Verified at 390px** (screens captured): the full stamp, the LEVEL 5 flourish + quest line, and the `Rank attained: DISCIPLE` promotion; loss shows XP+coins only; chapter first-clear `+100 XP · ✦15` vs replay empty; glint fires once then clears. **Headless caveat:** the `sfx_buff` playback itself isn't audible headless — the code path fires once (guarded); a real device confirms the sound.
- **Faction Introduction screen (`#factionintro`)** — UI layer only, engine untouched, baseline byte-identical, 38 tests green. Reached from the now-ACTIVE landing button (`showFactionIntro()`); back arrow → `showLanding()`; `music_menu` plays throughout. Frameless `menu_bg.jpg` treatment (added to the `#factionselect, #landing, #factionintro` selectors; their `::before/::after` are now `position:fixed` so the bg holds while the page scrolls). **Six tabs**, `How to win` first/default, then Devas/Asuras/Vanaras/Nagas/**Realms** (horizontally-scrollable tab strip; landing button caption is now "Meet the armies and the realms"). **Faction tab** = identity header (name in faction color, tagline, difficulty + playstyle badges) → mechanic box → strengths/weaknesses as two scrim panels (green `--green-dim` / red `--crimson-dim` borders) → the 22-card gallery (4-col thumbnail grid) → 2 strategy strips → matchup note (Deva & Naga tabs only). **Content authority:** all prose is verbatim from `docs/faction_intro_content.md` (transcribed into `FI_CONTENT`); every card name/power/effect shown is pulled LIVE from the engine (`DECKS`, `RARITY_NAME`) — the inspect view's text panel renders the card's real `txt`. **Gallery order:** Heroes → Units by power desc → Astras → Mantras → Artifacts. **Future-wave teaser:** after the 22 real thumbnails the grid appends **`FI_HIDDEN` (=22) locked slots** (LIVE_GAME_DESIGN seasonal waves) — pure CSS, ZERO network (no `<img>`): dark scrim, faction-color border @30% (`${hex}4d`) + a centered dim `?` glyph @35% (`${hex}59`), all identical and visually quieter than real cards; tapping one toasts `The realms have not revealed this card yet.` (no inspect/nav). The gallery header is data-driven: `CARDS — ${realCount} OF ${realCount+FI_HIDDEN} REVEALED` (real = engine deck length, so wave releases update it automatically → currently 22 OF 44). Lazy-load fetch count is unchanged (still exactly 22 thumbnails per tab). **Performance:** each tab's DOM (and thus its 22 `<img>` thumbnails) is built only on FIRST open and cached in `fiTabNodes` (verified: opening a tab loads only that faction's thumbs, re-opening fetches 0; never preloaded). Tapping a thumbnail opens a full-screen inspect (`#fi-inspect`, z-96) that loads the full `assets/cards/` frame on demand. **Inspect navigation (`fiOpenInspect`/`fiNav`/`fiShowCard`):** once open, the player moves through all 22 revealed cards of the current faction **in gallery order** (hidden slots skipped) via edge **‹ › arrows** (44px, same scrim+gold as back buttons, `position:fixed` vcentered, may overlay the card edge with a scrim behind), horizontal **swipe** (≥50px, vertical ignored — `fiSwiped` flag also swallows the trailing click so a swipe never closes/double-navs), and **ArrowLeft/Right** (Escape closes). **Wrap-around** both ends. A `N / 22` counter sits above the card, updating every move. Entry point sets the start index (gallery thumb → its index; strip thumb → `findIndex` by id). **Loading:** frame + engine-text panel swap together; spinner only when `!fiLoadedFrames.has(src)`; the two neighbors (N±1) are preloaded via off-DOM `fiFrameCache` Images so browsing feels instant — **preloads are lazy (only ±1, never the whole faction)**; verified opening card 5 fetches exactly frames 4/5/6. **Close** only on a direct backdrop tap (`e.target===ov`); the card taps to next, arrows/text/counter are dead-zones so closing isn't accidental mid-browse. **Strategy strips (`FI_STRIPS`):** horizontal step cards with a real card THUMBNAIL (tap→inspect), gold step label + one-line caption, gold `→` arrows, a RESULT panel; universal How-to-win diagrams (round-economy pip bars, when-to-pass icon rows) are pure HTML/CSS. Strips fit the column (no horizontal scroll). Thumbnails come from `assets/thumbs/` (see repo-layout; run `scripts/make_thumbs.sh`). **Content = `faction_intro_content.md` v1.1 (corrected to engine truth).** The two v1.0 PDF-era card references have been fixed against the engine (the engine was right): (1) **"Indra's Net"** never existed as a card — Deva diagram D2 is now **"Two Shields, Not One"** built on **Dharma Kavacha** (`PASSIVE: Dharma Shield protects TWO Units instead of one`), and the Deva strengths bullet is now "Doubled shielding — Dharma Kavacha protects two units at once". (2) **Rama Naam** is **+2** (not +3), and **Hanuman does not amplify Rama Naam** — its passive is `Each Vanara Unit of printed power 4+ you play gains +1 on entry`, so the V1 strip's Step 2 caption now states that, and the Vanara strengths spike bullet reads "(Rama Naam: +2 to your whole Vanara board)". No card fact is hand-typed; the inspect panel still renders live engine `txt`.
- **Realms tab (`fiRenderRealms`, 6th tab) — UI only, engine untouched, baseline byte-identical, 38 tests green.** Prose verbatim from `docs/realm_intro_content.md` (`FI_REALM_CONTENT` + `FI_REALM_INTRO`); **the effect line is `REALM_INFO[id].fx` rendered at RUNTIME, never hand-typed** (programmatically verified equal for all 7). Shows the tab intro line, then all 7 realms in engine `REALMS` order — each: **vertical board banner** (the whole board UNCROPPED at its native portrait aspect, ~60% column width / ≤230px, centered — NO fixed-height crop; `assets/thumbs/board_<id>.jpg` is 320px-wide q50 ~25–38 KB so it stays crisp at that display size) in a **CSS-only double-rule picture frame** driven by an inline `--ra:<accent hex>` (from `REALM_ACCENT`): outer 1px rule `color-mix(--ra 50%)` → 4px near-black mat (the banner's `padding` over `rgba(4,3,1,.6)`) → inner 1px rule `color-mix(--ra 30%)` on a `.fi-realm-inner` wrapper, plus 4 L-shaped corner ticks (`::before`, eight `linear-gradient` segments at `--ra 70%`). `box-sizing:border-box` keeps the padding/borders from changing the banner's outer width, so the tap target is unshifted. Tap → the full board image in the inspect view, name + epithet in the realm's accent color (`REALM_ACCENT`, from the pack header), a REALM EFFECT box (runtime `fx`), Thrives-here/Suffers-here twin panels (reusing `.fi-strength`/`.fi-weak`), and a TIP callout. Lazy like the faction tabs: DOM + the **7 board thumbs load only on first open, 0 on re-open** (`fiTabNodes` cache). **Board inspect:** the card-inspect component was generalized with a `mode` (`'card'`|`'board'`) — `fiOpenBoardInspect(idx)` reuses the same overlay/arrows/swipe/keyboard/close and **navigates between the 7 realm boards** (`N / 7` counter, wrap-around); the full board image (`assets/img/board_<id>.jpg`) is lazy-loaded on demand with the same ±1 neighbor preload. Board thumbnails come from the 2nd step of `scripts/make_thumbs.sh`.
- **Story Mode — Divya Yatra (Book of Order): ch1–5 playable (UI driver; engine untouched beyond the `opts.scenario` hard-error one-liner; baseline byte-identical).** Landing "Story Mode" button is ACTIVE → `showStorySelect()` (Book of Order: 7 chapter cards, **ch1–5 playable, ch6–7 `LOCKED_STUBS`** "The churning continues soon"; **sequential gating** — each unlocks when the previous is done; `dy_story` localStorage holds `{done,bonus}` per chapter). **Story matches never touch PvAI telemetry** (`showGameOver` early-returns to `showStoryResult` before `recordMatchResult`; `Story` also sets `matchRecorded=true`). Data in `src/chapters.js`; driver in `index.html`. **Guidance (LOCKED mode, `applyStoryGuidance` called at the end of `render`):** masks the hand to the single highlighted card (glow) or the pass button, disables everything else — **the mask only restricts what the UI submits; engine legality is untouched**. A **Brihaspati plate** (`#brihaspati`, portrait chip + one Cinzel line, tap-through) shows each beat's line once. **Opponent** turns run through `pump()` → `storyOpponentStep()` (scripted `playCard`/`pass` by card NAME, then `{handoff:'ai'}` → `aiTakeTurn` for ch2 R2–3). **Cutscenes** are a full **motion-comic player** (`playCutscenes`): 8 real panels in `assets/story/` (`b1c1_i1..i3,v1` / `b1c2_i1..i3,v1`; all 896×1344 except `b1c2_i2` 896×1284 — `background-size:cover` so no letterbox), **Ken Burns** per the `CUT_MOTION` manifest (transform/opacity only; two `#cut-panel` layers crossfade 0.8s), **typewriter plates** (~35 cps, soft `uitick` synth every 2–3 chars, TWO-TAP: tap completes the line, tap advances), a light ember/smoke emitter on `#cut-vfx`, and `skip ›`. **`CUT_MOTION` is pre-seeded with the ch3–5 art pack** (`b1c3_i1` PUSH-IN 1.05→1.14 · `b1c4_i1` PULL-OUT · `b1c4_i2`/`b1c4_i3` RISE · `b1c5_i1` SINK · `b1c5_i2` RISE) ahead of those chapters being authored — computed start/end transforms sampled & verified. Its images live in `assets/story/` (16 files present, all 896×1344 — `b1c5_i3` art + its motion entry (PUSH-IN 1.06→1.16 @50% 46%, the serpent's eye) have both since landed, so the whole ch3–5 pack is now fully specced). **VFX modes** (`cutEmitInterval`/`cutSpawn`, id-keyed via `m.vfx`): `none` · `embers`/`embers-sparse`/`embers-fade` · `smoke` · **`smoke-low`** (very sparse, op .18 — `b1c5_i1`) · **`smoke-heavy-green`** (2 puffs/tick, `rgb(30,58,42)`=#1e3a2a poison-green tinted to rhyme with the Venom teal, op .34 — `b1c5_i2`, tint applies to any mid-match reuse since it's id-keyed). The text plate is **image-independent** — a missing/404 panel image still renders its typewriter plate + hint cleanly (no crash). Panels **lazy-load** (intro prefetched on the chapter-select tap; other chapters fetch 0). **Plate text is now VERBATIM from `docs/BOOK1_CUTSCENE_BIBLE.md` §3** (the bible has landed; a programmatic whitespace-normalized diff confirms all 8 plates match, incl. the STRAIGHT apostrophe in "Shukracharya's"). Plate data shape is `{id, speaker?, plate, ambience?}` (prose lines, not two-line title cards); the two Brihaspati plates (b1c1_i3, b1c2_i2) carry `speaker:'Brihaspati'` → rendered as a small gold Cinzel `.cut-speaker` label above the `.cut-body` line (`.cut-speaker:empty` hidden). **Audio routing:** intro ducks `music_menu` −3 dB (`Audio2.musicDuck`), released into the menu→battle crossfade + conch at match start; victory rides the `sfx_victory` stinger into `stopMusic` silence (no restart); defeat keeps its text plate over `sfx_defeat`. Panel crossfades play a new quiet synth `swish`. **CH2 MID-ROUND STITCH:** b1c2_i3 ("We do not lose the morning. We lend it.") is a single **mid-match panel**, not an intro panel — `cutscenes.mid=[{afterRound:1, panels:[…]}]`; `pump()` checks `storyMidPending()` after the choreo guard and, once R1 resolves, runs `storyShowMid()` (PULL-OUT motion, ducks music, `ctx:'mid'` → "tap to fight ›" hint) before R2. Fires **exactly once per pass through R1** (`Story.midDone[afterRound]` guard) and **re-fires on retry** (a fresh Story run resets `midDone`, so a re-completed R1 shows it again — both verified). `#cutscene` bumped to `z-index:96` (above `#hudwrap` z-95) so the mid panel fully covers the board HUD — no score-pill bleed. **Predicates** (`STORY_PREDICATES` in chapters.js) evaluate win/bonus against final `g` (roundHistory/players/winner); each has a synthetic-stream unit test in `src/test_story.js`, plus headless ch1/ch2 drivers that mirror the UI driver. **Loss → §6 line + instant RETRY** (`storyResultPanel`). Player is Devas throughout (BOOK1_DESIGN §0). **Rewards** are stubs (M1 economy session). **LANDING FUNNEL — Story-first (`updateStoryLandingCta`/`maybeNudgeStory` in `showLanding`):** mode buttons reordered to **Story Mode — Divya Yatra (top, `.mode.primary` gold treatment) → Player vs AI (active, `.mode.active` one step quieter, caption "The full battle, no guidance") → Faction Introduction → Multiplayer (locked)**. Story caption is state-driven from `dy_story`: 0 done → "Begin here — learn as you play"; mid-book → "Continue — Chapter N" (next unfinished `order`); complete → "The Book of Order — complete". **First-launch nudge:** if 0 story chapters done AND 0 PvAI matches (`dy_profile.matches`), the Story button gets a subtle `.nudge` glow (`@keyframes modeNudge`) — **once ever**, persisted via `localStorage dy_nudged='1'`, never after any match; a glow, not a gate (nothing blocks). Verified at 390px: plates verbatim (8/8 programmatic diff), landing order + all three caption states + nudge only on a truly-fresh profile, ch1 single-round win (21–14, bonus), ch2 R1 loss 0–40 → mid-panel (fires once between R1/R2, PULL-OUT, no HUD bleed) → AI handoff → 2–1 win with bonus, retry re-fires the mid panel, gating, telemetry isolation, retry loop, console-clean.
- **Story Mode ch3–5 (SUGGESTED mode; UI-only, engine untouched, baseline byte-identical, test_story now 33 checks / scenario 50 / venom 38 / balance invariants all green).** Authored per `docs/BOOK1_CH3_5_AUTHORING.md`. **ch3 The Weapons of Heaven** (swarga, vs Asuras, handSize 9, mulligan 0) · **ch4 The Churning Begins** (gandharva, vs Asuras, handSize 10, **mulligan 3 — first time in story**) · **ch5 The Poison Rises** (patala, **first vs Nagas**, handSize 10, mulligan 3). **SUGGESTED guidance (`applyStoryGuidance` branches on `Story.mode`):** the recommended card/pass/`🛡 SHIELD` **glows** (`.story-hi`) and a suggested Astra TARGET glows on the board (`.bc.story-target`, via `data-uid`); **NOTHING is masked/disabled — free play** (normal `openInspect`→PLAY flow stays live). Beats advance one-per-turn-ending action via a hook in `runAction` (`storyAfterPlayerAction`, gated `Story.mode==='SUGGESTED' && opts.actor!==1`); free actions (shield/leap) don't advance. The **first non-suggested card play** shows the one Brihaspati aside (bible §6, `Story.asideShown`). Ambient lines hold until the next action via `Story.holdLine`: `introLine` (realm-chip line, ch3/ch5) at match start, `round2Line` (ch5 pacing) at R2, `mulliganLine` (ch4) inside the mulligan modal (`#mull-story`). **Mulligan runs for ch4/ch5** (`storyBeginMulligan`; the scripted opponent is NOT mulliganed so its opening hand stays intact). **Mid-panel triggers generalised** (`storyMidTriggered`): `afterRound` (ch2), **`afterTurn`** (ch3 — `Story.oppTurns>=3`, fires the b1c3_i1 fast-push "The blade seeks your champion" before the opponent's Astra turn), **`afterEvent`** (ch5 — first `venom` event on a side-0 uid fires the b1c5_i2 RISE "Poison does not duel. It waits."). **Cutscene player** now honours per-panel `img` (b1c3_i3 REUSES `assets/img/board_swarga.jpg`, zero new art) and inline `m` motion overrides, plus a **404→text-plate fallback** (`.cut-noart` warm vignette) so victory panels (b1c3/4/5_v1, art pending) render cleanly and real art drops in with zero code change. **Deck resolution (ROLES → engine DECKS, reported):** ch3 p0 = Indra + 6 Units + Vajra/Sudarshana; **ch4/ch5 add a Hero (Indra) — the pack's "8 Units + 2 Mantras" (no Hero) loses every headless seed even with competent play**, and their opponents are de-fanged (ch4: no Ravana/Kumbhakarna/Hiranyakashipu; ch5: light Naga list, no Vasuki/Kaliya). ch4/ch5 decks are **12 cards (>handSize 10)** so the mulligan can draw fresh + rounds 2–3 have draws. **Headless drivers (`driveSuggested`, competent `aiTakeTurn(g,0)` as the winnability prover) win 12/12 across 12 seeds each, bonuses achievable 12/12.** **THREE flagged event-stream validations (checked vs REAL streams, reported):** (ch3) **Heroes are never routed through `destroyUnit`** → no `destroy` event ever carries a Hero uid, so the original `ch3_hero_safe` was unfailable — **since REPLACED, see the truth-audit note below**; (ch4) **Gayatri emits NO `revive` event** (only Amrita Kalasha does) → `ch4_revived` detects the side-0 Gayatri `play` event + a prior side-0 death; (ch5) a venom kill fires in `sweepDeaths`→`destroyUnit(...,'reduced to 0')` — a **GENERIC** cause, so `ch5_no_venom_death` cross-references a `venom` event on the same uid; **venom rarely KILLS Deva units** (they clear between rounds and out-buff the −1/−token drain — venom's pressure is power/round-loss), so the bonus is near-always earned. Verified at 390px: chapter-select 3–5 playable / 6–7 stubbed, caption advances to "Continue — Chapter 3", full playthrough of each (intro panels, mid panels fire at their triggers, mulligan + Brihaspati line, victory 404 fallbacks, SUGGESTED glow + free play + aside, bonuses earned), console clean. **Bible ch2 doc corrected** (b1c2_i3 noted as the mid-round panel → intro is two panels). **Chapter-cleared dialog restyle + log grammar (UI-only polish):** `#storyresult`'s CONTINUE (`#sr-primary`) is now a proper **primary** button (Cinzel, letter-spaced, full-strength `--gold` text+border, `rgba(232,200,116,.12)` fill, 9px radius) paired with the quieter `.go-alt` REPLAY secondary (same 210px width, stacked) — it was an unstyled white default. **`fixLogGrammar(msg)`** (applied at the two log render points — `#logbody` lines + `#loglast`) corrects the engine's name-interpolated subject-verb agreement at DISPLAY time (engine untouched): `You WINS THE MATCH`→`You WIN THE MATCH`, `You wins Round`→`You win Round`; 3rd-person `Opponent WINS/wins …` passes through unchanged. The `⚔ ` prefix on the match line means the transform targets the subject+verb substring, not the string start.
- **Hero-interaction truth audit + ch3 bonus replacement (UI/data only, engine untouched, test_story now 35 checks, gate green).** **AUDIT — no launch card DESTROYS a Hero** (Heroes never go through `destroyUnit`). The nearest answers: **Sudarshana Chakra** = TEMPORARY removal — splices an enemy Hero into `removedHeroes` for the round, returns next round at `max(1, ceil(power/2))` (half power); **Meghnad** = −2 power to a Hero (floored at 0, stays on board); **Takshaka + a Naga Astra (Nagapasha)** = bind → 0 power contribution. **Realms-tab Swarga copy corrected** (index.html `FI_REALM_CONTENT` + `docs/realm_intro_content.md`): "Hero-removal gains value…" → "Hero answers gain value: **Sudarshana Chakra benches an enemy Hero for a round (it returns at half power)**…" (precise, no false destruction/permanence). **CH3 BONUS: `ch3_hero_safe` (unfailable) → `ch3_no_astra_death`** ("Win with no Unit lost to an enemy Astra"; predicate = no side-0 `destroy` whose cause ∈ `ASTRA_DESTROY_CAUSES` {Vajra,Brahmastra,Pashupatastra,Gandiva Arrow,Lanka Dahan}). **ENGINE TRUTH that shaped the design:** Dharma Shield is single-target-Astra **target-prevention** and does NOT mitigate AoE — **Pashupatastra ignored the shield and killed a Unit in 12/12 games → that bonus was unearnable.** So **ch3's telegraphed strike is now VAJRA** (single-target, respects the shield; Asuras have no other single-target Astra, so per BOOK1_DESIGN §3 "the Asuras unveil a true Astra"). The winning **defensive line** — shield the champion and don't over-expose a second big (effPower≥6) Unit to the round-1 Vajra — makes it find no unshielded mark and **fizzle → EARNED**; careless play (no shield) → Vajra kills → **FAILED**. Guidance beats reordered to steer this (shield before developing; hold your greater warriors). Validated BOTH ways vs real streams (`driveCh3` guided/careless in test_story: guided **wins 12/12 + earns 12/12**, careless fails); verified in-browser: shield → "★ Bonus earned", competent-but-unshielded win → "☆ Bonus missed". **NOTE:** an intermittent `drawImage` error can surface in the **untouched** board-VFX module only under rapid automated cutscene-clicking (absent at normal pace; `astraBurst` is clean in isolation; this task removes, not adds, a VFX path) — a pre-existing rapid-automation race, not introduced here.
- **Story Mode ch6–7 — BOOK 1 FINALE (FREE mode; UI/data only, engine untouched, baseline byte-identical; test_story now 46 checks / scenario 50 / venom 38 / invariants).** Book 1 is COMPLETE (ch1–7 all playable, `LOCKED_STUBS` retired to `[]`). **ch6 The Nectar and the Net** (RANDOM realm, vs Asuras, teaches Artifacts) · **ch7 The Betrayal (BOSS)** (RANDOM realm, Shukracharya boss). **FREE mode** (`applyStoryGuidance` early-branch): NO mask/glow/beats — only a one-time **Artifact shimmer** (`.hc.story-shimmer`) on the Amrita card + the held intro line; both dismissed on the first player action (`runAction` FREE hook). **Deck deviation (reported):** the pack's no-removal player deck can't hit the acceptance bands — an honest heavy-Asura match is a hard 0%/100% cliff (Kumbhakarna's 3-dmg sweep and Ravana+Maricha hard-counter the go-wide Deva board, and the AI can't answer them). So the graduation deck (shared ch6/ch7) adds **Vishwakarma** (the diegetic answer — the "Divine Architect" unmakes the boss's Chandrahas Artifact, denying the Astra-doubling) **+ Vajra** removal, alongside Amrita/Pavamana. **Bands (competent `driveFree` = aiMove + Vishwakarma/Vajra removal, 96-seed stable):** ch6 = **12/12** (≥9 ✓, opponent has no Kumbhakarna/Ravana); ch7 boss = **~6.6/12** (49–59%, in the 6–10 BOSS BAND ✓) with ONE heavy (Kumbhakarna) + Chandrahas + Pashupatastra. **THREE validation items (reported):** (a) **NO Asura card removes an enemy Artifact** (only Deva Vishwakarma) → ch6 bonus fails only by CHOICE; star text "The Kalasha never left the field" claims no protection. (b) **Mahabali** works across the script→AI handoff (no misbehavior) but an 8-power Hero pushes the boss to 0% → **EXCLUDED** from the boss list. (c) **Chaos Surge** is frequent (~5/game, Chandrahas doubles it) but does NOT swing the band (stable across 4×96-seed samples). **BONUSES:** ch6 `ch6_artifact_kept` (`g.winner===0 && players[0].artifact`) — earnable by HOLDING Amrita until the deciding round so it persists into `g.over` (endRound returns at match-over BEFORE the artifact-clearing reset), failable by never playing it; validated both ways (`driveFree` keep 24/24 kept, never 0/24). **ch7 `ch7_unmake`** ("The stolen blade, unmade" — win with the boss's Chandrahas destroyed) — REPLACED `ch7_sweep`, which was proven unearnable vs an in-band boss (0/96, round economy forces 2–1). VALIDATED: enemy-Artifact destruction is **STATE-ONLY (no event)** — Vishwakarma sets `opp.artifact=null` + increments the destroyer's `players[0].artifactsDestroyedByMe`; predicate = `winner===0 && players[0].artifactsDestroyedByMe>0`. EARNABLE (the removal line unmakes Chandrahas: headless 17/48 wins earn it + in-browser "★ The stolen blade, unmade"), FAILABLE via the **Yaksha realm** ("Artifacts cannot be destroyed" → Vishwakarma whiffs, wins earn 0). Note a **no-Vishwakarma line wins 0/96** (you MUST unmake Chandrahas to survive the doubled Pashupatastra), so it's the taught graduation play made explicit — diegetic, rhyming with the stolen-Vajra stitch. **UI:** shared **b1_defeat cutscene** (`DEFEAT_PANELS`, Brihaspati by the fire, §6 line) now plays on a loss for ALL 7 chapters before the retry panel (`showStoryResult`); **boss VS plate** (`#bossvs`, "SHUKRACHARYA · THE BETRAYAL") before the ch7 match; **ch7 mid** at boss T3 = b1c7_i2 reused + Shukracharya speaker + **the lightning flash** (`#cut-flash`, the book's single use, `flash` panel flag); **Mohini two-phase DRIFT** (`b1c7_v1`, `mohini` panel flag → `@keyframes mohiniDrift`: RISE, 1s hold, lateral drift to the Book-2 silhouette); **book completion** line on chapter-select + the existing landing 'complete' caption. Rewards remain STUBBED. Verified at 390px: FREE mode (no mask) + shimmer + intro line live; every cutscene/overlay rendered via direct trigger (ch6 victory, boss plate, mid flash + Shukracharya, Mohini drift, defeat panel) console-clean; chapter-select 6–7 playable + completion line + landing caption. **NOTES:** the ch7 boss **showcases Chandrahas on T3 — still carrying stale pre-delta frame text** (Sangbaran's Canva re-export, not code). **CHOREO HARDENING (shipped):** `resetChoreo()` (clears `choreoActive/choreoSkip/choreoTimers/pendingPlay/aiTimer/roundCountAtActionStart` + unlocks `#choreolayer`) is called at the top of `startGame()` AND `startStoryChapter()`, so a new match NEVER inherits a stuck `choreoActive`. Both choreography pumps (the `runAction` async + the mulligan async) now wrap every step in try/catch and the whole sequence in **try/finally**, so a thrown step (e.g. the drawImage race) is SKIPPED and `choreoActive` is ALWAYS released. **Root report:** the two failure modes share a DOWNSTREAM root (a choreo async that never reaches `choreoActive=false` → the lock stays stuck → the next match is poisoned, since `startGame` didn't reset it), but different proximate causes: the fast-pace freeze is a **hung await** (`animateTotals`' `requestAnimationFrame` never resolves in the throttled headless preview — reproduced with `errsThrown:[]`, no exception), while the drawImage race is a **thrown exception**. The try/finally fixes the exception path; `resetChoreo` fixes BOTH (it doesn't matter how the previous choreo stuck). Verified in-browser: reproduced the poisoning (match 1 froze, `choreoStuck:true`) → the next `startGame` yields a PLAYABLE match (`choreoActive:false`, accepts an action); and a forced `VFX.cardLand` throw mid-choreo leaves `choreoActive:false` with the match still advancing. Also: the **ch3 lore stitch** (Brihaspati's stolen-Vajra mid line + softened "His stolen weapon seeks your champion" plate) was already shipped in the truth-audit follow-up and is present.
- **FREE-MODE PASS BUG FIX + guidance fail-open hardening + objective chip (UI/data only, engine untouched, baseline byte-identical; test.js invariants + baseline / scenario 50 / venom 38 / story 48 green; index.html 0 console.log).**
  - **CONFIRMED ROOT CAUSE (the task's suspect was WRONG — it is NOT the disabled-state):** the pass button's **capture-phase click interceptor** read `if(Story && Story.mode!=='SUGGESTED'){ e.stopImmediatePropagation(); if(story-hi) storyPlayerTap(); }`. In **FREE mode** `mode!=='SUGGESTED'` is true, so it `stopImmediatePropagation()`'d — killing the real PvAI pass handler — but FREE never sets `story-hi`, so it never routed to `storyPlayerTap` either. **Every FREE-mode pass tap was intercepted and dropped.** The button was fully ENABLED (render sets `disabled=!myTurn`); it just looked clickable and did nothing. Card plays worked because no such interceptor exists on cards. The interceptor's own comment said "LOCKED: intercept… SUGGESTED: let it run" — the intent was LOCKED-only, but the code had drifted to "all non-SUGGESTED," sweeping FREE in.
  - **ROOT FIX + FAIL-OPEN HARDENING:** the interceptor now engages **only** `Story.mode==='LOCKED' && passbtn has story-hi` — i.e. only to route the one guided-pass tap. SUGGESTED, FREE, and any malformed/unknown mode fall through to the normal handler → **fully unlocked is the default; LOCKED is the only explicit restriction.** Companion guards: `storyPlayerTap()` early-returns unless `mode==='LOCKED'` with a non-empty `guidance` array; `#brihaspati` onclick plays the guided card only in LOCKED (else just dismisses); `applyStoryGuidance` **fails open** when a beat-driven mode has a missing/empty/exhausted `guidance` (no mask, no disable) — so a future chapter with a broken beat script is playable, never soft-locked. Verified: ch1 LOCKED still locks pass during card beats (disabled, click ignored) and unlocks+glows at its pass beat (guided pass fires); ch3 SUGGESTED free-play pass works + glow logic untouched; ch6 & ch7 FREE pass works as first action, mid-round, after a shield interaction, and across the round-1→2 boundary.
  - **FREE-MODE OBJECTIVE CHIP (`#objchip`):** a persistent collapsed chip (top-left) shown **only in FREE mode** — win condition (`storyWinText(ch)` from `win.type`) + bonus text (`ch.bonus.label`), both sourced from `chapters.js`; tap to expand/collapse; **no glow** (information, not guidance). `renderObjective()` runs at the top of `applyStoryGuidance` (before every early return) so it survives opponent turns/choreo, self-hides for LOCKED/SUGGESTED/PvAI, and is cleared on the result screen + `showLanding`. Verified 390px: ch6 shows "Win the match / ★ The Kalasha never left the field", ch7 shows its own "★ The stolen blade, unmade", expand works, persists across rounds.
  - **ch6/ch7 DUAL-LINE (Amrita instruction):** the FREE intro plate is now dual-line — ch6's `introInstruct` ("Play Amrita Kalasha (the glowing card) when you like — keep it on the board to hold the win.") renders under the aphorism via `showBrihaspati(holdLine, chapter.introInstruct)`. ch7 stays silent (boss; no intro line), so no instruction there — correct.
- **Real-device Story-Mode playtest fixes — SIX UI/UX bugs (UI-only, engine untouched, baseline byte-identical; test.js invariants + baseline / scenario 50 / venom 38 / story 48 green; index.html 0 console.log).**
  1. **PLATE POSITION (the blocker):** `#brihaspati` moved from `bottom:98px` (over the hand, hiding the card it names) to **vertical center** (`top:44%; translate(-50%,-50%)`) — over the board, well above the hand. Verified at 390px: plate bottom ≈418px clears the hand (top ≈573px) → **hand fully visible + tappable beneath**. Same element serves the SUGGESTED aside.
  2. **AUTO-SCROLL TO HIGHLIGHT:** `storyScrollToHighlight(el)` centers the highlighted hand card in the `#hand` slider — **manual `hand.scrollTo({left,behavior:'smooth'})`, NOT `scrollIntoView`** (which silently no-ops on horizontal-only containers here). Only fires when the highlighted card's uid changes (`Story.scrollUid`), so it doesn't fight manual scroll. Verified: far-end card of an 8-card hand (Kubera) scrolled 989px into view (`highlightInView=true`). Hand cards now carry `data-uid`.
  3. **BEAT AVAILABILITY GUARD (desync family):** `storyEffectiveHighlight()` — if a beat names a card no longer in hand (spent in SUGGESTED free-play, or never drawn), it resolves to `{auto:'bestOrPass'}` instead of pointing at a ghost, and raises a one-time aside **"The moment has changed — fight with what you hold."** (`Story.momentAsideShown`). Used by BOTH the glow render AND `storyPlayerTap` so they never disagree. One generic guard covers **every** ch1–5 card beat (audited: ch1 Yama/Marut/Ashwini/Surya, ch2 Deva Soldier, ch3 Surya/Marut/Vajra, ch4 Surya/Yama/Gayatri, ch5 Surya/Yama/Pavamana). Verified with a deliberately-spent Vajra in ch3 → beat skips to Surya Dev + the aside, glow never on Vajra.
  4. **GLOW RELIABILITY — the "virtualization" suspicion was UNFOUNDED:** the hand is NOT virtualized — `render()` rebuilds all `.hc` elements every frame (`me.hand.forEach`), and `applyStoryGuidance` re-applies `story-hi` each render, so the glow class always lands on the DOM element. The reported "Marut didn't glow" was the card being scrolled **out of view** — fixed by item 2's auto-scroll (the glow is applied AND brought on-screen).
  5. **COPIED-CARD ART (root: faction, not `disguisedAs`):** Maricha (engine) FULLY overwrites the card — `c.n/id/base = target's` — so a Maricha (Asura) copying Surya Dev becomes `SuryaDev` but stays Asura-owned; `cardArtSrc` built `Asuras_Unit_SuryaDev…png` (nonexistent → text placeholder). Fix: a `CARD_FACTION` index (id/name→home faction, built once from engine `DECKS`) + `cardArtSrc` now resolves the faction from the card's **home deck** (`cardHomeFaction(c) || faction`) → `Devas_Unit_SuryaDev_P6_rEpic.png` (exists). Identical for normal cards; also fixes Mohini-stolen art. A **copy marker** (`.copybadge` ⧉ on the board mini, `.sc-copy` "⧉ COPY" on the showcase) shows when home faction ≠ owner AND not a Mohini steal (that has its own 🌀 badge). ⚠ Limitation: a **same-faction** Maricha copy is indistinguishable from an original without an engine flag → renders correctly (right art) but gets no copy marker; the cross-faction case (the reported/visible failure) is fully handled.
  6. **OPPONENT SHOWCASE:** the showcase trigger is generic (`opts.actor===1 && a play event` → `showcaseCard`), so every opponent scripted play showcases; Maricha appeared "invisible" only because its post-copy art 404'd (onerror removed the img) — item 5's faction fix makes it load. Verified: disguised Maricha showcases as Surya Dev with art + the COPY tag.
  - Verified at 390px (screens: centered plate + tappable hand, auto-scrolled Kubera highlight, disguised Maricha showing Surya Dev art on the board); console clean.
- **Story text v2 (ch1–3) — verbatim narration rewrite (data-only; engine untouched; full gate green — no test asserts prose).** Source: `docs/story_text_v2_ch1-3.md` (playtest feedback: "texts too little, doesn't connect — a player wants a story"). Every ch1–3 cutscene plate is now **2–4 sentences of narration** (the typewriter/two-tap system handles the longer text unchanged — verified: a 237-char plate types progressively, tap 1 completes the line, tap 2 advances; the ch3 mid still renders as **two sequential plates** — telegraph then Brihaspati quote). Replaced in `src/chapters.js`: all ch1/ch2/ch3 intro+victory+mid plates and the **ch3 beat `instruct` lines** (aphorism `line`s kept). **b1c1_i3 and b1c2_i2 dropped their `speaker:'Brihaspati'` field** — the v2 text is 3rd-person narration ("the old guru Brihaspati sets a steadying hand…"), so a Brihaspati speaker-label would misrender; ch3 mid plate 2 keeps `speaker:'Brihaspati'` (pure quote). **The SUGGESTED aside (`STORY_ASIDE`, in index.html) is now DUAL-LINE** — aphorism "Your hand, your war — I only counsel." + instruction "You played your own choice — that's allowed…" — via a new `Story.holdInstruct` paired with `holdLine` (the render already used the two-arg `showBrihaspati(line, instruct)` for the FREE intro; holdInstruct is set with the aside and cleared at every other holdLine setter). ⚠ Prior CLAUDE notes that ch1–3 plates are "verbatim from BOOK1_CUTSCENE_BIBLE §3" are **superseded for ch1–3** by this v2 rewrite.
- **Story text v2 (ch4–7) — narrative pass COMPLETE for all 7 chapters (data-only in `src/chapters.js`; engine byte-identical; full gate green — no test asserts ch4–7 prose).** Source: `docs/story_text_v2_ch4-7.md` (the sole text authority). Replaced all ch4/5/6/7 intro+victory plates (2–4-sentence narration), the ch7 mid plate, the **ch4** opener + Gayatri beat instructions, the **ch5** Yama/Pavamana/bestOrPass beat dual-lines, and the **ch6** Artifact-pointer `introInstruct` — verbatim except the three ENGINE-VERIFY clauses (below). **UNTOUCHED:** all motion/VFX/SFX, the ch5 MID plate ("Poison does not duel. It waits." — already matches the doc's FIRST-VENOM-TICK aphorism; doc gave no separate ch5 mid text), the shared defeat plate, the completion line, ch1–3.
  - **THREE ENGINE-VERIFY VERDICTS (checked vs `src/engine.js`):** **(1) VENOM timing — CORRECTED.** Doc said "poisoned warriors weaken as the round goes on"; engine `venomRoundEnd` ticks ONCE at round end (before scoring), and ch5 **intentionally excludes Karkotaka** (chapters.js line 230 comment "so ticks stay at round-end") → no per-turn drain. Corrected the ch5 Yama instruct to "…**lose power when the round ends**." **(2) PAVAMANA — CONFIRMED.** Doc "it cleanses the Venom from your warriors"; engine strips Venom Tokens from **ALL** friendly Units (scope ✓ = "your warriors"), and also heals debuffs + grants **+1 power per effect removed** — the cleanse clause is accurate (the buff is an unmentioned bonus, not an error), kept verbatim. **(3) AMRITA KALASHA — CONFIRMED.** Doc "Artifacts add no power, but stay on the field and keep working"; engine: Amrita is `p:0` (adds no power ✓), artifacts occupy the artifact slot and persist on the board (the ch6 bonus rewards keeping it there into `g.over`), and Amrita's lingering revive-aegis keeps working after its on-play +2 — kept verbatim.
  - **SPEAKER metadata:** all plates narration (no `speaker` field) EXCEPT **b1c7_m1** (`speaker:'Shukracharya'`, already set). **The cutscene player already supports any speaker** — `$('cut-speaker')` renders the name as a gold uppercase Cinzel label (`.cut-speaker`); the ब chip is ONLY on the separate in-match `#brihaspati` plate, never the cutscene. So SHUKRACHARYA renders as a name label with no chip (verified, screenshot). Per the ch1–3 precedent, the doc's outer wrapping quotes on b1c7_m1 are dropped (the speaker label denotes the speaker).
  - **MULLIGAN dual-line (reported limitation):** the ch4 mulligan teach in the doc is dual-line, but the mulligan modal renders a **single** `.mull-story` line; `mulliganLine` already equals the doc's aphorism verbatim, so it's unchanged — the second (instruction) register would need a modal UI change, out of scope for this data-only pass.
  - Verified at 390px: a 331-char ch4 plate types progressively + two-taps (tap 1 completes, tap 2 advances); b1c7_m1 renders the **SHUKRACHARYA** label (screenshot); the ch4 Gayatri dual-line renders both registers (Cinzel aphorism + Crimson Pro instruction). Book 1 v2 narrative pass is now complete for ch1–7. Any wording deltas from Sangbaran's ch4–7 phone playthrough apply on top of this baseline.
- **Story guidance regression + showcase fixes (playtest batch 2, ch4–6; UI/data only, engine byte-identical, full gate green — test.js invariants + baseline / scenario 50 / venom 38 / story 48; index.html 0 console.log).**
  - **ITEM 1 — GLOW/TEXT DESYNC root (the id-vs-name hypothesis was REFUTED):** the guard already keys on `c.n` (name), and with the named card present it resolves correctly (opener glows Surya Dev, not Indra). The real defect: the moment-changed aside was gated by a **one-shot `momentAsideShown`**, so only the FIRST fallback suppressed the stale `beat.instruct`; every later fallback beat rendered its stale instruct while the glow fell back → text/glow desync. **This was a regression introduced by the previous guard task** (it added the glow fallback but left the text on the one-shot aside). ("Indra glows" was imprecise — the fallback glows the highest-power playable **Unit**, e.g. Yama; Indra is a Hero, excluded from bestOrPass.) **FIX — TEXT AND GLOW SHARE ONE RESOLUTION:** `storyEffectiveHighlight()` now returns `{highlight, fellBack, tgt}` where `tgt` carries the resolved **uid**; the glow anchors to the hand card **by uid** (`storyHiEl`, immune to DOM/index drift — this also fixes the ch6 shimmer, which now points at Amrita by uid, never Indra); the plate renders `beat.instruct` **only when `!fellBack`**, and on `fellBack` shows the moment aside and **never** the stale instruct (verified on repeated renders). Applied to LOCKED, SUGGESTED, and the ch6 shimmer pointer; `storyPlayerTap` uses the same `eff.tgt`.
  - **ITEM 2 — GUARD CHECKS PLAYABILITY, not just presence:** `storyCardPlayable(name)` = in hand AND `playableIndices(G,0).includes(idx)` (the SAME legality the UI uses to gate taps — no parallel check). Illegal (e.g. Gayatri with an empty discard) → same bestOrPass fallback as absent.
  - **ITEM 3 — CH4 GAYATRI BEAT TIMING (diagnosis: turn-indexed, not event-gated):** the beat advanced by player-action count and reached the Gayatri moment before the scripted Pashupatastra (opp T4) killed anything. **FIX:** a `holdForCard:true` flag on the ch4 Gayatri beat (chapters.js) — `storyAfterPlayerAction` does NOT advance past a `holdForCard` beat until its card is legal, so the beat **waits at engine state** (unit in discard AND Gayatri drawn) rather than a turn index. Verified by driving ch4: beat held at idx 2 with `fellBack` while the discard was empty; Pashupatastra killed **3** units (kill is guaranteed by the full Asura board — no script hardening needed); then, once Gayatri was in hand + discard had units, it glowed Gayatri and fired. Never fired on an empty discard. The `ch4_revived` bonus predicate (Gayatri play + prior death; Gayatri emits no `revive` event) still passes both ways (story suite).
  - **ITEM 4 — CH5 NAGASTRA SHOWCASE (already fixed, suspicion wrong):** astras DO emit a `play` event (generic emit at engine line 834, before the type dispatch), so the showcase triggers; and **Nagastra's home faction is `asuras`** (it lives in `DECKS.asuras`), so yesterday's `cardHomeFaction` change already resolves its art to `Asuras_Astra_Nagastra_rRare.png` (present on disk) — `imgLoaded=true`. The playtest report predated that fix. No code change; verified.
  - **ITEM 5 — MARICHA TWO-PHASE REVEAL:** the `play` event's `abilityName` is the **pre-copy** name (`'Maricha'`), because the generic emit (line 834) precedes Maricha's overwrite (line ~1958); the card object found by uid is post-copy. `showcaseCard(uid, playedAs)` compares `playedAs` to the resolved card's name — on a mismatch it renders **phase 1** = the played card (Maricha art + a crimson `◈ DECEIVER` tag) via a new `fillShowcase` helper, holds ~1.2s, then re-pops to **phase 2** = the copied card (`⧉ COPY` tag, the copied card's art). Reuses the existing `#showcase` plumbing (no new overlay). DOM-verified both phases (phase 1 Maricha art loaded; phase 2 `Devas_Unit_SuryaDev_P6_rEpic.png` loaded).
  - Verified at 390px: ch4 opener glow+plate both name Surya Dev (screenshot); ch4 Gayatri holds on empty discard and fires after the kill (driven sequence); ch3 spent-Vajra fallback glow+text agree with no stale instruct; ch5 Nagastra + Maricha two-phase DOM-verified. Console clean.
- **Real-device Story-Mode playtest fixes — FOUR items (UI/data only, engine untouched, baseline byte-identical; test.js invariants + baseline / scenario 50 / venom 38 / story 48 green; index.html 0 console.log).**
  1. **TEXT-PLATE CUTSCENE BACKDROP:** any cutscene panel whose image 404s (notably the **ch3/4/5 victory plates** — `b1c3_v1.jpg`/`b1c4_v1.jpg`/`b1c5_v1.jpg` are NOT in `assets/story/`, so `cutImgUrl` builds a path that 404s) now renders over the **chapter's first intro image, darkened ~65%**, never over plain black. `playCutscenes(panels,onDone,ctx,fallback)` gained a 4th `fallback` param (stored on `CutState.fallback`); all 4 call sites pass `storyIntroFallback(ch)` = `ch.cutscenes.intro[0]` image (or `menu_bg.jpg` if even that is missing). The panel-image probe's `onerror` calls `setCutBackdropFallback(next, fallback)` → sets `background-image:linear-gradient(rgba(0,0,0,.65)…), url(fallback)` and itself falls back to `menu_bg.jpg` if the fallback image also errors. Verified at 390px: all three victory plates render over their darkened first-intro art (ch3 screenshot shows the moon-blade figure behind "Heaven's weapons answer only when heaven is ready.").
  2. **CHOREO WATCHDOG (the real-device freeze root — iOS backgrounding throttles rAF, hanging `animateTotals`'s rAF loop mid-await, sticking `choreoActive`):** (a) **`choreoRace(promise, snap)`** wraps the rAF-based `animateTotals` await in a `Promise.race` with a **3s hard cap** that `snapTotals()` (totals jump to `totalPower` final values) and resolves — so a starved rAF can no longer hang the choreo; a `choreoForce` finisher list lets skip/hide/watchdog end it immediately. (b) **`visibilitychange→hidden`** calls `fastForwardChoreo()` (drains every pending `cDelay` timer + every `choreoForce` finisher) so a backgrounded tab can't stick. (c) **8s belt-and-braces watchdog** (`setInterval(1500)` on `choreoActive && performance.now()-choreoStartedAt>8000`) force-finishes then hard-releases the lock + re-enables input. `skipChoreo` now delegates to `fastForwardChoreo`; `resetChoreo` also clears `choreoForce`/`choreoStartedAt`; both choreo pumps (runAction + mulligan) set `choreoStartedAt=performance.now()` on lock and clear the new globals in `finally`. **Reproduced at 390px:** overrode `requestAnimationFrame` to a no-op (total rAF starvation), played a card through the live UI path — after the 3s cap `choreoActive` released, `#choreolayer` unlocked, the unit landed, **`#ptotal` display snapped to the correct value despite rAF never firing**, and the AI took its turn (match fully playable). Direct tests: `choreoRace(neverResolves)` resolves at 3001ms (cap) or 0ms (fast-forward), snapping both. (The prior `resetChoreo` + try/finally hardening is confirmed present in the build.)
  3. **GUIDANCE PLATES — DUAL-LINE:** every guidance beat and Brihaspati plate now carries a plain-language **instruction line** beneath the aphorism. `#brihaspati` gained a `.bri-instruct` div (**Crimson Pro** — non-Cinzel — muted **`--gold-2`** 12px, `:empty{display:none}` so dialogue holdLines with no instruct hide it); `showBrihaspati(line, instruct)` fills both; the `applyStoryGuidance` beat call passes `beat.instruct`. **Instruction lines authored for ALL ch1–5 guidance beats** (ch6/7 are FREE mode — no beats) in `src/chapters.js`, each naming the exact card or button — e.g. ch4 Gayatri Mantra → "Tap the glowing card: Gayatri Mantra — it brings back your fallen unit.", ch1 pass → "Tap PASS ROUND — you are ahead; stop here and win the round." Verified at 390px: dual lines render (ch1/ch4 beat data confirmed carrying `instruct`), empty-instruct dialogue plates hide the line.
  4. **LOG GRAMMAR, GENERALIZED:** `fixLogGrammar` (display-time only — `G.log` stays raw) now fixes **all** 2nd-person `"You <verb>s"` lines, not just WIN/WINS. `_baseVerb` applies the English 3rd-person→base rule (`-es` after ss/sh/ch/x/z/o → drop 2; `[^aeiou]ies`→`y`; else drop trailing `s`); the anchored regex `/(^|⚔ )You ([A-Za-z]+)/` only touches subject-position "You" so score lines ("Round 2 ends — You 21 vs …") and 3rd-person subjects ("Asura raises …") are untouched. Verified live: "You plays Ashwini Kumars" → rendered "You play Ashwini Kumars"; "You raises Dharma Shield"→"You raise", "You passes"→"You pass", "You WINS"→"You WIN".
- **Match-start conch + round-end gong (`sfx_conch` / `sfx_round_end`, −14 LUFS)** — audio layer only, engine untouched, baseline byte-identical, 38 tests green. **`sfx_conch`** is a match-start announcement timed to **round 1, not the mulligan**: `startGame()` arms a once-per-match guard (`conchPending=true`) alongside the battle-music crossfade (music plays at mulligan), and `fireConch()` at the top of `pump()` consumes the guard the first time `pump()` runs for the match — which is the round-1 start, right after the mulligan resolves (both the instant KEEP-HAND / reduced-motion path and the draw-choreography path reach `pump()`). `pump()` is never called during the mulligan phase, so the conch can't fire early; the guard makes it silent on rounds 2/3; `startGame` re-arms it each match. **`sfx_round_end`** replaces the synth round-end cue in `showRoundBanner()`, gated `if(!G.over)` — so it sounds for rounds 1 & 2 but is **suppressed at match end** (round 3, or a 2–0 clinch), where the `sfx_victory`/`sfx_defeat` stinger in `showGameOver()` owns the moment (no gong under a stinger). The `!G.over` guard is local to the gong line, so suppression holds regardless of caller. **Test-SFX = 34** (16 synth — incl. the story `uitick` + `swish` — + 18 files). Both `assets/audio/sfx_conch.mp3` and `sfx_round_end.mp3` are present. Verified in-browser: conch=0 during mulligan, =1 at round-1 start, =0 on later pumps (rounds 2/3), exactly once per match across two consecutive matches (guard re-arms); gong=1 on a non-over round, gong=0 at match end, `showGameOver`→stinger=1/gong=0.
- **Power-change blips + coalescing (`sfx_buff` / `sfx_debuff`, −17 LUFS)** — 2 new files; audio layer only, engine untouched, baseline byte-identical, 38 tests green. `playEventSound` routes the `buff` event → `sfx_buff` (power INCREASE: unit buffs, Rama Naam, Indra's Net, Amrita Kalasha +2, Kishkindha Crown leap bonus, Pavamana cleanse gains, Chaos Surge, Surya Dev, Neela, Kalki Kshetra, …) and the `damage` event → `sfx_debuff` (power DECREASE from non-Venom effects: Ashvatara, Sarpa Satra, debuff/damage Astras). **The Venom drain keeps its own identity** — it emits its own `venom` event (never `damage`), so the quiet synth Venom tick is untouched. **Coalescing (`powerChangeSound`):** the batching signal is the **runAction event beat** — `resetPowerBeat()` clears a `(sound|abilityName)` key-set each beat, and because a multi-unit effect shares one `abilityName` across its per-unit events, a whole-board buff or drain collapses to **one** blip (never one-per-unit). A separate real-time overlap guard (`powerRecent`, 350 ms window) caps each sound at **≤2 concurrent**, dropping extras. Applied in both the choreographed path and the reduced-motion audio path. `ward`/`revive` stay on the synth `buff` cue (survival/revival, not a power increase). **Test-SFX = 30** at this task's landing (14 synth + 16 files); the conch/gong task has since landed and raised it to **32** (that task's `sfx_conch` + `sfx_round_end` asset files are still pending in the repo). Coalescing verified in-browser: one effect × many units → 1 blip; 3 distinct effects in a beat → capped at 2; separate beats → blip each; buff+debuff same beat → 1 each; a Venom tick → 0 debuff blips.
- **Impact VFX (`VFX` module) — quality overhaul (sprite-based, reads as real phenomena)** — one board-sized `#vfxcanvas` (pointer-events none, z-5) driven by the event stream, rAF + delta timing, **DPR-aware (cap ×2)**, skipped under `prefers-reduced-motion`, no external libs. **Sprites pre-rendered ONCE at init** (5 radial-gradient fire glows white→deep-red, one ember dot, 3 warm-dust + 3 dark-smoke irregular multi-blob sprites) → all particles are `drawImage`, so per-frame cost is trivial. **Hard cap 120 particles** (up from 40). Two draw passes: smoke in `source-over`, fire/embers/bolts in `globalCompositeOperation='lighter'` (additive). (a) **card-lands** = warm brown-grey dust (rotate/spin, scale 1×→2.5×, fade-in 10% + long tail, sine curl) + 5–8 gold embers. (b) **Astra fire burst** = 3 populations (6 large soft glows, 12 hot cores, 8 embers) with upward buoyancy + inward pull + per-particle ~10–15 Hz flicker, lifetime color ramp via sprite index, then 4 grey smoke puffs after the flame peak — on the sfx_astra/shake beat. (c) **Mythic/Brahmastra** = real **midpoint-displacement lightning** (fractal polyline, 6 levels, 2–3 forked branches; 3 additive strokes: wide blue-white glow / medium / 1px white core) in a strike sequence (dim tint → bolt 1 → dark gap → bolt 2 with a NEW shape → `#vfxflash` full-screen flash + afterglow → heavy smoke roll + embers at impact), over the existing shake + `sfx_brahmastra`. All durations ×`CHOREO_SPEED`. **Frame-timing verified** at the 120-particle + 2-bolt worst case (DPR-2 canvas 746×875): render **avg 0.10–0.12 ms / worst 0.6 ms** — ~55× under the 16.7 ms budget, far under the 33 ms target; **cap 120 unchanged**. All three effects confirmed firing on their correct `play`-event beats (unit→cardLand, astra→astraBurst, Mythic astra→astraBurst+lightning). Build ships **console-clean**.
- **NOTE: audio LOUDNESS / loop-seam clicks are not verifiable headless (no audio output). Verified: all 4 files fetch+decode, crossfade cycles menu⇄battle, both loop via buffer (gapless by construction). Total added page weight ≈ 1.27 MB (audio 960 KB + menu_bg 336 KB). A real device is needed to judge mix balance.**

**ENGINEERING BACKLOG: EMPTY.** The core single-player game loop is engineering-complete. The only remaining items are art production and a designed deferral (below) — no code work outstanding.

**Deck building — DECIDED: deferred to Season 2 (designed deferral, not open scope).** 22-card decks drawn from 22-card factions have **zero degrees of freedom** — there is nothing to build. Card-level deckbuilding becomes a real feature only when the pool exceeds the deck size (e.g. Rishis expanding it to **44-into-22**). Until then, faction-select is the correct and complete shipping form.

**Art production (content, NOT engineering; `cardArtSrc` naming already handles all of it):**
- ✅ Devas 22/22 · ✅ **Asuras 22/22** · ✅ Vanaras 22/22 (art present)
- ⏳ **Nagas 0/22** pending
- ✅ **Four blocking re-exports DONE:** Chandrahas, Hanuman, Neela, Angad — Canva re-exported with engine-correct frame text (same filenames, 750×1050 PNG, dims unchanged from predecessors); `assets/thumbs/` regenerated for the four via the `scripts/make_thumbs.sh` sips command (200×280 JPEG, 20–24 KB). Verified both sizes load (full + thumb, no onerror) and the ch7 Chandrahas boss showcase renders the new frame.

**Post-launch (explicitly out of the core loop):** Capacitor mobile wrap · multiplayer (Node + Colyseus/Nakama) · tutorial/onboarding.
