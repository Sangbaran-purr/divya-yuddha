# DIVYA YUDDHA — CONTINUATION 2026-07-22
# Paste at the start of the next session. Supersedes the 07-21 doc for
# program state; the 07-21 doc and the fork ADDENDUM remain valid for
# WHO/HOW and the fork chat. State of record through 299a8b6.

## WHO/HOW — unchanged, load-bearing, two additions.
Owner rules by artifact and one-liners; Claude designs, audits, authors,
runs the in-container pipeline; CC codes STEP-0-first; engine is sole
card-fact authority; heredoc over download; ls -lt before mv; verbatim
tails. NEW: (1) curl URLs with query strings MUST be double-quoted —
zsh globs the question mark ("no matches found"). (2) Off-ledger commit
runs are now routine — the ledger books them from push ranges; the mv
"No such file" errors they cause are expected noise, EXCEPT when a file
was never downloaded at all (b1c7_i1 incident below: always diff the
modified-file count against the manifest before commit).

## HEADLINE: THE B1 CUTSCENE REGEN PROGRAM IS LIVE. 52/52.
One session, three sources (openart, Gemini, MJ-adjacent), 26 panels in
both orientations, defeat pair included, all three free wins shipped
(b1c3_v1, b1c4_v1, b1c5_v1 — first-ever files, went live same-day).
T56 shipped by CC (a1f8a1c): cutImgUrl selects _wide at viewport 1024
and above, resolve-once-per-panel, T54 layered 404-fallback to mobile,
inline stamp literal (the de-facto cutscene stamp — there is NO shared
v-constant in the codebase; the convention is per-asset inline literals,
codex assets sit at v2). c3_i3 board override deleted; the arena panel
is a true panel. Migration 1d79a48 swapped 21 mobiles + stamp v3;
completion 299a8b6 swapped the missed b1c7_i1 + stamp v4. CURRENT
CUTSCENE STAMP: v4. Engine untouched all session, byte-identical proofs
on both CC commits, 40.9/59.1 anchor held.

## COMMIT LINEAGE THIS SESSION (from d174f0b)
d174f0b ch1 wides x4. b2991d2 ch2 wides x4. Off-ledger: ch3 wides+free
win (inside b2991d2..fc671cc). d389409 ch4 wides+free win. Off-ledger:
c5 wides x4, c6 wides x3, c7_v1 wide (inside dbd5552..2e79764).
2e79764 c7_i1+i2 wides. 733be80 c5_v1 free win + c3_i3 + defeat wide.
62fb84f VANARA_CARD_DB_v1 (P13-gated). a1f8a1c T56 (CC). 1d79a48
migration x21 + v3. 299a8b6 b1c7_i1 + v4.

## RULINGS THIS SESSION (by artifact unless noted)
- SHUKRACHARYA EYES: ONE-BLIND (his right), ruled by the c7_i1 rerolls.
  Consequence: shipped c3_i1 pair (both-blind) is OFF-CANON — reroll
  debt, both orientations. Grey-crimson robes and topknot are c7-local;
  c3 stays loose-haired.
- MOHINI SILHOUETTE LAW: enforced twice. The c6 full-face pair is HELD
  (drawer / amend — UNRULED). The c7_v1 challenger mobile (lit profile)
  cannot ship as-is. Law verbatim: silhouette ONLY, c7_v1 only.
- c7_v1 INCUMBENT vs CHALLENGER: UNRULED. Incumbent (crowd-of-arms,
  elephants, law-clean both) is processed and live. Challenger
  (colossal hands, grail-read goblet) needs a mobile face-kill reroll
  if chosen; its wide is a zero-risk byte-swap (never cached).
- c5_i3: single-serpent supersedes the eye-field spec (reroll direction
  = ruling). Bonus: reads as Vasuki in the poison — c4 continuity.
- c6_i1: Dhanvantari-bearer supersedes vessel-alone. Reverence gate
  passed at full marks (he is on the named list).
- BLUE-SKIN DISTINCTION (ledger-grade): blue-hued devas in myth-apt
  context (churning lines, our storm-grey Indra canon) are defensible;
  a recognizable avatara in an anachronistic context is not (c3 arena
  Krishna-read, rerolled; c2_v1 Christ-figure, rerolled).
- KEEP-ALL PATTERN: ch1 register drifts (Spartan mobile, Rajput fort,
  i1 architecture split, i3 inverted beat) owner-kept and precedent-
  setting for later batches. Indra storm-skin is character canon.
- CRESCENT STAFF (defeat pair Brihaspati): UNRULED keep/reroll. Drifts
  from the c1/c2 plain danda; defeat is the most-seen panel.
- VESSEL SHAPE: no canon exists; four shapes shipped. Post-launch
  cleanup candidate, not a block.

## LAWS ADDED THIS SESSION
1. SIGNATURE/WATERMARK INPAINT LANE proven: color- or luminance-masked
   strokes + TELEA, machine-verified residuals. About 12 saves, zero
   forced regens. Corner-scan (thin bright/dark clusters) is standing
   step for every intake; battlefield/bioluminescent palettes false-
   positive — cross-check against source by eye.
2. GEMINI INTAKE: sparkle-watermark check is step zero (pale 4-point
   star, lower-right region, semi-transparent). Gemini wides arrive
   16:9 — cover-crop to 3:2 is a content decision, call it explicitly.
   Precision beats shotgun: mask the star, never carpet-patch a
   quadrant (one reverted incident).
3. SCRIPT-ON-PAGES is inpaint-tractable (defeat mobile book: dark rows
   on pale parchment, two passes). Carved-in-stone stays reroll-class.
4. Swap+stamp-same-commit executed twice under the per-asset literal
   convention (v2 to v3 to v4). The perl one-liner conditioned on the
   assets/story line is the tool; grep -c the new value = 1 is the gate.
5. Pillow/OpenCV processing standard: cover-crop to 896x1344 / 1344x896,
   JPEG quality-search 200-280KB. Below q78 = device-glance flag.

## DEVICE-GLANCE LIST (owner, on the live game)
MANDATORY: defeat mobile book pages (text ghosts); c7_i2 both BR
quadrants (sparkle survival — my crop-verify lane was dead; machine
verdict incomplete). Then: first wide renders at 1024+, zari gold-bands
c2_i2, c4_i2 wide (q74), c7_i1 wide far-eye, inpainted corners c1/c2,
c3_i3 relief bands (densest carving since Blueprint).

## THE VANARA FRONT (next mountain, already staged)
- docs/VANARA_CARD_DB_v1.md on main (62fb84f): launch 22 with epithets,
  ROSTER-sourced, P13-GATED. Watchlist: Leap never pulled; Rama Naam
  engine-fair is flat +2; Hanuman passive wording; Angad truncated.
- TASK P13 (in the doc, read-only) NOT YET RUN. It gates the frame run
  and any codex/ruling citation.
- THE VANARA WAVE ART BATCH IS ALREADY IN DOWNLOADS — roughly 20 files,
  EVERY ONE (1)-suffixed. The suffix trap is LIVE across the whole
  batch: originals may coexist; ls -lt verification before every mv is
  doing real work that session. Includes wave names (Gaja, Gavaksha,
  Kumuda, Dronagiri) — so it is the WAVE roster, not launch frames;
  reconcile against WAVE1_ROSTER v0.2 + vanara_review before intake.
- Frame program state: Deva 44/44, Asura 33/44 (Mahishi next, closes
  the T5 pair; Ravana/Kumbhakarna one-worders still owed), Vanara crest
  + roundel ground UNRULED.

## STANDING QUEUE (rough order)
1. Device pass (mandatory two first). Fix cycles are one-file cheap.
2. c3_i1 reroll pair (one-blind). Prompts in-thread 07-22.
3. Three one-worders: c7_v1 incumbent/challenger, Mohini drawer/amend,
   crescent staff keep/reroll.
4. P13 run + doc reconciliation.
5. Vanara wave intake (the (1) minefield) + frame-run direction doc.
6. Asura frames: Mahishi + remaining 10 + Rakta.
7. THE FORK CHAT (07-19 + ADDENDUM + 07-20 + 07-21 + THIS doc).
8. Section 5 DEPLOYMENT SCOPE — still the biggest unruled decision,
   now pressing harder: a fully-arted Book 1 is a shippable artifact.

## HOLD/DRAWER MANIFEST
Downloads is now art-inbox only (hold pile fully migrated). DRAWER:
Mohini c6 pair (Book 2 material, unruled), c7_v1 challenger pair.
Dead: c2_v1 Christ pair, c3 arena Krishna mobile, c5_i3 render pair,
c5_v1 seascape mobile, c7_i1 wuxia wide.
